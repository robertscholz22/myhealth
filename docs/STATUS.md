# MyHealth — implementation status

Source of truth for scope: `docs/PLAN.md` §5 (83 tasks, P0.1 … P9.6). Toolchain: `docs/TOOLCHAIN.md`.
One row per task. A task is **Done** only when its acceptance commands in PLAN §5 pass verbatim.

Legend — **Done**: `yes` / `no` / `n/a`. **Tests**: executed unit tests reported by `tools/verify.sh`
after the task. **APK MB**: debug APK size measured after the task. **Notes**: deviations, follow-ups.

Last update: 2026-09-14 — **P18.1 (Pose model, side view, animation clips for all 87 shipped
exercises)**: the pose vocabulary (`BodySegmentId`, `BodyFace {FRONT, BACK, SIDE}`, `BodyPose`)
moves into `domain/model/Body.kt` so the catalog can author animations against it, and `BodyPose`
gains **`rootScale`** alongside `rootOffsetX/Y` and `rootAngle` — a 213-unit figure cannot lie
horizontally inside the 100-wide box, and `an03` pins every keyframe inside it. The geometry stays
in `ui/common/body` and gains a **profile skeleton** (`BodyProfile.kt` / `BodyProfileMuscles.kt`):
all sixteen segment ids, the same joint heights as the front view, and 15 of the 16 muscle groups
(only `ADDUCTORS` is invisible edge-on). **44 clips** (`AnimationClips.kt`,
`AnimationClipsMobility.kt`, DSL and types in `AnimationPoses.kt`) cover **all 87 catalog ids**
through `ExerciseAnimations`, whose `init` check fails the build if the catalog and the table ever
drift apart. `bf01`…`bf05` are untouched (`MusclePaths.SIDE` is deliberately outside `pathsFor`) and
`bm04` was widened to the profile. `bash tools/verify.sh` → **VERIFY OK** (1011 unit tests, 0
failures, lint 0 errors; debug 88.6 MB, release 14.5 MB) and `:app:compileDebugAndroidTestKotlin`
green.
Before that, **P16.1 (My equipment + load progression: data and engines)**: DB **v7**
(`MIGRATION_6_7` — `profile.availableEquipmentJson`, `strength_set_log.feedback`, the
`exercise_progress` table keyed on the catalog id; backup schema 6 → 7), the pure
`ProgressionEngine` / `ProgressionDefaults` (double progression from a per-exercise `Feedback`, with
every value pinned by `pg01`…`pg12`) and `ExerciseSubstitution` / `EquipmentSetCodec` (`eq01`…`eq05`),
plus the repository side: `prescriptionFor`, the `exercise_progress` CRUD and `saveSetLogs`, which
applies exactly one progression step per exercise and persists it. `StrengthWorkoutSeeder` now
materialises the six built-ins against the owner's equipment, so an accepted strength suggestion
inherits the substitutions. Everything stays inert while `availableEquipmentJson` is `null` and no
feedback has been given.
Before that, **P14.9 (Instrumented + emulator pass)**: three new instrumented tests —
`ZonesScreenTest` (seeds a profile + a 40-minute HR≈140/3.0 m/s run, then More → Zones & paces shows
all five named zone rows and at least one measured pace band), `WorkoutScreenTest` (More → Strength
workouts seeds the six built-ins, opens "Upper A", moves its first exercise down, renames and saves,
then attaches the renamed workout to a `STRENGTH_UPPER` planned session through the planned-session
editor's `WorkoutPickerField` and confirms the Training card shows "Workout: …") and
`MuscleLoadCardTest` (a 220-AU run yesterday makes Load & Recovery's Muscle load card show "Legs are
loaded — an upper-body day fits today") — run via `bash tools/connected.sh emulator-5554`: **21/21
green**, including `MyHealthMigrationTest.migration_5_to_6_…` (the v6 FK survives Room's runtime
schema validation) and every pre-existing test. Two pre-existing-test/lint fixes needed along the
way: P14.6's new "Heart-rate zones" Settings section pushed the profile item two items further from
`AppPreferencesSection`, so `SettingsPersistenceTest` could no longer assume the "Sleep target" field
was still composed after scrolling past it — its two scroll assertions now call
`performScrollToNode` explicitly instead of a bare `performScrollTo()`/`onAllNodesWithText`; and
`zones_polarisation_hint`'s unescaped `Less than 70 % easy…` tripped lint's `StringFormatInvalid`
(`% e` parses as a space-flagged `%e` conversion), fixed by escaping it to `70%%`. `bash
tools/verify.sh` → **VERIFY OK** (960 unit tests, 0 failures, lint 0 errors; debug 91.0 MB, release
14.4 MB).
Before that, **P14.8 (Muscle-load card on Load & Recovery + Today hint)**: Load &
Recovery gains a "Muscle load" card — `BodyFigure` as a fresh/loaded/fatigued heat map
(`load / ref` per group, clamped `0..1`), the legend, the three most-loaded groups with their AU
and band, and a one-line hint ("Legs are loaded — an upper-body day fits today" /
"Legs are fresh — a lower-body day fits"), or an empty state when nothing has loaded a muscle in 14
days; `LoadViewModel` resolves today's `MuscleLoadState` directly against `ActivityRepository`/
`PlanRepository`/`StrengthRepository` — the same join `SuggestionMuscleResolver` does for the
suggester (P14.5), re-derived here since `ui/` may not import `data/repository` (`ArchitectureTest`),
exactly the seam `ZonesViewModel` used for pace bands (P14.6). Today's load card shows the same
hint as a one-line chip, but only when a lower-body group is actually `FATIGUED` (stricter than the
Load screen's `LOADED`-or-`FATIGUED` condition) — **960 unit tests** (`MuscleLoadUiStateTest`
`mlui01`…`mlui04`), lint clean, debug APK 89.8 MB / release 14.4 MB.
Before that, **P14.7 (Body figure, exercises, workouts, set logging)**: a pure
`ui/common/body/MusclePaths` (16 `MuscleGroup`s, ~22 simple-shape polygons per side in a normalised
100 × 220 box, plus the outline pieces) backs `BodyFigure` — a `Canvas`-only front+back silhouette
that blends `surfaceVariant → primary` by intensity (continuous, so it doubles as P14.8's heat map)
and hit-tests a tap to a group by its polygon bounding box — and `BodyFigureLegend`
(primary/secondary or fresh/loaded/fatigued). `highlightFor(exercise)`/`highlightFor(workout)`
(primary 1.0, secondary 0.35, union/max) are pure and shared by every screen that draws a figure.
**Exercises** (More entry): search + equipment chips + a tappable body-figure muscle filter, list
rows, and a detail screen with a large figure, equipment/pattern/flags/cue and "Add to workout…" (a
picker over the live workout list, appending a default-prescribed row and re-saving the whole
workout). **Strength workouts** (More entry): list with kind chip, exercise count, estimated
minutes and a figure thumbnail; new / duplicate / edit / delete (confirmed) / "Plan for a day"
(writes a `STRENGTH_*` `planned_session` with `workoutId` set, `targetDurationMin =
estimatedMinutes`, via the same `PlanRepository.upsertSession` manual sessions already use).
**Workout edit**: name, kind, ordered exercise rows (sets, reps *or* seconds fixed by the exercise's
own `isTimed`, load/bodyweight, rest, note) with move up/down/remove, `ExercisePickerSheet` (a
`ModalBottomSheet` over `ExerciseCatalog.search`, no ViewModel — ephemeral local state, the
`LinkActivitySheet` pattern), a live figure + estimated minutes, and validation. Both screens seed
the six built-ins on open through a plain `suspend () -> Int` handed in from
`AppGraph.strengthWorkoutSeeder::seed` — `ui/` cannot reference the concrete `data/` seeder type
(`ArchitectureTest`). **Planned session integration**: `PlannedSessionDraft` gained `workoutId`;
the editor offers a workout dropdown (`SessionType.isStrength()`) only for a `STRENGTH_*` session;
`PlannedSessionCard` shows the linked workout's name (`TrainingViewModel` now also observes
`strengthRepo.observeAll()` for the `workoutId -> name` lookup); "Mark done" on a session whose
workout has loaded opens `SetLogSheet` instead of completing immediately — one row per set,
pre-filled from the prescription, each skippable, writing `StrengthSetLog` rows via
`strengthRepo.insertSetLogs` before marking the session done; "Skip logging" or a session with no
workout behaves exactly as before. **Tests** (+11, 945 → 956): `MusclePathsTest` `bf01`…`bf05`
(coverage per group, every point inside the box, front/back side agrees with the enum, `highlightFor`
clamped and 1.0/0.35), `WorkoutEditUiStateTest` `swui01`…`swui06` (reorder, add with catalog
defaults, remove, `UPPER_A` → 44 min, reps-xor-seconds, blank name) — both pure, no Robolectric.
`bash tools/verify.sh` → `VERIFY OK` (956 tests, 0 failures, lint 0 errors; debug 89.3 MB, release
14.4 MB) and `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved**: no
`ExerciseDetailViewModel` is listed in P14.7's file set, so `ExerciseDetailScreen` reuses
`ExercisesViewModel` (query/filter state it does not need, plus the `workouts` list and
`addToWorkout` it does) rather than adding an unlisted file; the figure's polygon count came out at
~22 per side rather than the plan's "roughly 24 / 20" — coverage and box bounds are what `bf01`/`bf02`
pin, not the count (risk K4); and the reps-vs-seconds choice per row is fixed by the exercise's own
`isTimed` flag (as the built-in templates already assume), so the editor shows one numeric field per
row rather than a manual toggle that could disagree with the catalog.
Before that, **P14.6 (Zones & paces screen; zone/pace across the plan UI + Settings)**:
a new **Zones & paces** screen (More entry) shows the five named HR zones with bpm ranges and scheme
in plain words, a per-zone pace band with its confidence sentence, the Daniels E/M/T/I/R paces from
the current VDOT, a 28-day easy/moderate/hard polarisation bar with a "< 70 % easy" hint, and a
per-session-type target table; `ZonesViewModel` resolves the zone model, pace bands and VDOT the same
way the P14.3 suggester repository does, but directly against the domain repositories (`ui/` cannot
import `data/`). Settings gains a **Heart-rate zones** section: LTHR, four zone-boundary bpm fields
(all four ascending or none — `HrZoneBoundsStatus`) with a live `ZoneTable` preview. The suggestion
review card, the planned-session card and editor, and the Today plan card now show a target-zone
chip and, for a structured session, the one-line summary with an expandable warm-up/work/recovery/
cool-down step list (`PlannedSessionCard.kt`'s `TargetZoneChip`/`WorkoutStructureSection`, reused by
all three). Activity detail's HR-zone table is now named Z1–Z5 with bpm ranges from the model and
marks the row matching a linked planned session's target zone — **945 unit tests** (`ZonesUiStateTest`
`zui01`…`zui08`, `SettingsZoneValidationTest` `zui09`/`zui10`), lint clean, debug APK 89.3 MB / release
14.3 MB.
Before that, **P14.5 (Muscle-load engine + strength suggestion rules)**: every session,
endurance or strength, now deposits its TRIMP onto the 16 muscle groups through `MuscleDistribution`'s
share tables (or, with a workout, through the exercises' own primary/secondary sets), the deposits
decay with a 48-hour half-life and `MuscleLoadEngine` bands each group against `ref = max(0.35·CTL, 12)`
— which is what constraint **`C15`** reads to drop a leg day while the legs are fatigued, inside 36 h
after a hard run/match/ride or inside 48 h before a hard run, while `Scorer.muscleBonus` gives the
upper-body day `+0.10` in exactly that window and a placed `STRENGTH_*` session proposes an
alternating built-in workout (`UPPER_A` → `UPPER_B` → …) that `accept` materialises into
`planned_session.workoutId` — **935 unit tests**, `sug28`'s baseline byte-identical with
`muscleLoad = null` (`sug39`/`sug40`), lint clean, debug APK 88.2 MB / release 14.2 MB.
Before that, **P14.4 (Exercise catalog, muscle groups, workout templates)**: a
code-resident `ExerciseCatalog` of **54 exercises** (split upper/lower/core for R10) with primary and
secondary muscle groups, equipment, movement pattern, unilateral/timed flags and a coaching cue —
every one of the 16 muscle groups is someone's primary, 20 entries are bodyweight-only — plus
`search`/`filter`/`byId`/`byPrimary`, the computed `StrengthWorkout.estimatedMinutes`, the six
built-in templates (`UPPER_A` = 6 × 3 × 10 @ 90 s rest → **44 min**) and an idempotent
`StrengthWorkoutSeeder` wired as `AppGraph.strengthWorkoutSeeder` — **913 unit tests**, lint clean,
debug APK 88.2 MB / release 14.2 MB.
Before that, **P14.3 (Structured workouts + interval catalog + suggester wiring)**:
`IntervalCatalog`'s eleven templates and `IntervalBuilder`'s seven selection rules turn a placed
`INTERVAL_RUN` / `TEMPO_RUN` / `BIKE_INTERVALS` / `TRAINER_SESSION` into a `WorkoutStructure` —
"5 × 1000 m @ 3:54" with a warm-up, a jog recovery and a cool-down — with pace targets from the
measured bands or Daniels, power targets from the FTP, and a zone-only fallback when the athlete has
neither; the suggester attaches `structureJson` + `targetPaceSecPerKm` (candidate generation, scoring
and constraints untouched), three new rationale ids explain it, and `accept` carries the structure
into `planned_session` — **898 unit tests**, `sug01`…`sug33` / `ar01`…`ar08` byte-identical
(`sug28`), lint clean, debug APK 88.2 MB / release 14.2 MB.
Before that, **P14.2 (Zone ↔ pace engine + Daniels paces)**: `DanielsPaces`
inverts the §3.4 VDOT polynomial into the E/M/T/I/R pace table (VDOT 50 → 334/268/255/234/219
s/km) and `PaceZoneEngine` turns 90 days of HR+speed streams into a measured pace band per heart-rate
zone — 20 s HR-lag pairing, 10-minute warm-up skip, weighted median + IQR, a HIGH/MEDIUM/LOW ladder
blended against the Daniels anchor, and `recommendedPaceFor(sessionType)` for the plan UI —
**884 unit tests**, lint clean, debug APK 88.2 MB / release 14.2 MB.
Before that, **P14.1 (Zones, schema v6, strength storage)**: the five-zone
`HrZoneModel` (Karvonen / Friel / manual) whose default bands are the ones `timeInZones` has drawn
since P2.9, `SessionZoneTargets`, the `WorkoutStructure` JSON model + codec, DB **version 6**
(`MIGRATION_5_6`: `strength_workout`, `strength_workout_exercise`, `strength_set_log`, two zone
columns on `profile`, three prescription columns on `suggested_session`, `structureJson` +
`workoutId` on `planned_session` — the FK added by `ALTER TABLE`, no table recreation), the
strength repository wired as `AppGraph.strengthRepo`, and `BackupFile.CURRENT_SCHEMA_VERSION` 5 → 6
— **860 unit tests**, lint clean, debug APK 87.3 MB / release 14.2 MB.
Before that, **P12.4 (Cycling workouts: UI)**: Settings gains a FTP override + indoor
trainer + "Rides / week" (the `CYCLE` cap is now reachable), Activity detail gains a power card +
power chart + rpm cadence, a new Bike screen (More → "Bike & power") shows the FTP card and the
power/time PR tables, and the Goals editor's `BIKE_EVENT` gets a real distance/time form —
**828 unit tests**, lint clean, debug APK 88.9 MB / release 14.1 MB.
Before that, **P12.3 (Cycling workouts: suggestions)**: the four cycling rows of the
session catalog behind a gate (`CYCLE` cap or a `BIKE_*` goal), `BikeRules` (indoor season, trainer,
`C14` spacing), a second phase table for cycling goals, the `BIKE_*` rationale ids and the two new
profile fields in `inputsHash` — **824 unit tests**, `sug01`…`sug26` byte-identical (`sug28`).
Before that, **P12.2 (Cycling workouts: engines)**: `SplitFinder` extracted from
`RunningBestEngine`, `FtpEstimator` + `BikeBestEngine` + `BikeDefaults`, the `POWER_TSS` rung of
the TRIMP ladder, ride bests and the FTP resolved ahead of TRIMP in `LoadRecomputeService`, the
three cycling goals in `GoalProgress`, and power-based `completedKcal` (807 tests). Before that,
**P12.1 (Cycling workouts: data + sources)**, see the P12 table below:
DB **version 5** (`MIGRATION_4_5`), cycling power through every source (CSV / FIT / Health
Connect), the `ride_best` table, `PowerMath`, the four `SessionType` / three `GoalType` /
`LoadMethod.POWER_TSS` enum members, and `BackupFile.CURRENT_SCHEMA_VERSION` 3 → 5 —
**775 unit tests**, lint clean, debug APK 88.0 MB / release 14.0 MB.

Previous: 2026-09-12 — **polish batch** (P8.1 + P8.6 + P8.7 + POLISH-8 + POLISH-9, see
`docs/VERIFICATION.md` "Polish batch"): every user-facing string in `ui/**` moved into
`strings.xml` (43 → 778) with `HardcodedText` as a lint **error**; consistent empty/loading/error
states, pull-to-refresh on Today and Activities, shared 16 dp / 12 dp spacing tokens and a redrawn
adaptive launcher icon; the minified release build (**13.9 MB** vs 87.2 MB debug) wired into
`tools/verify.sh` and smoke-tested on the emulator with a clean logcat. Suggester: new hard
constraint **C13** (no repeated session type on consecutive days, ≥ 48 h between strength sessions)
and the `PROPOSED`-batch lifecycle fixes — **691 unit tests**, lint clean. P8.8 (widget) skipped as
optional.

Previous: 2026-09-12 — **P8.2 + P8.3 charts** (see `docs/VERIFICATION.md` "Charts"): the
hand-drawn Compose-`Canvas` chart layer (`ui/common/charts/`) and its four call sites (Body, Load,
Activity detail, Running PRs) — **684 unit tests**, APK 86.1 MB, lint clean; all four screens were
screenshotted on `emulator-5554` against the existing seeded data.

Previous: 2026-09-12 — **fix batch 2 + P8.4 + P8.5** (see `docs/VERIFICATION.md` "Fix batch 2"):
BUG-8, POLISH-4/5/6/7 and NOTE-6 ("Scan from photo") fixed, JSON backup and ingredient FTS shipped —
**643 unit tests**, APK 86.1 MB, lint clean; the 1 → 2 migration was verified in place on the
running emulator without clearing app data.

Previous: 2026-09-12 — **P7 complete** (P7.1–P7.6): the Garmin FIT SDK wrapper, the FIT →
domain mapper (with a real binary `run_5k.fit` fixture, also published to `docs/testassets/`), the
header-driven Garmin CSV parser, the zip-in-zip archive walker, the chunked import pipeline with
`import_record` provenance, and the Import screen incl. the share-sheet intent filter — 620 unit
tests; the end-to-end FIT import was verified on `emulator-5554` through the real file picker.


## P0 — Bootstrap (prove the build works)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P0.1** Project skeleton and toolchain proof | opus | **yes** | 3 | 59.4 | Verified toolchain copied from the template; no version changed. |
| **P0.2** Test infrastructure | opus | **yes** | 3 | 59.4 | SanityTest (3 tests) + Fixtures.fixedClock; androidTest deps declared, not run (no emulator). |
| **P0.3** App shell: theme, Application, nav scaffold | opus | **yes** | 3 | 59.4 | AppGraph/LocalAppGraph/rememberVm, M3 + dynamic color, all 30 routes, 5 bottom-bar screens. |
| **P0.4** Room bring-up with KSP and schema export | opus | **yes** | 3 | 59.4 | BootstrapEntity -> app/schemas/com.myhealth.data.db.MyHealthDatabase/1.json. |
| **P0.5** Verification script and lint baseline | opus | **yes** | 3 | 59.4 | tools/verify.sh, app/lint.xml, this file. Lint: 0 errors; remaining warnings are pinned-version advisories + ObsoleteSdkInt on mipmap-anydpi-v26 (path fixed by PLAN P0.1). |

## P1 — Core data, profile, settings

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P1.1 Domain enums and core models | sonnet | **yes** | 12 | 59.7 | All §2.1 enums + §2.3 domain models transcribed across 9 model files + 4 util files; `ActivityStreams` hand-writes equals/hashCode (arrays); no `android`/`androidx`/`data` imports in `domain/`. |
| P1.2 Room entities A (profile, body, activity, health) | sonnet | **yes** | 12 | 60.3 | 9 entities + `Converters` (22 enums as TEXT with a never-throwing fallback, `List<String>` ⇄ CSV). All §2.2.1–§2.2.3 columns/indices/FKs; `idx_sleep_night` is the unique index §2.2.3 requires. |
| P1.3 Room entities B (calendar, plan, nutrition, derived) | sonnet | **yes** | 12 | 60.3 | 17 entities per §2.2.4–§2.2.6. CASCADE for owned children (overrides, template/log items, suggested sessions, streams, laps), SET_NULL for soft links, RESTRICT for `meal_template_item.ingredientId`. |
| P1.4 DAOs | sonnet | **yes** | 12 | 60.3 | 16 DAOs, 119 `@Query` (≥ 40 required). Every query from P1.4 present incl. `EventDao.observeOverlapping`, `IngredientDao.search`, `MealDao.observeDay` (`@Transaction` → `MealLogWithItems`), `RunningBestDao.observeBestPerDistance` (`MIN(timeSec) GROUP BY distanceMeters`), `ActivityDao.sumTrimpPerDay` → `DayTrimp`. |
| P1.5 Database, relations, schema v1 | opus | **yes** | 12 | 60.3 | `MyHealthDatabase` v1, 26 entities, `@TypeConverters`, `build(context, allowDestructiveMigration)` (destructive fallback only under `BuildConfig.DEBUG &&` flag). Bootstrap entity/DAO removed; `Migrations.ALL = emptyArray()` + documented convention; 3 relations; `schemas/…/1.json` regenerated (26 tables); androidTest compiles (not run — no emulator). |
| P1.6 Mappers entity ⇄ domain | sonnet | **yes** | 22 | 68.8 | 8 mapper files cover all 26 entities (`ProfileMappers`, `BodyMappers`, `ActivityMappers` incl. stream/lap JSON + relation, `HealthMappers` incl. sleep stages JSON, `CalendarMappers`, `PlanMappers` incl. rationale JSON, `NutritionMappers` incl. meal relations + warnings CSV, `LoadMappers`). `MapperRoundTripTest` has 10 round-trip cases (≥ 8 required). |
| P1.7 Repository interfaces + profile/body implementations | opus | **yes** | 46 | 62.2 | 16 interfaces in `domain/repository` (15 new + the existing `SettingsRepository`): reads are `Flow`, writes are `suspend … : Outcome<…>`, no blocking getters. `RoomProfileRepository` / `RoomBodyRepository` over the P1.4 DAOs + P1.6 mappers, `withContext(Dispatchers.IO)` (injectable, defaults to IO) for one-shots and `runCatchingApp` for writes. `IngestResult`, `MealItemInput`, `SyncKeys` are declared next to their interfaces so `domain/` needs no `data/` type. `RepositoryContractTest` (4) parses the interface sources; fake-DAO tests: profile (5), body (11). APK measured on a full `--rerun-tasks` build (the 68.8 MB in the P1.6/P1.8 rows came from incremental builds). |
| P1.8 DataStore settings | sonnet | **yes** | 22 | 68.8 | `domain/repository/SettingsRepository.kt` created now (minimal, per task instructions — P1.7 adds the rest); `AppSettings` extended with `garminDirectEnabled`/`hasCompletedOnboarding`. `data/prefs/{SettingsKeys,DataStoreSettingsRepository}.kt` on `androidx.datastore:datastore-preferences` (added to `app/build.gradle.kts`, no version change). Wired into `AppGraph.settings`. |
| P1.9 AppGraph wiring + ArchitectureTest | opus | **yes** | 46 | 62.2 | `AppGraph` now exposes `appScope`, `clock`, `zoneId`, `db`, `settings`, `profileRepo`, `bodyRepo`; `MyHealthApp` already constructed it (P0.3). `ArchitectureTest` (4 tests) resolves `app/src/main/java/com/myhealth` from `user.dir` (module dir or project root) and asserts: no `android`/`androidx`/`kotlinx.coroutines.android`/`com.myhealth.{data,ui,di}` import under `domain/`; no `com.myhealth.data.` import under `ui/`; no `com.myhealth.data.garmin` import outside `di/` and `data/garmin/`; plus a guard that the scan sees the files. Verified by injecting `import android.util.Log` into `Profile.kt` (test failed naming the file) and a blocking getter + Unit-returning write into `LoadRepository.kt` (contract test failed naming both lines), then reverting. |
| P1.10 Onboarding screen | sonnet | **yes** | 71 | 63.2 | `ui/onboarding/{OnboardingDraft,OnboardingUiState,OnboardingViewModel,OnboardingScreen}.kt`; pure `validate(draft, today)` (6 fields, `today` injectable for deterministic tests). 3 steps (identity → body → preferences); saves `Profile` (id 1) + an initial `BodyMeasurement` (source `MANUAL`) + `settingsRepo.setHasCompletedOnboarding(true)`, then emits a one-shot `OnboardingEvent.Saved`. New common composables `ui/common/{SectionCard,NumberField,DatePickerField,DropdownField}.kt` and a tiny `ui/common/PreferredSports.kt` codec (`Profile.preferredSportsJson` ⇄ `Map<SportGroup,Int>`) shared with Settings. `ui/nav/MyHealthNavHost.kt`: start destination now resolves from `profileRepo.observeProfile()` behind a `ProfileLoadState.Loading` gate (full-screen spinner, never flashes Today), bottom bar hidden while on `OnboardingRoute`, `onDone` pops with `popUpTo(OnboardingRoute){inclusive=true}`. `OnboardingValidationTest`: 14 cases (8 invalid across all 6 fields + boundary + valid cases). |
| P1.11 Settings screen + Body & Health v1 | sonnet | **yes** | 71 | 63.2 | `ui/settings/{SettingsUiState,SettingsViewModel,SettingsScreen,SettingsSections}.kt`: edits every `AppSettings` key (theme, sync interval, suggestion horizon, treadmill-in-PRs, OFF contact, garmin-direct flag, allow-destructive-migration) plus every profile field (name, sex, birth date, height, goal weight, goal pace, NEAT level, resting/max HR overrides, sleep target, mobility-on-rest-days, preferred-sports caps); writes go straight through `profileRepo.upsert` / `settingsRepo.update`. `ui/body/{BodyUiState,BodyViewModel,BodyScreen}.kt`: latest-weight card with goal-delta, FAB "Log weight" dialog (weight + optional body-fat%), plain last-90-days list with delete, no charts (P8.3). `ui/more/MoreScreen.kt` was already complete from an earlier task (all 11 destinations, dead links land on the existing `PlaceholderScreen`) — no change needed. `ui/nav/MyHealthNavHost.kt` wires `BodyRoute`→`BodyScreen`, `SettingsRoute`→`SettingsScreen`. `BodyUiStateTest`: 9 cases for `weightDeltaToGoalKg`, `sortedByRecencyDescending`, `withinLastDays`, and `BodyUiState.latest`/`deltaToGoalKg`. `grep -c TODO -r app/src/main/java/com/myhealth/ui` → **0** across all 28 files. **P1 roll-up: 11/11 done.** |

## P2 — Health Connect sync + activities

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P2.1 Health Connect availability, manifest and permission plumbing | opus | **yes** | 93 | 72.0 | `implementation(libs.health.connect)` added (connect-client 1.1.0, no version change). Manifest: `<queries>` for `com.google.android.apps.healthdata`, **19** `android.permission.health.*` read permissions (17 record types + `READ_HEALTH_DATA_HISTORY` + `READ_HEALTH_DATA_IN_BACKGROUND`; **no** exercise route, amendment A6), the rationale activity with `androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE`, and the `ViewPermissionUsageActivity` alias (`VIEW_PERMISSION_USAGE` + `HEALTH_PERMISSIONS`, `START_VIEW_PERMISSION_USAGE`). `data/healthconnect/HcPermissions.kt` builds `REQUIRED_CORE` from `HealthPermission.getReadPermission(X::class)` for 18 record classes plus `HISTORY`/`BACKGROUND` constants; `HealthConnectProvider` maps `getSdkStatus` → `HcStatus{AVAILABLE,UPDATE_REQUIRED,UNAVAILABLE}` and exposes `client()`, `granted()`, `permissionContract()`. `ui/settings/PermissionsRationaleActivity.kt` is a standalone Compose activity (5 new strings, data-stays-on-device text) with no `data` import. Wired as `AppGraph.healthConnect`. `HcPermissionsTest` (4 cases) asserts the manifest declares **exactly** `HcPermissions.ALL` and that no exercise-route permission exists — a drift between the two lists is invisible at build time and would otherwise only surface on the device. It also pins the fact that `StepsCadenceRecord` shares `READ_STEPS` (18 record classes → 17 distinct permission strings). |
| P2.2 Health Connect readers | opus | **yes** | 93 | 72.0 | `HcDto.kt` (plain `@Serializable` DTOs: `HcExercise`, `HcSample`/`HcHeartRateSample`, `HcLap`, `HcSegment`, `HcDailyTotals`, `HcDailySummary`, `HcSleep`/`HcSleepStage`, `HcBody`) so nothing above `data/healthconnect` sees a Health Connect type. `HcReader.kt`: interface `HcReader` (all four methods return `Outcome<…>`) + `HealthConnectReader`; `readAllRecords` pages while `pageToken` is non-null **and** non-empty; every read bounded by an explicit `TimeRangeFilter`; each session's HR/distance/speed/cadence/elevation/kcal read with that session's own window and samples clipped to it; `hcCatching` maps `SecurityException → HealthConnectPermissionDenied`, `RemoteException`/`IllegalStateException → HealthConnectUnavailable`, `IOException → Storage`, rest → `Unexpected`, re-throwing `CancellationException`. `HcAggregates.kt`: `DAILY_METRICS` (steps/total kcal/active kcal/distance/floors) via **one** `aggregateGroupByPeriod(Period.ofDays(1))` with a local-time filter, plus raw reads of resting HR, SpO2, respiratory rate, HRV and VO2max folded into local-day buckets. No `!!` anywhere in `data/healthconnect`. |
| P2.3 Health Connect → domain mapping | sonnet | **yes** | 93 | 72.0 | `ExerciseTypeMap` implements the P2.3 table by **constant name** only (never an int literal, risk R3); soccer title containing `match`/`spiel` upgrades to `SOCCER_MATCH`; everything outside the table → `OTHER`. `SleepStageMap` covers all 8 `STAGE_TYPE_*` with `UNKNOWN` fallback. `HcMapper.kt`: interface `HcMapper` + `HealthConnectMapper` producing `ActivitySourceRecord(HEALTH_CONNECT, externalId = metadata.id, payloadJson = HcExercisePayload snapshot)`, a normalized `ActivitySession` (day = local day of start, `dedupeBucket = "group|start/300000"`, HR axis down-sampled to ≥ 1 s with offsets from start, speed/cadence resampled onto that axis, avg speed falls back to distance/duration), `DailyHealthSummary`, `SleepRecord` (same-`night` sessions merged, earliest id kept) and `BodyMeasurement` (weight + body fat at one instant merged). `HcMapperTest`: **18** JVM-only cases (≥ 10 required) incl. unknown type → OTHER, empty HR samples, midnight-crossing session, "Spiel" → SOCCER_MATCH, treadmill + full type table, stage mapping, same-night merge, kcal channels. **P2.4 addendum**: `ROWING`/`ROWING_MACHINE` → `SportType.ROWING` and `YOGA`/`PILATES`/`STRETCHING` → `SportType.MOBILITY` added to `ExerciseTypeMap`, with cases `hc19`/`hc20` (20 tests total). |
| P2.4 Activity de-duplication and merge engine | opus | **yes** | 127 | 72.0 | `domain/engine/activity/{DedupeKey,ActivityMatcher,ActivityMerger}.kt` implement §2.4 verbatim: 4-part predicate (symmetric — every tolerance is taken from the larger of the two values), 5-minute bucket with `n-1..n+1` lookup, the field-precedence table as `MergeFieldGroup` (STREAMS / MOTION / ENERGY / IDENTITY / MANUAL_FIRST; an unlisted source ranks last), `userEditedFields` protection and `mergedSources` accumulation. The canonical row takes part in the merge as a pseudo-candidate ranked by the **best** source in its own `mergedSources`, which is what makes HC→FIT and FIT→HC produce byte-identical rows and stops a re-ingest of one source from pulling back a field the other legitimately won. `ActivityMatcherTest` (`dedup01`–`dedup07`) + `ActivityMergerTest` (`dedup08`–`dedup10` plus order-independence, MANUAL precedence and re-ingest cases) = 14 tests. |
| P2.5 Activity repository and ingestion pipeline | opus | **yes** | 127 | 72.0 | `ActivityIngestor` (+ `TransactionRunner` / `RoomTransactionRunner` / `DirectTransactionRunner` so it is testable without Room) does insert-IGNORE on `(source, externalId)`, re-merges known records with a refreshed `payloadJson`, looks candidates up by bucket ±1 + `ActivityMatcher`, merges and upserts the row, streams and laps in one `db.withTransaction {}`; `removeSourceRecord` deletes the canonical row when the last source goes and otherwise recomputes `mergedSourcesCsv`/`primarySource`. `RoomActivityRepository` pins `rpe`/`note` in `userEditedFieldsCsv` on manual edits. `RoomHealthRepository` never overwrites a non-null `bodyBattery`/`stressAvg`/`trainingReadiness` with null and merges same-night sleep by `night`. `RoomSyncStateRepository` is read-modify-write per field. `ActivityIngestorTest` (9) over `FakeActivityDao`. **Interface changes** (reported, see notes below the table). |
| P2.6 Incremental sync with changes tokens + backfill | opus | **yes** | 127 | 72.0 | `HcReader` gained `getChangesToken(kinds)` / `getChanges(token)` returning the Health-Connect-free `HcChanges(upserts, deletedIds, nextToken, hasMore, expired)`, so `HcSyncService` is driven by a fake in tests. Four channels, one token each (`hc.exercise/daily/sleep/body`): no token → bounded 30-day read then token; otherwise page while `hasMore`, persisting `nextChangesToken` after every page; `expired` (or a failure whose message mentions the token) → clear token, **one** 30-day re-read, fresh token, `lastError` recorded, never a loop. Daily rows are re-aggregated for `[minChangedDay, today]` capped at 30 days (a deletion carries no timestamp, so the whole window is re-read). `HcBackfill.run(fromDay)` requires `READ_HEALTH_DATA_HISTORY`, reads 14-day windows newest→oldest with a 250 ms pause and persists `backfillCompleteDay` per window (resumable). `HcSyncServiceTest` (9) covers first-run token acquisition, incremental upsert, paging, deletions, expiry recovery (exactly one re-read), a failing channel, and an interrupted+resumed backfill. Wired into `AppGraph` (`syncStateRepo`, `healthRepo`, `ingestor`, `activityRepo`, `hcMapper`, `hcReader`, `hcSync`, `hcBackfill` — the last three null when the SDK is unavailable). |
| P2.7 WorkManager sync scheduling | sonnet | **yes** | 134 | 102.0 | `implementation(libs.work.runtime)` added (work-runtime-ktx 2.11.2, no version change). `sync/HealthSyncWorker.kt`: `CoroutineWorker` pulling `(applicationContext as MyHealthApp).graph`; `KEY_BACKFILL_FROM_DAY` input runs `HcBackfill.run` instead of `HcSyncService.syncIncremental`; pure `mapOutcome(Outcome<*>): WorkerVerdict` (`Success`/`Retry`/`Failure(reason)`) — `HealthConnectUnavailable` → retry, everything else → failure with `AppError.describe()` as the output data reason (`KEY_FAILURE_REASON`), `Ok` → success. `sync/SyncScheduler.kt`: `schedulePeriodic(intervalHours)` (`hc_sync`, `ExistingPeriodicWorkPolicy.UPDATE`, `BatteryNotLow`, `BackoffPolicy.EXPONENTIAL` 15 min), `syncNow()` (`hc_sync_now`, `KEEP`), `backfill(fromDay)` (`hc_backfill`, `REPLACE`), `observeState()`/`observeBackfillState()` (`Flow<SyncWorkState>` from `getWorkInfosForUniqueWorkFlow`). `MyHealthApp` implements `Configuration.Provider` (default `Configuration` — no `WorkerFactory` needed) and schedules/re-schedules periodic sync from `settings.settings.map{syncIntervalHours}.distinctUntilChanged()` in `appScope`; manifest disables the default `androidx.startup` `WorkManagerInitializer` entry (`tools:node="remove"`, needed `xmlns:tools` added to the manifest root) since WorkManager is only ever reached through `AppGraph.syncScheduler`. Wired as `AppGraph.syncScheduler`. `HealthSyncWorkerTest` (3): success/retry/failure-with-reason for `mapOutcome`. **Fix (reported by P2.6/P1.7)**: `RoomBodyRepository.upsertAll` inserted HC rows with `id = 0`, and Room's `@Upsert` falls back to an update **by primary key** on the `(source, externalId)` unique-index conflict — which a fresh `id = 0` never matches — so a corrected weight from Health Connect was silently dropped. Fixed with `BodyDao.upsertAllResolvingIds` (a `@Transaction` default method that resolves each row's existing id via the already-present `getBySourceExternalId` before calling `upsertAll`); `RoomBodyRepository.upsertAll` now calls it. `RoomHealthRepository.upsertSleep` was checked for the same pattern — already safe, since it resolves `stored.id` via `getByNight` before every upsert. New test `RoomBodyRepositoryTest.upsertAll_updates_existing_row_by_external_id`. |

**P2.4–P2.6 interface changes** (P1.7 contracts extended, all reported per R9):
`ActivityRepository.ingest` now takes `List<ActivityIngestItem>` (source record **+** the normalized candidate session) instead of `List<ActivitySourceRecord>` — the repository cannot decode `payloadJson`, whose schema belongs to the source, and the merger needs field values, not JSON. Added `ActivityRepository.removeSourceRecord`, `HealthRepository.deleteSleepByExternalId`, `BodyRepository.deleteByExternalId` (Health Connect deletions, P2.6) and `IngestResult.plus`/`EMPTY`. New DAO methods only (no schema change, `schemas/…/1.json` untouched): `ActivityDao.{insertSourceRecordIgnoring,updateSourceRecord,deleteSourceRecord,getRangeFull}`, `SyncStateDao.observe`, `SleepDao.getBySourceExternalId`.
Known limitation: when one of several sources is deleted, the canonical row keeps its field values and only its `mergedSourcesCsv`/`primarySource` are recomputed — un-merging would need the payloads decoded back into field values, which only the owning source's mapper can do. The next ingest of any remaining source corrects the row.
| P2.8 Integrations screen | sonnet | **yes** | 134 | 102.0 | `ui/settings/{IntegrationsScreen,IntegrationsViewModel,IntegrationsUiState}.kt`. `di/HcIntegration.kt` adds the `ui`-safe seam over Health Connect (`ui/` must never import `com.myhealth.data.*`, per `ArchitectureTest`): a `di`-owned `HcStatus` enum plus `interface HcIntegration { status(), granted(), allPermissions, permissionContract() }`, implemented by `HealthConnectIntegration` wrapping `HealthConnectProvider`; exposed as `AppGraph.hcIntegration`. Screen shows the three SDK states (available / update-required / not-installed, the latter two with a button to `market://details?id=com.google.android.apps.healthdata`), the granted-permission list with check/cross icons and human labels (`permissionLabel`, a curated map over `HcPermissions.ALL` with a title-cased fallback so no permission is ever unlabeled), "Grant permissions" via `rememberLauncherForActivityResult(hcIntegration.permissionContract())`, "Open Health Connect settings" (`Intent("androidx.health.ACTION_HEALTH_CONNECT_SETTINGS")` — the SDK's own accessor is a deprecated method, not a usable constant, so the literal action string is used directly per the task's fallback), "Sync now" with a running indicator from `SyncScheduler.observeState()`, last-success time + last error per `SyncKeys.HC_*` from `SyncStateRepository.observeAll()`, and a backfill section (start-date picker defaulting to 365 days ago, a note about the history permission, "Start backfill", and `backfillCompleteDay` shown as "Backfilled back to `<date>`"). Route wired into `ui/nav/MyHealthNavHost.kt` (`IntegrationsRoute` → `IntegrationsScreen()`); `ui/more/MoreScreen.kt` already linked to it. Three `@Preview`s (available/update-required/not-installed). `IntegrationsUiStateTest` (3): every permission in `HcPermissions.ALL` gets a non-blank label, two curated labels pinned exactly, one unrecognised permission exercises the fallback — this test file imports `HcPermissions` directly since `ArchitectureTest` only scans `src/main`, not `src/test`. |
| P2.9 Activities list and detail screens | sonnet | **yes** | 143 | 102.0 | `domain/engine/load/HrZones.kt`: pure `timeInZones(offsetsSec, hr, hrRest, hrMax): List<Double>` (5 bands `<60,60-70,70-80,80-90,>=90 %` of HRR; interval `offsetsSec[i+1]-offsetsSec[i]` capped at 60 s and attributed to `hr[i]`'s band; a `null` reading skips its interval). `ui/common/{SportIcon,SourceBadge,StatTile,EmptyState,ErrorBanner}.kt` (the last three were also on P2.10's list — built once, used by both). `ui/activities/{ActivitiesUiState,ActivitiesViewModel,ActivitiesScreen}.kt`: sport-group filter chips (`All` + every `SportGroup`, a superset of the task text's abbreviated "All/Soccer/Run/Strength/Cycle/Other" so Walk/Swim stay filterable too), month-grouped list (`groupByMonth`, pure), row = icon, title (falls back to `sportType` display name), start time, duration, distance (km, 2 dp), avg HR, TRIMP, merged-source badges; empty state with a "Sync now" action wired to `SyncScheduler.syncNow()`. `ui/activities/{ActivityDetailUiState,ActivityDetailViewModel,ActivityDetailScreen}.kt`: header stats (duration/elapsed/distance/avg-max HR/pace-or-speed/cadence/elevation/calories/TRIMP+method), source badges, title edit (upserts the session with `title` added to `userEditedFields` via `ActivityFields.TITLE` so a later merge cannot clobber it, §2.4), notes edit (`ActivityRepository.setNote`), laps table when `laps` is non-empty, an HR summary table (min/avg/max from the stream plus the `timeInZones` band table; hrRest/hrMax from `Profile.restingHrManual ?: 60` / `Profile.estimatedMaxHr(ageYears)`, which already folds in `maxHrManual`), a "RPE — coming soon" placeholder section, and delete with an `AlertDialog` confirmation. Wired into `ui/nav/MyHealthNavHost.kt` (`ActivitiesRoute`, `ActivityDetailRoute(id)`); `ui/more/MoreScreen.kt` already linked to Activities. **Interface change (R9)**: `ActivitySummary` (`domain/model/Activity.kt`) gained `mergedSources: List<ActivitySource>` (populated in `ActivitySessionEntity.toSummary()`, no schema change — the column already existed) because the list row's source badges are specified against `mergedSources`, which the pre-P2.9 `ActivitySummary` did not carry (only `primarySource`); the one existing call site was updated, nothing else referenced the constructor. `HrZonesTest` (4), `ActivitiesUiStateTest` (3: month grouping, pace formatting x2). |
| P2.10 Today screen v1 | sonnet | **yes** | 143 | 102.0 | `ui/today/{TodayUiState,TodayViewModel,TodayScreen}.kt`. `TodayViewModel.state` is one `StateFlow<TodayUiState>` built from nested `combine(...)` (two 4-/3-flow typed `combine`s merged by a third, since `combine` has no 7-flow typed overload) + `stateIn(WhileSubscribed(5_000))` over `activityRepo.observeRange(today,today)`, `bodyRepo.observeLatest()`, `profileRepo.observeProfile()`, `healthRepo.observeDay(today)`, `healthRepo.observeLatestSleep()`, `syncStateRepo.observeAll()`, `syncScheduler.observeState()`. Sections in order: sync-status banner (`ErrorBanner` + "Sync now" on error, else `lastSyncedLabel` text + "Sync now"), nutrition placeholder ("Targets arrive in P4"), plan/suggestion placeholder, recovery placeholder, load placeholder, today's activities (tap → `ActivityDetailRoute`), body chip (`weightChipLabel`: latest weight + signed delta to goal, reusing `ui/body`'s `weightDeltaToGoalKg`), sleep tile (`StatTile` in hours). Pull-to-refresh skipped (optional per task). Two `@Preview`s (populated + empty/error). Wired into `ui/nav/MyHealthNavHost.kt` (`TodayRoute` now takes `onOpenActivity`). `TodayUiStateTest` (2: `lastSyncedLabel` formatted-vs-never-synced, `weightChipLabel` signed-delta/no-goal/no-weight). **P2 roll-up: 10/10 done.** |

## P3 — Calendar, events, linking

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P3.1 Recurrence expander | opus | **yes** | 179 | 102.0 | `domain/engine/calendar/{RecurrenceRule,RecurrenceExpander}.kt`. `RecurrenceRule.parse/format` cover the §2.2.4 subset (`FREQ=WEEKLY\|DAILY;BYDAY;INTERVAL;UNTIL;COUNT`), case-insensitive, tolerating `UNTIL=yyyy-MM-dd` and an instant suffix; an unsupported/broken rule parses to `null` (event then renders as a one-off) and a bad `BYDAY`/`INTERVAL` token degrades to the default instead of failing. `expand(event, overrides, from, to)` is pure `LocalDate` arithmetic: `INTERVAL` counts ISO weeks from the event's own week (`startOfIsoWeek`), weekly `BYDAY` defaults to the start weekday, `UNTIL` is inclusive and the earlier of rule-`UNTIL` / `recurrenceUntilDay` wins, `COUNT` counts from the series start (not from the window), candidates are generated 31 days past `to` so a `MOVE` backwards into the window still lands, and the result is hard-capped at `MAX_OCCURRENCES = 1000`. `linkedActivityId` is carried only on the occurrence whose day is the event's own `startDay` (per-occurrence links are materialized child events, §2.2.4). `RecurrenceExpanderTest` = the 10 named cases (`rec01…rec10`, incl. the 1000-guard and a 2026-03-29 DST week); `RecurrenceRuleTest` = 4 parse/format round-trips. |
| P3.2 Calendar repository and day aggregation | opus | **yes** | 179 | 102.0 | `data/repository/{CalendarAggregator,RoomCalendarRepository,RoomPlanRepository}.kt`. `CalendarAggregator.aggregate(...)` is a pure function over lists (events + overrides expanded via P3.1, planned sessions, activity summaries, meal summaries, target snapshots, daily loads, sleep) → `Map<Long, CalendarDay>` with **every** day of the range present (`CalendarDay.empty`), intake summed from the meal snapshots, all-day events sorted first. `RoomCalendarRepository.observeRange` combines the six DAO flows + sleep in three typed `combine` groups (no 7-flow overload) and aggregates in `withContext(Dispatchers.Default)`; plus `observeDay`, `observeOccurrences`, `observeKeyEvents`, `observeLinkProposals` (P3.3), event/override CRUD (`upsertEvent` stamps timestamps and denormalizes `recurrenceUntilDay` from the rule) and `linkActivity`, which upgrades a linked activity to `SOCCER_MATCH` for `SOCCER_MATCH` events through the new `ActivityRepository.setSportType` (pins `sportType` in `userEditedFields` and re-derives `sportGroup`/`dedupeBucket`, §2.4). `RoomPlanRepository` implements `PlanRepository` incl. "at most one ACTIVE plan" (activating archives the others via the new `PlanDao.archiveOtherActivePlans`). Added: `CalendarDay.sleep` + `CalendarDay.empty()` (§2.3 extension), `PlanDao.{archiveOtherActivePlans,getSessionRange}`, `MealLogWithItems.toSummary()`. `CalendarAggregatorTest` (5 pure cases incl. empty range, a day holding every kind, expansion, overrides, sorting/out-of-range), `RoomPlanRepositoryTest` (6, fake DAO), `RoomCalendarRepositoryTest` (5, fake event/activity DAOs). |
| P3.3 Event ↔ activity auto-link engine | opus | **yes** | 179 | 102.0 | `domain/engine/calendar/EventActivityLinker.kt` (+ `LinkProposal`). Implements P3.3 verbatim: `0.5*overlapRatio + 0.3*sportScore + 0.2*startScore`, sport 1.0/0.8/0.0, `startScore = 1 - clamp(|Δstart|/90min)`, all-day events `overlapRatio = 1.0` + `startScore = 0.5`; candidates are same-day activities that no occurrence already links, occurrences that already carry a link are skipped; propose ≥ 0.55, auto-apply ≥ 0.80 when the best is strictly unique (ties never auto-apply, and one activity is never auto-applied to two events). Confidence is rounded half-up to 6 dp before the comparison so an exact boundary (e.g. `0.25 + 0.3`) is not lost to binary representation. A timed event without a duration is assumed 60 min; a null event `sportType` scores 0. `EventActivityLinkerTest` = the 6 named cases (`link01…link06`, boundaries assert 0.55 and 0.80 exactly). `CalendarRepository.observeLinkProposals(from, to)` feeds P3.7. |
| P3.4 Calendar screen (month / week / agenda) | opus | **yes** | 192 | 102.0 | `ui/calendar/{CalendarGrid,CalendarUiState,CalendarViewModel,CalendarScreen,MonthGrid,WeekStrip,AgendaList,DayCell}.kt`. `monthGridDays(anchor)` is the pure grid helper: Monday-first ISO weeks, always 42 cells (6 rows × 7) from the Monday of the week holding the 1st, so every month fits and the height never changes between pages; `weekDays`, `visibleRange`, `monthTitle`/`weekTitle`/`fullDateTitle`/`formatMinuteOfDay` sit beside it. `DayCell` shows the day number (today filled, out-of-month dimmed), up to 4 marker dots (event/planned/activity/meal, from the pure `dayMarkers`) and the thin kcal-delta bar from the pure `kcalDeltaBar` (intake − target: ≤ 0 green, ≤ +300 amber, above red; `null` unless a target snapshot **and** logged intake both exist). Month and week mode each page horizontally with `HorizontalPager` (foundation, already in the BOM — no new dependency) over 1201 pages centred on the page the screen opens on; the settled page writes the anchor back to the VM and an anchor change scrolls the pager, so the two never fight. `CalendarViewModel` keeps `anchorDay`, `mode` and `selectedDay` in `SavedStateHandle` (new `di/rememberVmWithSavedState` + `createSavedStateHandle()`), recomputes the visible window on every change and re-subscribes with `flatMapLatest` to `calendarRepo.observeRange` (page ± one page of slack). Week mode = `WeekStrip` (7 selectable days) + `AgendaList` (events with time, planned with status, activities, meal count). FAB → `EventEditRoute(epochDay = selected)`, day tap → `DayDetailRoute`. `CalendarGridTest` (8: 42 cells/Monday start across 5 anchors, 1st + last of month, Feb 2028 leap, a month starting Monday, one starting Sunday, week days, visible range, title/time formats), `DayCellMarkersTest` (3). |
| P3.5 Day detail screen | sonnet | **yes** | 192 | 102.0 | `ui/calendar/{DayDetailScreen,DayDetailViewModel,DayDetailUiState}.kt` + `DayDetailSections.kt` (the seven section composables, split out to keep both files under the R10 400-line limit). `DayDetailViewModel` observes `calendarRepo.observeDay(day)` through `flatMapLatest` over a `SavedStateHandle`-backed day, so the ±1-day arrows in the top bar survive process death. Sections: Events (type chip, time/all-day, title, linked-activity badge, overflow edit → `EventEditRoute(id)` / link-unlink → P3.7 message / delete behind an `AlertDialog`), Planned (sport, type, status; overflow mark done / skip via `PlanRepository.setSessionStatus`, edit → `PlannedSessionEditRoute`), Activities (Activities-list row, tap → `ActivityDetailRoute`), Meals (per-slot kcal + protein, day total, tap → `NutritionDayRoute(day)`), Sleep (duration + stage minutes + score), Targets vs intake (kcal/protein/carbs/fat `LinearProgressIndicator` from the pure `targetProgressRows`/`progressFraction`, clamped to 1.0, with a "No target yet" state) and Load (TRIMP, ACWR, recovery score/band, flags). `@Preview`s for a fully populated fixture day (`previewCalendarDay`, also used by the P3.4 previews) and an empty day. Wired in the NavHost: `CalendarRoute` replaces its placeholder, `DayDetailRoute` gets the real screen, and Today's activities card links to the day detail (`TodayUiState.day`). `DayDetailUiStateTest` (2). |
| P3.6 Event editor with recurrence | sonnet | **yes** | 203 | 102.0 | `ui/calendar/{EventDraft,EventEditUiState,EventEditViewModel,EventEditScreen,RecurrencePicker}.kt` + `ui/common/{TimePickerField,DurationField}.kt`. `EventDraft` covers every field: type, title, date, optional time (`hasTime` + `startMinuteOfDay`), duration (min), location, sport (shown for `SOCCER_MATCH`/`SOCCER_TRAINING`/`RACE`, prefilled via `defaultSportTypeFor(type)` when the type changes), target distance (races, entered in km, stored ×1000 m), key-event switch, notes, and recurrence (`RecurrenceMode.NONE`/`WEEKLY`; "every n weeks" is the same `WEEKLY` mode with `recurrenceIntervalWeeks > 1`, matching the RFC subset's single `INTERVAL` field) + optional until date. Pure `validate(draft): Map<EventField,String>` (empty title, duration ≤ 0, until before start, `RACE` without distance, weekly rule with no weekday) and `EventDraft.recurrenceRule()` build the §2.2.4 rule via `RecurrenceRule.format()`/`untilDay`; `eventDraftFrom(event)` reloads an existing event (weekday/interval/until parsed back out of the stored rule string). `RecurrencePicker` is the Mo–Su `FilterChip` row + interval `NumberField` + `DatePickerField` (with a Clear button). `TimePickerField` wraps M3's `TimePicker` in a plain `Dialog` (no `TimePickerDialog` exists in this BOM, unlike `DatePickerDialog`); `DurationField` is a thin `NumberField` wrapper for whole minutes. `EventEditViewModel`: `id = -1` seeds a new draft from `epochDay` (or `Clock`-now); otherwise loads the event and records whether it recurs. Save calls `upsertEvent`; delete on a non-recurring event asks once and calls `deleteEvent`, a recurring event's dialog offers "This occurrence" (writes `EventOverride(action="SKIP", occurrenceDay)`, from the `epochDay` the screen was opened on) or "Whole series" (`deleteEvent`). Wired into `ui/nav/{NavRoutes,MyHealthNavHost}.kt`: `EventEditRoute` replaces its placeholder; `DayDetailNavActions.onEditEvent` now also carries the occurrence day so "this occurrence only" targets the row the user actually opened, not the event's own start day. `EventDraftValidationTest` (6: the 5 named invalid cases + 1 valid), `EventDraftRecurrenceTest` (3: weekly two days, every-2-weeks-with-until, none → null). |
| P3.7 Linking UI and link suggestions | sonnet | **yes** | 203 | 102.0 | `ui/calendar/LinkActivitySheet.kt` (+ pure `linkCandidatesFor`/`confidenceLabel`): a `ModalBottomSheet` for one event occurrence — the linker's proposals for that exact occurrence first (confidence % badge, descending), then the day's other activities as a manual fallback (activities already listed as a proposal are never duplicated), an "Unlink" row when the occurrence already carries a link; selecting a suggested row calls `linkActivity(eventId, activityId, AUTO_ACCEPTED)`, a fallback row `MANUAL`. Hooked into Day detail: `DayDetailViewModel` gained `observeLinkProposals(day, day)` + `openLinkSheet/closeLinkSheet/linkActivity/unlinkActivity` (the old placeholder `requestLink` snackbar is gone; `EventsSection`'s overflow "Link activity"/"Manage link" row now opens the sheet). Activity detail (`ActivityDetailViewModel` now also takes `CalendarRepository`): a "Linked event" card resolves the linked occurrence by observing the activity's own day and matching `linkedActivityId`; unlinked shows "Link to event…", opening a small local `EventPickerSheet` listing that day's events (`calendarRepo.linkActivity(eventId, activityId, MANUAL)` on selection). Today: `TodayViewModel` added `observeLinkProposals(today-7, today)` filtered to `confidence >= EventActivityLinker.PROPOSE_THRESHOLD` and to un-dismissed proposals (`dismissed: MutableStateFlow<Set<String>>`, keyed by `linkProposalKey` — VM-only, not persisted); `TodayUiState.linkSuggestions` drives a "Suggested links" card (shown only when non-empty) with Accept (`linkActivity(..., AUTO_ACCEPTED)`, which is what upgrades a `SOCCER_MATCH` event's activity sport via the repository's existing `setSportType` path) and Dismiss per row. `LinkSheetStateTest` (2: proposals-before-fallback-without-duplicates ordering, `confidenceLabel` rounding). **P3 roll-up: 7/7 done.** |

## P4 — Nutrition

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P4.1 Meal math and nutrition domain | sonnet | **yes** | 230 | 102.0 | `domain/engine/nutrition/MealMath.kt` (`MealQuantity`, `MealLine`, `MealMathResult`, `gramsOf`/`factor`/`nutrientsFor`/`totals` — §3.7 verbatim; `PIECE` with no `pieceGrams` resolves to 0 g plus a `MISSING_WEIGHT` warning instead of throwing) and `domain/engine/nutrition/NutritionValidator.kt` (the §3.6.1 step-7 checks over a `NutritionFacts`: sat-fat/sugar exceeding their parent, any per-100 field > 100 g except energy, Atwater `4p+4c+9f+2fiber` vs `max(30, 0.25·kcal)`, missing energy, < 3 recognised fields — reused as-is by the P4.3 editor and, later, P4.7's parser via the `hasEnergyKj` param). `MealMathTest` (`meal01`…`meal06` exactly as named; `meal07` is P4.5's repo-level test, out of scope here) + `NutritionValidatorTest` (5 named cases + 1 "no warnings" sanity check). |
| P4.2 Ingredient repository and search | sonnet | **yes** | 230 | 102.0 | `data/repository/RoomIngredientRepository.kt` over the existing `IngredientDao`; added two count queries to `MealDao` (`countLogItemsByIngredient`/`countTemplateItemsByIngredient`) since neither existed yet, used by `deleteOrArchive` to pick archive vs. hard delete. `search` trims and treats a blank query as `observeRecent`; `upsert` requires a non-blank name and non-negative kcal/protein/carbs/fat (all effectively-required per §2.2.5), else `Outcome.Err(Validation)`; `markUsed` bumps `useCount`/`lastUsedAtMillis` from the injected `Clock`. Added `observeArchived(limit)` to `IngredientRepository`/`IngredientDao` (not in the original file list, but required for P4.3's "show archived" switch — noted here as a deviation). Wired `ingredientRepo` into `AppGraph`. `RoomIngredientRepositoryTest` (9): markUsed increments, archive-vs-delete both branches, search-empty-returns-recents, search delegates to the DAO, validation rejects negative kcal / blank name, valid upsert stores. |
| P4.3 Ingredients list and editor | sonnet | **yes** | 230 | 102.0 | `ui/ingredients/{IngredientsScreen,IngredientsViewModel,IngredientsUiState,IngredientEditScreen,IngredientEditViewModel,IngredientEditUiState,IngredientDraft}.kt`. List: 250 ms debounced search (VM-side), favorite star toggle, "show archived" switch (client-side query filter over `observeArchived`), FAB → new. Editor: every `ingredient` field incl. a basis dropdown that relabels the unit suffix ("g / 100 g", "g / 100 ml", "g / piece"), a read-only derived sodium field (`saltG / 2.5`), a live `NutritionValidator` warnings banner (never blocks save — only `IngredientDraft.validate` errors do), save, and delete-with-confirmation (repo decides archive vs. delete). "Scan" navigates to the existing `ScanRoute` placeholder (P4.8); `IngredientEditRoute.barcode` prefills the barcode field (OFF lookup itself is P4.10). Wired into the NavHost (`IngredientsRoute`/`IngredientEditRoute` replace their placeholders) — already linked from the More list. Pure `validate(draft)`/`toIngredient()`/`fromIngredient()` in `IngredientDraft.kt`. `IngredientDraftTest` (6: negative kcal, blank name, `PER_PIECE` without `pieceGrams`, missing required macro, negative optional field, valid draft round-trip). |
| P4.4 Meal templates | sonnet | **yes** | 271 | 102.0 | `data/repository/{RoomMealRepository,MealLogWriter}.kt` (split per the task's ≤ 400-line guidance: the repository does the reads/templates, `MealLogWriter` owns the §3.7 math and the snapshot-on-log rule) + `ui/meals/{MealTemplatesScreen,MealTemplatesViewModel,MealTemplatesUiState,MealTemplateEditScreen,MealTemplateEditViewModel,MealTemplateEditUiState,MealTemplateDraft}.kt`. List: computed kcal/P/C/F per template via the pure `templateTotals` (= `MealMath.totals` over items × ingredients resolved through `ingredientRepo.getByIds`), favorite star, "Log now" dialog (`DatePickerField` defaulting to today + slot dropdown defaulting to `defaultSlot ?: LUNCH`) → `logTemplate`, which snapshots the items and bumps `useCount`/`lastUsedAtMillis`. Editor: name, default-slot dropdown (incl. "No default"), note, favorite, item rows (quantity + unit dropdown restricted to `validUnitsFor(basis, ingredient)`), searchable ingredient picker in a `ModalBottomSheet` (250 ms debounced `ingredientRepo.search`), live totals, per-row delete, save, delete-with-confirmation. `upsertTemplate` replaces the item set and renumbers `sortOrder` from the row order. `TemplateTotalsTest` (5, incl. equality against a hand-built `MealMath.totals`) + `MealTemplateDraftTest` (8). Reorder was not required and is not implemented. |
| P4.5 Meal log repository and nutrition diary | opus | **yes** | 271 | 102.0 | `RoomMealRepository`/`MealLogWriter` logs part + `ui/nutrition/{NutritionScreen,NutritionViewModel,NutritionUiState,MealSlotSection,DiaryHeader}.kt`. `logMeal` prices every item with `MealMath` and stores the **absolute** values denormalised in `meal_log_item` (`nameSnapshot` + kcal/protein/carbs/sugar/fat/satFat/fiber/salt), then `markUsed`s each ingredient; `updateItemQuantity` re-snapshots from the current ingredient, or scales the stored snapshot by the quantity ratio when the ingredient is gone (`ingredientId` SET_NULL); `deleteItem` drops an emptied `meal_log` with it; `copyDay` re-inserts the source day's logs with their snapshots **verbatim** (the interface KDoc said "re-snapshots" — corrected to match the snapshot rule); `observeDay` returns `MealLog` aggregates and `observeRange` one `MealLogSummary` per logged meal (the interface's shape; a day's numbers are the sum of its rows). Diary: ±1-day strip + date, `DiaryHeader` with energy bar/remaining/macro bars reusing P3.5's `targetProgressRows`, "Why this target?" expander over `explanation`, and the "Targets arrive after onboarding + P4.11" state when `nutritionRepo.observeTarget(day)` is null; slots in `MealSlot` order with per-slot totals, item rows (name, quantity+unit, kcal, protein) with tap-to-edit quantity dialog and overflow delete (overflow rather than swipe), "+ Add" per slot → `AddFoodRoute`, "Copy yesterday" in the top bar. `NutritionRoute` = today, `NutritionDayRoute(epochDay)` = that day, same screen (day lives in `SavedStateHandle`). **Deviation**: added `data/repository/RoomNutritionRepository.kt` + `AppGraph.nutritionRepo` (not in the file list) because the diary header needs `observeTarget`; it is storage-only — `ensureTarget` returns the stored snapshot or a validation error until the engine of P4.11/P4.12 exists. `RoomMealRepositoryTest` (13, incl. `meal07_editing_ingredient_does_not_change_past_logs`, `copyDay` verbatim after an ingredient edit, and both `updateItemQuantity` branches) over new `FakeMealDao`/`FakeMealIngredientDao`; `NutritionUiStateTest` (6). |
| P4.6 Add-food screen | sonnet | **yes** | 271 | 102.0 | `ui/nutrition/{AddFoodScreen,AddFoodViewModel,AddFoodUiState,QuantityEditor}.kt`. Tabs Recents · Favorites · Search · Templates · Scan (picking Scan navigates to `ScanRoute`, placeholder until P4.8); search debounced 250 ms in the VM. Selecting an ingredient opens `QuantityEditor` (bottom sheet): quantity field, unit dropdown from the pure `validUnitsFor(basis, ingredient)`, quick chips from `quickChipsFor` ("100 g"/"100 ml", the serving when `servingGrams` exists — labelled with `servingLabel` when present, "1 piece" when `pieceGrams` exists), live `MealMath` macro preview, "Add" → one-item `logMeal` then pop back; the top bar's + opens `IngredientEditRoute`. Templates tab: tap → `logTemplate(templateId, day, slot)`. Unit rules: the basis' own unit always, `PIECE` only with a known `pieceGrams`, grams for a `PER_PIECE` ingredient only with `pieceGrams`, `SERVING` only with an explicit `servingGrams` (MealMath's 100 g fallback is not offered in the UI). `AddFoodUiStateTest` (9). NavHost: `NutritionRoute`, `NutritionDayRoute`, `AddFoodRoute`, `MealTemplatesRoute`, `MealTemplateEditRoute` all replace their placeholders; the More list already carried "Meal Templates". Also added to `MealDao`: `getDayFull`, `upsertItem`, `getItemById`, `countItemsFor`. |
| P4.7 Nutrition label parser | opus | **yes** | 305 | 102.0 | `domain/engine/label/{OcrLine,Lexicon,NumberTokenizer,LabelValidator,NutritionLabelParser}.kt` — §3.6 pipeline: normalisation chain (NFKD → lowercase → `ß→ss` → diacritics stripped → `·•|`→space → unicode dashes → collapse; a spacing-preserving variant keeps the char offsets the column estimate needs), the step-2 number regex (comma decimals, `<`/`≤` upper bounds, kJ thousands guard, > 4 integer digits rejected — plus numerals glued to a word rejected, so `Bal1aststoffe` is not a 1), longest-keyword-first matching with Levenshtein ≤ 1 for keywords ≥ 6 chars (×0.8) and a `davon …`/`of which …` guard that also splits merged lines, column detection by header `centerX` with `(150 g)` serving grams, unit normalisation (mg/µg→g, kJ→kcal ÷ 4.184 half-up, sodium↔salt ×2.5, ×0.85 derived), step-7 validation delegated to `NutritionValidator` (P4.1), `Failed(NO_NUTRIENTS_FOUND)` on zero fields. 10 fixtures in `app/src/test/resources/fixtures/ocr/` (`text|left|top|right|bottom`, loaded off the classpath so R12 is moot). 34 tests: `ocr01…ocr19` (17 named + English two-column + the 40-line budget, ~2 ms warm on this machine vs the 20 ms §6.6 budget), `NumberTokenizerTest` (8), `LabelValidatorTest` (7). Deviations: unmarked energy numbers are read as kJ only at ≥ 1000; a two-column row whose nearest-centerX assignment collapses onto one header is repaired left-to-right and charged the ×0.9 ambiguity factor (the uniform-char-width estimate mis-places the first number of long rows such as `davon gesättigte Fettsäuren`); a serving-only label fills both `perServing` and the editable top-level fields and warns `COLUMN_AMBIGUOUS`; `perServing` carries `MeasureBasis.PER_PIECE`; one OCR spelling variant (`EiweiB` → protein, scored as fuzzy) is registered because `eiweib`→`eiweiss` is two edits. |
| **P4.8** Camera scan screen (CameraX + ML Kit) | opus | **yes** | 328 | 82.0 | Deps applied from the catalog only (CameraX camera-core/camera2/lifecycle/view 1.6.2 — `camerax-camera-core` alias added at the *same* 1.6.2, no new version; unbundled `play-services-mlkit-text-recognition:19.0.1` + `play-services-mlkit-barcode-scanning:18.3.1`, amendment A3). Manifest: `CAMERA`, `INTERNET`, `<uses-feature camera required="false">`. `data/ocr/`: `MlKitTextSource.recognize(InputImage)` (→ `Outcome<List<OcrLine>>`, `TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)`), `MlKitBarcodeSource.firstBarcode` (`BarcodeScannerOptions` limited to EAN-13/EAN-8/UPC-A/UPC-E), `OcrLineMapper` (`Text.Line.boundingBox` → `OcrLine`; null boxes and blank text dropped; reading order = 12 px row bands, then left→right — a "within N px" comparator is non-transitive and `sortedWith` rejects it), `FrameImages` (the `@ExperimentalGetImage` site: `ImageProxy.image` → `InputImage.fromMediaImage(image, rotationDegrees)` for analyser frames, upright `toBitmap()` + JPEG in `cacheDir` for the single shot, because `ImageCapture` hands back JPEG which `fromMediaImage` rejects), `OcrErrors` (hand-written `Task` → coroutine bridge, since `kotlinx-coroutines-play-services` is not in the catalog; `MlKitException` UNAVAILABLE/FAILED_PRECONDITION/NOT_FOUND/NETWORK_ISSUE → `AppError.Network(MODEL_NOT_READY)` → "model is still downloading"). `ui/camera/`: `ScanScreen` (LABEL/BARCODE segmented button, torch, shutter, `RequestPermission()` + `ACTION_APPLICATION_DETAILS_SETTINGS` deep link on denial), `CameraPreview` (`PreviewView` in `AndroidView`, `ProcessCameraProvider.getInstance().awaitValue()` — 1.6.2 has no `awaitInstance`; `DEFAULT_BACK_CAMERA`; only the mode's use case is bound; `ImageAnalysis` with `STRATEGY_KEEP_ONLY_LATEST`), `ScanViewModel`, `ScanUiState`, `DraftStore` (one-slot `AtomicReference` hand-off in `AppGraph`). `di/ScanIntegration.kt` is the seam (`ScanSources`/`OffLookup`/`ScanMessages`) so `ui/` never imports `com.myhealth.data` — it takes `ImageProxy` (an androidx type), keeping `InputImage`/`Text`/`Barcode` behind it. `OcrLineMapperTest` (3, on `RawLine`/`Bounds` stand-ins — no `Rect`, no ML Kit). **APK delta**: clean-built debug APK 86,017,255 B (82.0 MB) vs 74,165,900 B (70.7 MB) for the same tree with P4.8–P4.10 removed → **+11.3 MB** for CameraX + the unbundled ML Kit clients (which drag in play-services-base/basement) + OkHttp, all unminified in debug — more than §5's +3–6 MB estimate, but still inside the ~81 MB baseline §6.6/V4 predicts for the full dependency set. Note the 102.0 MB recorded in every row since P2.7 came from *incremental* builds; a clean `assembleDebug` of that same code is 70.7 MB, so only clean-build numbers are comparable. |
| **P4.9** OCR review screen | sonnet | **yes** | 328 | 82.0 | `ui/camera/{OcrReviewScreen,OcrReviewViewModel,OcrReviewUiState}.kt` + `ui/common/ConfidenceUnderline.kt` (`ConfidenceLevel`/`confidenceLevelOf`: ≥ 0.9 green, ≥ 0.7 amber, else red; `Canvas`-drawn bar, no chart lib). Nine editable rows (kcal, fat, saturates, carbs, sugars, fiber, protein, salt, kJ) with confidence colouring, `isError` + `FocusRequester` auto-focus on the first red field, warnings banner (re-run through `LabelValidator` after every edit), "Use per-serving column" switch when `perServing` exists (swaps values **and** the basis note; per-serving rows inherit the per-100 confidence, since the parser stores that column as plain `NutritionFacts`), name/brand fields, Retake / Cancel / Accept. Accept writes the confirmed facts back into `DraftStore` with `basis = PER_PIECE` when the per-serving column is in use, and navigates to `IngredientEditRoute()`; `IngredientEditViewModel` consumes `DraftStore.take()` on start and prefills through the new pure `IngredientDraft.withScan` (a value the scan lacks never overwrites the form; `PER_PIECE` fills `pieceGrams` from the serving weight). **Never auto-saves** — the VM has no repository at all. `@Preview` with a low-confidence draft. The captured JPEG is shown (decoded off-thread, `inSampleSize = 2`); recognised boxes are **not** drawn — the draft keeps line *indices*, not line geometry, so an overlay would mean carrying every `OcrLine` through the hand-off for decoration. `OcrReviewUiStateTest` (4). |
| **P4.10** Open Food Facts client and barcode flow | sonnet | **yes** | 328 | 82.0 | `data/off/{OffDto,OffMapper,OffClient}.kt`: OkHttp 5.1.0 + kotlinx-serialization (`ignoreUnknownKeys`), `GET /api/v2/product/{barcode}.json?fields=product_name,brands,quantity,serving_size,image_url,nutriments`, required `User-Agent: MyHealth/0.1 (personal app; <contact>)` from `SettingsRepository.offUserAgentContact` (fallback `no-contact`), 10 s connect/read/write timeouts, exactly one retry on `IOException`, `OffThrottle` token bucket of 15/minute on the injected `Clock`, `status != 1` → `Outcome.Err(AppError.Network(404))` → "Product not found in Open Food Facts". Mapper covers all ten nutriment keys, derives kcal from `energy-kj_100g` (÷ 4.184, half-up per A4, confidence 0.85), derives salt↔sodium ×2.5, reads values whether OFF sent numbers or strings, `PER_100ML` when `quantity`/`serving_size` mentions ml, serving grams from `serving_size` (`"30 g"`, `"1 bar (30,5 g)"`, `"250ml"`). Wired into `AppGraph` (`offClient`/`offLookup`) and used by both paths: the scan screen (first EAN → analyser stopped → lookup → `IngredientEditRoute(barcode)`, prefilled; a miss offers "Enter it manually" instead of a dead end) and the editor's new "Look up barcode" action for a hand-typed code. Fixtures `fixtures/off/{full_product,sparse_product,not_found}.json`; `OffMapperTest` (10) + `OffThrottleTest` (4), no network in any test. |
| **P4.11** Nutrition target engine | opus | **yes** | 391 | 82.9 | `domain/engine/nutrition/{NutritionDefaults,MetTable,DayTypeResolver,MacroSplitter,NutritionTargetEngine}.kt` — §3.1 verbatim. `NutritionDefaults` holds every constant plus the two arithmetic conventions: `roundTo` half-up (A4) and a **floor-wins** `clamp` (A9) used everywhere, because `coerceIn` throws when a safety floor crosses its ceiling (fat floor vs fat ceiling at a very low target). BMR: Mifflin (`+5`/`-161`/`-78`) or Katch–McArdle iff a body fat within 30 d lies in `[3, 60] %`, weight ladder measurement → `fallbackWeightKg` → `goalWeightKg` → 75.0 with `MISSING_WEIGHT`, clamp `[1000, 3500]`. `MetTable` = the plan's table + ACSM `0.952*v+1` clamped `[6, 20]` (9.8 unknown); `TrainingEnergyCalculator` sums completed `activeEnergyKcal` (else MET) plus the planned sessions that were not executed (> 50 % time overlap with a completed one, or linked to it). TDEE ladder with its three labels, clamp `[BMR, 3*BMR]`. Target: `goalDelta` (pace capped 1.0 loss / 0.5 gain, `|W-goal| <= 1` ⇒ maintain), match/race override, `target = roundTo(max(floor, min(raw, ceil)), 10)`. `MacroSplitter` in the normative order incl. the 35 % protein cap, the fat floor/ceiling, the carb-floor rebalance and the ≤ 10 % target raise on carb-priority days. Entry point is `compute(input): NutritionTarget`; `computeDetailed`/`energySummary` expose the unrounded intermediates for the tests and for the P4.12 hash. Tests: `NutritionTargetEngineTest` = all 20 `nut01…nut20` (BMR 1780 / TDEE 2225 / target 2230 / protein 130 g; Katch 1838.8; MET 10.52 → 761.6; nut13 loops the 24-case matrix), `MacroSplitterTest` (7), `DayTypeResolverTest` (9 — all 8 rows in order + precedence), `MetTableTest` (6). |
| **P4.12** Target snapshots, recompute worker, diary integration | opus | **yes** | 391 | 82.9 | `RoomNutritionRepository.ensureTarget(day)` gathers profile, weight/body-fat ≤ 30 d, the day's `daily_health_summary`, its completed activities, its still-`PLANNED` sessions and the occurrences of `day-1 .. day+1` → `DayTypeResolver`, with `isDayComplete = day < today`; `data/repository/TargetInputsHash.kt` hashes `profileUpdatedAt|weight|bodyFat|goalWeight|pace|neat|dayType|plannedIds|completedIds|tdeeSource|tdeeValue` with `MessageDigest` over a key-sorted canonical form, so the digest is reproducible and order-independent. The cheap `energySummary` pre-pass supplies the TDEE rung for the hash; the full compute and the write happen only when the hash differs. `sync/TargetRecomputeWorker.kt` walks `[today-1, today+7]` (a `Validation` error = no profile yet ⇒ skip, storage errors ⇒ `retry`). `SyncScheduler` gained `scheduleDailyTargetRecompute()` (24 h periodic, initial delay to the next 03:00 local, unique `targets_daily`) and `requestTargetRecompute()` (unique `targets_now`, `REPLACE` + 30 s delay = the debounce), called from onboarding/settings profile saves, the weight log, `setSessionStatus`, event save/delete/skip and at the end of every successful `HealthSyncWorker` run; `MyHealthApp` schedules the daily one. Diary: `NutritionViewModel` calls `ensureTarget` on every day change, `DiaryHeader` shows target vs intake, remaining and the "Why this target?" expander with the explanation **and** the engine warnings. Today: the placeholder is replaced by a real nutrition card (kcal bar + macro bars + remaining, tap → `NutritionRoute`). Tests: `TargetHashTest` (6, incl. a pinned digest literal), `RoomNutritionRepositoryTest` (7, fake repos: computes once, skips on an unchanged hash with `computedAtMillis` frozen, recomputes after a weight or day-type change, `Validation` before onboarding), `SyncSchedulerTest` (4). |
| **P4.13** Water logging | sonnet | **yes** | 391 | 82.9 | The `water_log` half of `RoomNutritionRepository` was already storage-complete from P4.5; it now also rejects non-positive and > 5 000 ml entries. `ui/nutrition/WaterCard.kt`: day total vs the snapshot's `waterMl` with a progress bar, +250 / +500 ml quick buttons, a custom-amount dialog (`WaterAmountDialog`), and the day's entries with `hh:mm` and a delete button. Wired into the diary through `NutritionViewModel` (`observeWaterTotalMl` + `observeWaterLogs`, `addWater` stamps the minute of day off the app clock). Label/fraction helpers are pure and unit-tested in `NutritionUiStateTest` (+4 tests); CRUD in `RoomNutritionRepositoryTest`. |

## P5 — Training load, recovery, running PRs

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P5.1** TRIMP calculator and HR bounds | opus | **yes** | 467 | 82.9 | `domain/engine/load/{TrimpDefaults,HrBounds,TrimpCalculator}.kt` per §3.2.1–§3.2.2. `HrBounds.compute` (Tanaka + observed-max override, resting-HR median ladder, both clamps, reserve guard) and `TrimpCalculator.compute` → `TrimpResult(trimp, method, warnings)` with the full ladder (`HR_SAMPLES` with the 60 s interval cap + null skipping, `HR_AVERAGE`, `RPE_ESTIMATE`, `DURATION_ONLY`), sex-specific `y`, the default-RPE table and the 0–600 AU clamp. Computed: `load01` = **108.095**, `load02` = **90.418** (both inside ± 0.5). 13 + 8 tests (`load01…load08`, `load18`). Choice: the plan's "other 5.0" RPE row is implemented as the `DURATION_ONLY` factor, not as a per-sport default, or `DURATION_ONLY` would be unreachable; the `[150,220]` clamp also applies to a manual `maxHrManual`, which makes the `reserve >= 30` guard structurally unreachable — it is kept and tested via `HrBounds.liftedMaxHr`. |
| **P5.2** Load series engine | opus | **yes** | 467 | 82.9 | `domain/engine/load/LoadSeriesEngine.kt` per §3.2.3: EWMA ATL/CTL (λ 0.25 / 2/29) seeded at 0, ACWR null while CTL ≤ 1, TSB from yesterday, rolling ATL7/CTL28 + rolling ACWR as `DayLoadDetail.rolling`, `reliable` (≥ 21 of 28 days with data), Foster monotony/strain with population sd and the sd = 0 → 3.0 rule, `acwrZone()` and the four flags + `INSUFFICIENT_HISTORY`. `compute()` returns `List<DailyLoad>`; `computeDetailed()` adds the secondaries the table has no columns for. Computed `load13`: sd **42.9047**, monotony **1.36515**, strain **559.712**. 14 tests (`load09…load17`). Choices: "day with data" = `trimp > 0 ‖ sessionCount > 0`; ACWR bands closed at the top (1.30 → OPTIMAL, 1.50 → CAUTION); `RAMP_HIGH`/`NO_REST_DAY_7D` only once their whole window is inside the series; input gaps are filled with zero-load days. |
| **P5.3** Recovery engine | opus | **yes** | 467 | 82.9 | `domain/engine/load/RecoveryEngine.kt` per §3.3: sleep 40 (duration/quality/consistency) + resting HR 25 + load 25 + HRV 10, every point formula verbatim, missing components dropped and weights renormalised, `confidence = availableWeight / (available + missing)`, score/band thresholds, `SLEEP_DEBT`, load flags copied through, null score + `INSUFFICIENT_HISTORY` when nothing is available. `RecoveryInput.extraComponents` is the P9.6 Body-Battery seam (an extra component joins the same renormalisation untouched — covered by a test). 16 tests (`rec01…rec10`). Choice: a missing **sub**-signal scores the neutral half of its sub-weight (the plan's own rule for absent sleep stages, so an absent bedtime history = 2.5 of 5) instead of dropping the whole 40-point component. |
| **P5.4** Running best-effort engine | opus | **yes** | 467 | 82.9 | `domain/engine/running/{CanonicalDistances,RunningBestEngine,RiegelPredictor,VdotCalculator}.kt` per §3.4: eligible sports (treadmill only behind `includeTreadmillInPrs`), Method A (tolerance `max(1 %, 50 m)`, scaling, `isEstimated` beyond 5 m), Method B two-pointer best split with interpolation on **both** edges (two O(n) passes), quality gate `medianIntervalSec ≤ 10`, pace sanity 140–900 s/km, one effort per canonical D ≤ total, `bestPerDistance` (PR = MIN(timeSec)), Riegel `T2 = T1·(D2/D1)^1.06` with the ≤ 180 d / D ≥ 3000 / largest-D source rule, Daniels VDOT. Computed: `pr10` = **2501.918 s**, `pr11` = **49.806**. 16 + 5 + 5 tests (`pr01…pr12`). Choices: when both methods yield a candidate the non-estimated one wins, then the faster; a stream that never reaches D yields no split (no extrapolation); GPS jitter is made monotone before the scan. |
| P5.5 Load repository and recompute worker | sonnet | **yes** | 476 | 82.9 | `data/repository/{RoomLoadRepository,RoomRunningBestRepository,LoadRecomputeService}.kt`, `sync/LoadRecomputeWorker.kt`, `SyncScheduler.{requestLoadRecompute,scheduleDailyLoadRecompute}`, `AppGraph` (`loadRepo`, `runningBestRepo`, `loadRecomputeService`), `MyHealthApp` (schedules the nightly run), `HealthSyncWorker` (requests a recompute after every successful sync). `LoadRecomputeService.recompute(fromDay)`: HR bounds from profile + 7-day resting-HR + 365-day observed max HR; TRIMP via `TrimpCalculator.computeForSession` for every activity in `[fromDay−28, today]`, persisted via `ActivityRepository.setTrimp` only when the value/method changed; day series from `ActivityDao.sumTrimpPerDay` + local session counts through `LoadSeriesEngine.computeDetailed`; `RecoveryEngine` per day from bulk-fetched sleep/resting-HR/HRV history; `running_best` refreshed per RUN activity via `RunningBestEngine.computeRows` + `RunningBestRepository.replaceForActivity`. `RoomLoadRepository.recomputeFrom` is wired to `LoadRecomputeService` through a constructor lambda (not a direct reference) to avoid a circular `AppGraph` `by lazy` initialization — noted in both files' KDoc. Added one DAO method, `LoadDao.getRange` (a one-shot counterpart of the existing `observeRange`, needed by `LoadRepository.getRange`). `LoadRecomputeTest` (3): idempotence (two runs → byte-identical `daily_load` rows), TRIMP filled from an average-HR session, 400 days of history recomputed in **53 ms** (measured, asserted < 2 s). |
| P5.6 Load & recovery screen | sonnet | **yes** | 476 | 82.9 | `ui/load/{LoadScreen,LoadViewModel,LoadUiState}.kt`; wired at `LoadRoute` (More already linked to it). 28/90/365-day range selector; ATL/CTL/ACWR (zone-coloured)/TSB tiles; monotony/strain tiles; daily-TRIMP list as `LinearProgressIndicator` bars (charts arrive in P8); active flags mapped to one-line explanations. Recovery card recomputes `RecoveryState` live from bulk-fetched sleep/resting-HR/HRV history (mirroring `LoadRecomputeService`'s inputs) because `daily_load` only caches the score/band/confidence/flags totals, not the component breakdown §2.2.6 asks the screen to show — so the per-component (name, points/max) list is rebuilt in `LoadViewModel`, not read from the cache. `LoadUiStateTest` (3): flag → text mapping (incl. an unknown-flag fallback), ACWR zone-bucket boundaries, recovery-band labels. |
| P5.7 Running PRs screen | sonnet | **yes** | 476 | 82.9 | `ui/running/{RunningPrsScreen,RunningPrsViewModel,RunningPrsUiState}.kt`; wired at `RunningPrsRoute` (More already linked to it). PR table per canonical distance (`mm:ss`/`h:mm:ss`, pace `/km`, date, "(est.)" badge, tap → `ActivityDetailRoute` when `activityId != null`), Riegel predictions for every canonical distance from `RiegelPredictor.pickSource`, a VDOT header stat, and an "Add manual PR" dialog (distance dropdown over `CanonicalDistances.ALL`, minutes/seconds fields, date picker) that writes a `MANUAL` `running_best` row via `RunningBestRepository.upsertAll`. `RunningPrsUiStateTest` (3): `mm:ss` vs `h:mm:ss` time formatting, pace formatting, canonical-distance labels. |
| P5.8 Today screen v2 (load + recovery) | sonnet | **yes** | 476 | 82.9 | `ui/today/{TodayScreen,TodayViewModel,TodayUiState}.kt`. Replaced the two P5 placeholders with real cards, both tapping through to `LoadRoute`: recovery (score/100, band chip, confidence, top active flag, "not enough data yet" before the first `daily_load` row) and load (ACWR with its zone colour, ATL, CTL, 7-day summed TRIMP). Reads `loadRepo.observeLatest()` plus a 7-day `observeRange` for the weekly TRIMP sum; reuses `ui/load`'s `acwrZoneOf`/`recoveryBandLabel`/`flagExplanation`/`AcwrZone.color()` so the two screens agree on wording and colour. |
| P5.9 RPE entry and load recompute trigger | sonnet | **yes** | 476 | 82.9 | `ui/activities/{ActivityDetailScreen,ActivityDetailViewModel}.kt` (the RoomActivityRepository `setRpe` path already pinned the field from P2.9 — no change needed there). A 1–10 `FilterChip` row (horizontally scrollable) replaces the old RPE placeholder card; selecting a value calls `ActivityRepository.setRpe` then `syncScheduler.requestLoadRecompute(activity.day)`. The same card shows the activity's current TRIMP method label ("HR samples"/"avg HR"/"RPE estimate"/"duration only", from the existing `LoadMethod.label()` helper). |

## P6 — Training plans and the suggestion engine

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P6.1** Goals repository and screens | sonnet | **yes** | 554 | 83.3 | `data/repository/RoomGoalRepository.kt` (+ `GoalDao.updatePriority`/`demoteOtherPrimaries`, no schema change), `domain/engine/goal/GoalProgress.kt`, `ui/goals/{GoalDraft,GoalsUiState,GoalsViewModel,GoalsScreen,GoalEditViewModel,GoalEditScreen}.kt`, `AppGraph.goalRepo`, `GoalsRoute`/`GoalEditRoute` wired (More already linked to Goals). `GoalProgress.compute(goal, bests, weights, today, activities)`: `RACE_TIME` percent `clamp(target/best)` + on-track via `RiegelPredictor` over the last **60** days within 2 %; `BODY_WEIGHT` linear from the first measurement at/after `createdAtMillis` (the entity has no `startValue` column — no migration needed) with the 80 %-of-required-rate rule; `CONSISTENCY` sessions/week over 4 weeks; `STRENGTH_LIFT`/`SOCCER_AVAILABILITY` are **manual** (`isManual`, percent 0) since no series in the data model measures them. Repository demotes every other `priority = 1` goal on `upsert(priority=1)` and on `setPrimary`. Tests: `GoalProgressTest` (9, incl. `goal01`…`goal05`, `goal07`, `goal08`), `RoomGoalRepositoryTest` (4, incl. `goal06`) on a new `FakeGoalDao`, `GoalDraftTest` (6), `GoalsUiStateTest` (4). |
| **P6.2** Periodization | opus | **yes** | 554 | 83.3 | `domain/engine/suggest/{SuggestionInput,Periodization}.kt` per §3.5.2 verbatim: phase table by days-to-race, `IN_SEASON` when a match falls inside 21 days else `BASE`, the `weeksSincePlanStart % 4 == 3` recovery-week override (never over `TAPER`/`RACE_WEEK`), `ctl*7*factor`, the 25 % ramp cap with the 150 AU floor, ACWR > 1.5 ×0.75, `FATIGUED` ×0.85, `STRAINED` ×0.60. §3.5.1 is extended with `planStartDay` (the override needs it; `null` disables it). Choices: a race whose `targetDay` has passed is ignored; `recentLoad.last()` = largest `day`; `lastWeekActual` sums `[today-7, today-1]`. `PeriodizationTest` (14): `sug09`–`sug12`, `sug17`, `per01`–`per06` + 3 helper cases. |
| **P6.3** Constraints and session catalog | opus | **yes** | 554 | 83.3 | `domain/engine/suggest/{SessionCatalog,DayPlan,Constraints}.kt`. Catalog is the §3.5.4 table with `estTrimp = TrimpDefaults.RPE_TO_TRIMP * rpe * minutes` **computed** (`cat01` checks all 12 rows against the plan's column); a `RACE` event has no catalog row and is seeded as a `MAX` outdoor run at RPE 9. `DayPlan`/`SuggestionGrid` are immutable (placing returns a new grid, so the greedy loop re-evaluates against "the grid as it currently stands"); fixed items are events, locked planned sessions and `BLOCKED` markers. `Constraints.violations(candidate, day, grid, ctx)` implements C1…C12. Readings: "within 48 h before" = the event day and the 2 days before it; C6's "overlapping" = sharing a day (candidates have no time); C5/C10 count over every rolling 7-day window containing the day; a `BLOCKED` day still counts as C3's rest day; `SportPreferences` reads the caps map plus an optional `longRunWeekday` out of `preferredSportsJson`. `ConstraintsTest` (15): `c01`…`c12`, `cat01` + 2. |
| **P6.4** Scorer and suggestion engine | opus | **yes** | 554 | 83.3 | `domain/engine/suggest/{Scorer,Rationale,SuggestionEngine,SuggestionInputsHash}.kt`. Five weighted terms, the recovery×intensity matrix, the phase-preference table (TAPER interval ×0.6 duration), the 9-step algorithm with tie-breaks `(score desc, day asc, sessionType.ordinal asc)`, the `< 0.35` / `< 10 % budget` stop rules, the 20-iteration cap, post-passes 7a (drop the lowest-scoring session in a window with no rest day), 7b (downgrade anything above `LOW` on the eve of a match/race to an easy run) and 7c (mobility on rest days), rationale entries with rule ids, and `inputsHash = sha256(canonical serialization)` (`MessageDigest`, sorted collections, `Locale.US` `%.3f` doubles). Output `SuggestionResult(batch (unsaved, id 0), sessions, phase, weeklyTarget, inputsHash)`. Choices: goals carry no sport, so the goal's sport comes from its `GoalType` and a sport-agnostic (or absent) goal makes `goalFit` the phase preference (1.0 preferred / 0.6 otherwise); `durationScale`'s "Σ remaining planned defaults" is read as `min(freeRestDays, ceil(affordableMinutes / defaultMin)) * defaultMin`. `SuggestionEngineTest` (18: `sug01`–`sug08`, `sug13`–`sug16`, `sug18`–`sug20` + 3), `ScorerTest` (8). |
| **P6.5** Plan repository and suggestion persistence | sonnet | **yes** | 566 | 83.9 | `data/repository/RoomSuggestionRepository.kt` + `AppGraph.{suggestionEngine,suggestionRepo}`; `SuggestionDao` gained 5 queries (`getLatestBatch`, `supersedeProposedBatches`, `getSessionsForBatch`, `getSessionsByIds`, `updateSessionStatuses`) — queries only, **no schema change**. `RoomPlanRepository` was already complete from P3.2 and is reused unchanged. `generate(horizonDays)` assembles §3.5.1 verbatim (today, profile, `ACTIVE` goals by priority, occurrences over horizon **+ 3** days, locked planned sessions inside the horizon, 42 days of `daily_load`, the recovery state read back off the newest `daily_load` row, 14 days of activities, the `ACTIVE` plan's `startDay`), runs `SuggestionEngine`, marks every previous `PROPOSED` batch `SUPERSEDED`, then writes batch + sessions. `accept(ids)` copies each suggestion into `planned_session` (`PLANNED`, `sourceSuggestionId`, rationale bullets flattened into the TEXT column, `planId` = the `ACTIVE` plan — created as "My plan" starting today when none exists) and fires `syncScheduler.requestTargetRecompute()` through an `onPlanChanged` lambda, so `data/` keeps no dependency on `sync/`. Missing profile → `AppError.Validation`, not a crash. The two `observeLatestBatch` queries gained an `id DESC` tie-break (two batches can share a millisecond). `RoomSuggestionRepositoryTest` (2) on a new `FakeSuggestionDao` + the existing plan/goal/profile/calendar/load/activity fakes. |
| **P6.6** Training plan screen | opus | **yes** | 566 | 83.9 | `ui/training/{TrainingUiState,TrainingViewModel,TrainingScreen,WeekBoard,PlannedSessionCard,WeeklyLoadBar}.kt`, `TrainingRoute` wired with `TrainingNavActions`. One week at a time from the `CalendarDay` aggregate (one observer for events + planned + activities + `daily_load`), paged ±1 week around the ISO Monday and carrying its own `startDay` so paging cannot tear. Header: phase badge (the latest batch's phase, else `Periodization.phase` recomputed live from active goals + 21 days of events + the plan's start day) and the load bar; the weekly target only comes from a batch whose horizon overlaps the displayed week. Day rows list fixed match/training/race events with an icon, `PlannedSessionCard` (sport icon, type, intensity chip, duration/distance/pace/AU, status, a lock toggle as its own tap target, and an overflow with mark-done/skip/reopen/edit/delete) and completed activities compactly. "Generate suggestions" runs `suggestionRepo.generate(settings.suggestionHorizonDays)` and navigates to the review on success; "+ Session" opens the editor on the selected day. Pure helper `weeklyLoadSums(planned, target, actual)` counts only still-`PLANNED` estimates (a completed session's load is already in `actual`) and scales all three bars to the largest — `WeeklyLoadBarTest` (2). **Lead addendum**: the Today placeholder card is replaced by `ui/today/TodayPlanCard.kt` — today's planned sessions (sport, type, intensity, duration, status, "Done") or, when none, the top-scoring suggestion for today from the open batch with its first rationale line and "Review" → `SuggestionReviewRoute`, else "Generate suggestions" → `TrainingRoute`; `TodayViewModel` keeps its single `StateFlow` (one extra `TodayPlan` sub-flow joined into the final `combine`). |
| **P6.7** Suggestion review screen | sonnet | **yes** | 566 | 83.9 | `ui/training/{SuggestionReviewUiState,SuggestionReviewViewModel,SuggestionReviewScreen}.kt`, `ui/common/RationaleList.kt`, `SuggestionReviewRoute` wired. Observes the latest batch and its sessions; header shows phase, weekly target, total suggested load, rest-day count and "n of m selected". One card per suggestion (day, sport icon, type, intensity chip, duration, estimated load) with all rationale bullets expanded — never truncated, since the reasoning is the point of proposing rather than imposing. Cards start **accepted**, so the toggle map records only rejections; "Accept selected" does `accept(selected)` + `reject(unselected)` in one go (no suggestion left in `PROPOSED` limbo) and pops back to Training, "Regenerate" reruns the suggester. Pure `suggestionRows(batch, sessions)` walks the batch's whole horizon and emits a `Rest` row for every day without a surviving suggestion (rejecting a day's only session turns it back into a rest day) — `SuggestionReviewUiStateTest` (1). `@Preview` with 5 suggestions across a 7-day horizon, i.e. including rest days. |
| **P6.8** Planned session editor and completion linking | sonnet | **yes** | 566 | 83.9 | `ui/training/{PlannedSessionDraft,PlannedSessionEditViewModel,PlannedSessionEditScreen}.kt` (`PlannedSessionEditRoute(id, epochDay)` wired): sport → session type (filtered through `SessionCatalog` by sport group) → intensity stay mutually consistent, plus day, optional start time, duration, distance (km), pace (min + sec → `targetPaceSecPerKm`), description, lock switch, save/delete, and a live `0.30 * rpe * minutes` load estimate. `validatePlannedSession` blocks a non-positive duration/distance when set and a pace outside 2:00–15:00 /km (seconds 0–59) — `PlannedSessionDraftTest` (3). `domain/engine/plan/PlannedAutoCompleter.kt` is the pure completion engine: `confidence = 0.5*sport + 0.3*time + 0.2*durationCloseness` (rounded half-up to 6 dp like `EventActivityLinker`, whose `START_WINDOW_MIN` it reuses), completing a session at ≥ **0.7** — a wrong-sport activity tops out at 0.5 and can never complete anything; a session with no planned clock time is not constrained by time (score 1.0) and one with no target duration scores a neutral 0.5. Only `PLANNED`, unlinked sessions are touched, each activity completes at most one session, ties break on the lower activity id. Run from `LoadRecomputeService` over `[today − 7, today]` after every recompute/ingest (new **optional** `planRepo` parameter, `null` in the P5.5 tests), writing `linkedActivityId` + `COMPLETED`. `PlannedAutoCompleteTest` (4): match, wrong sport, two candidates → closest duration, already-completed untouched. |

## P7 — FIT and CSV import

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P7.1** FIT SDK spike and decode wrapper | opus | **yes** | 620 | 84.9 | `com.garmin:fit:21.214.0` resolves on this machine (`:app:dependencies --configuration debugRuntimeClasspath` → `\--- com.garmin:fit:21.214.0`). **Jar findings**: 912 KB, 560 entries, all `com.garmin.fit*`, no transitive dependencies; it *does* use `java.time` — `DateTime` has `DateTime(java.time.Instant)`, `getInstant()`, `DateTime.from(Instant)` — which is native at minSdk 34, so no desugaring is needed and nothing above Java 8 is required. Dexes to 1.17 MB (382 KB deflated); clean-build APK 83.9 → **84.9 MB (+1.0)**. `data/fit/FitRecords.kt`: `FitFileData`/`FitFileId`/`FitSession`/`FitLap`/`FitRecord` are plain `@Serializable` data classes (no Garmin type leaks past the decoder, which is what makes the mapper fixture-testable) plus `FitEpoch` (offset 631 065 600 s) and `Semicircles` (`deg = semicircles * 180 / 2^31`). `data/fit/FitFileDecoder.kt`: `decode(InputStream): Outcome<FitFileData>` over `Decode` + `MesgBroadcaster` with one collector implementing `FileIdMesgListener`/`SessionMesgListener`/`LapMesgListener`/`RecordMesgListener`/`ActivityMesgListener` (the last one only for the local-timestamp offset); a `FitRuntimeException` becomes `AppError.Parse`, so one bad file inside a 100 MB export cannot abort the import. `proguard-rules.pro`: `-keep class com.garmin.fit.** { *; }` + `-dontwarn`. |
| **P7.2** FIT → domain mapper | opus | **yes** | 620 | 84.9 | `data/fit/{FitSportMap,FitToDomainMapper}.kt`. Sport map per the plan (`RUNNING` + `TREADMILL`/`INDOOR_RUNNING`/`VIRTUAL_ACTIVITY`→`RUN_TREADMILL`, `TRAIL`/`ULTRA`→`RUN_TRAIL`, `TRACK`→`RUN_TRACK`; `SOCCER`→`SOCCER_TRAINING`; `TRAINING`→`STRENGTH` with `CARDIO_TRAINING`/`HIIT`→`HIIT` and `YOGA`/`FLEXIBILITY_TRAINING`→`MOBILITY`; `CYCLING`, `FITNESS_EQUIPMENT`, `WALKING`, `HIKING`, `SWIMMING`, `ROWING`; everything else → `OTHER`). `toIngestItems(data, zone, now)` returns one `ActivityIngestItem` per `session` message — the same seam `HcMapper` feeds — with `externalId = sha256("serial|timeCreated|startTime").take(32)` (`|`-separated so `12`+`345` cannot collide with `123`+`45`), streams on a one-sample-per-second axis (HR as `List<Int?>` so dropouts stay nulls; distance/speed/cadence/altitude/lat-lng carried forward, since a `DoubleArray` cannot hold a null; channel omitted entirely when no record carried it) and laps. Judgement calls, both documented in the source: running/walking cadence is doubled to steps-per-minute so FIT and Health Connect agree on the same run, and `total_calories` maps to `activeEnergyKcal`. Tests: `FitToDomainMapperTest` (11, incl. `fit01`…`fit08`) over 9 JSON fixtures in `app/src/test/resources/fixtures/fit/` (one per sport + one with HR/position/time gaps). **Binary fixture**: `FitEncoderFixtureTest` writes `run_5k.fit` with the SDK's `FileEncoder` when missing (file id, 1 session, 2 laps, 1 501 records at 1 Hz with HR/distance/speed/position) and publishes a copy to `docs/testassets/run_5k.fit`; `FitFileDecoderTest` (4) decodes it with the real SDK on the JVM and asserts session distance ≈ 5 000 m, ~1 500 records, 2 laps, the local-timestamp offset, and that the decoded file maps to one 5 km `RUN_OUTDOOR` activity. |
| **P7.3** Garmin activities CSV parser | sonnet | **yes** | 620 | 84.9 | `data/fit/GarminCsvParser.kt` (parser + `toIngestItem`) and `data/fit/GarminCsvColumns.kt` (header vocabulary, cell grammar, activity-type map — split out only to stay under the ~400-line rule R10). Header-driven: the header row is normalised (lowercase, unit suffix in parentheses dropped, umlauts and Garmin's `Ø` folded, non-alphanumerics collapsed) into `Map<canonicalName, index>`; unknown columns are ignored and absent known columns yield nulls. Handles RFC-4180 quoting (`""` → `"`), `--` as null, per-file German/English decimal detection (`10,52` / `1.234,5` vs `5.00`), `h:mm:ss` / `mm:ss` / plain-seconds durations, pace columns (`5:00` min/km → m/s) as well as km/h speed columns, km distances, and `dd.MM.yyyy HH:mm` / `yyyy-MM-dd HH:mm:ss` timestamps in the app's zone. `externalId = sha256(rawRowText)` (full 64 hex). German header aliases are accepted too (`Aktivitätstyp`, `Datum`, `Distanz`, `Ø HF`, `Anstieg gesamt`), since a localised export would otherwise lose every column. A row with an unparseable date fails alone and is reported; a file with no date column yields one error and no rows. `GarminCsvParserTest` (11, incl. `csv01`…`csv08`) over 3 fixtures (`garmin_en`, `garmin_de`, `garmin_sparse`). |
| **P7.4** Archive walker (zip-in-zip) | sonnet | **yes** | 620 | 84.9 | `data/fit/GarminArchiveWalker.kt`: `suspend walk(input, onEntry: suspend (ArchiveEntry) -> Unit): ArchiveWalkResult`. No assumption about `DI_CONNECT/`'s upload folder name — it just recurses, opening any `.zip` entry as a nested `ZipInputStream` **on the entry stream itself** (a non-closing filter), so a nested archive is never buffered and only the current `.fit`/`.csv` is held in memory. Guards: depth ≤ 3 (deeper archives recorded in `depthSkippedPaths`, not opened), total uncompressed ≤ 500 MB (configurable; the walk stops and sets `truncatedBySize`), and zip-slip rejection (`\` folded to `/`, then no `..` segment, no absolute path, no drive prefix) into `rejectedPaths`. The callback is suspending so the import can ingest and release each entry inline. `GarminArchiveWalkerTest` (4): `zip01`…`zip04` over zips built in memory. |
| **P7.5** Import pipeline | opus | **yes** | 620 | 84.9 | `data/repository/{ImportService,RoomImportRepository}.kt`, `sync/ImportWorker.kt`, `domain/model/Import.kt`, `AppGraph.{importRepo,importService,pendingImportUri}`. `import(uri, kind, force): Flow<ImportProgress>`: SHA-256 the document (streamed), short-circuit with `AlreadyImported` when `import_record` holds that hash unless `force`, then decode and ingest in chunks of 50 through `ActivityRepository.ingest` — the same seam Health Connect uses, so §2.4 de-dup and field precedence come for free — and finally write one `import_record` (counts + per-item errors as JSON) and call `syncScheduler.requestLoadRecompute(minAffectedDay)`. Memory guard: candidates are buffered one at a time and flushed at 50, so never more than 50 decoded activities plus streams are in flight. A corrupt `.fit`, a rejected zip entry or a bad CSV row is a recorded item error, not a failed import. `ImportContentSource` is the `content://` seam (`AndroidImportContentSource` via `ContentResolver` + `OpenableColumns`), which keeps the service unit-testable. `ImportProgress`/`ImportCounts`/`ImportItemError` live in `domain/model/Import.kt` rather than next to the `ActivityImporter` interface, because `RepositoryContractTest` (rightly) parses multi-line data-class constructors in `domain/repository` as interface members. `ImportWorker` republishes counts through `setProgress`; `SyncScheduler` gained `startImport`/`observeImportState`/`clearImportState` on a unique `file_import` slot (`REPLACE`, no battery constraint — the user is waiting). `ImportServiceTest` (7) on fakes for the file source and `import_record` but the **real** `ActivityIngestor` over `FakeActivityDao`: duplicate-file short-circuit (+ `force` re-ingesting idempotently), partial-failure accounting inside a zip, chunking (`50, 50, 20` for 120 CSV rows), a failing chunk, an unreadable document, and a FIT file merging into an existing Health Connect activity (one canonical row, `HEALTH_CONNECT,FIT_IMPORT`, FIT streams + laps + distance win, HC calories win). |
| **P7.6** Import screen | sonnet | **yes** | 620 | 84.9 | `ui/imports/{ImportUiState,ImportViewModel,ImportScreen}.kt`, `ImportRoute` replaces its placeholder, `AndroidManifest.xml` + `MainActivity` handle the share sheet. `OpenDocument` picker limited to `application/zip`, `text/csv`, `text/comma-separated-values`, `application/octet-stream`, `*/*`; the kind comes from the file name (`ImportKinds.forFileName`) with a MIME fallback, and an unsupported pick shows an `ErrorBanner` instead of starting anything. Sections: pick card, live status (running progress with `parsed · saved · duplicates · errors` and the current entry, finished summary, "Already imported" with **Import anyway** = re-run with `force`, or a failure banner) and the `import_record` history list. The work runs in `ImportWorker`, so progress survives rotation and leaving the screen. `MainActivity` is now `singleTask` with `ACTION_SEND` (those MIME types) and `ACTION_VIEW` (`application/vnd.ant.fit`, `application/octet-stream`) filters; `onCreate`/`onNewIntent` park the URI in `AppGraph.pendingImportUri`, `MyHealthNavHost` navigates to `ImportRoute` when it sees one, and the screen starts the import and clears it. `ImportUiStateTest` (3). **Runtime check on `emulator-5554`**: installed, pushed `docs/testassets/run_5k.fit` to `/sdcard/Download/`, picked it through the real document picker → "Import finished · 1 parsed · 1 saved · 0 duplicates · 0 errors" and the history row `run_5k.fit · FIT file`. Note for the lead: that run (2026-05-10) is now in the emulator's database. |

## P8 — Polish

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P8.1** String extraction and lint clean-up | opus | **yes** | 691 | 87.2 | `res/values/strings.xml` **43 → 778** `<string>` entries, `screen_element` keys prefixed per feature; every literal inside a `@Composable` in `ui/**` now goes through `stringResource(R.string.…)`. ViewModel/UiState text travels as the new `ui/common/UiText.kt` `UiMessage(@StringRes resId, args)` and is resolved in the composable (`resolve()` / `resolve(context)`), so `domain/` stays Android-free and no ViewModel touches a `Context`; enums that carried a label (`BodyRange`, `LoadRange`, `OcrField`, `MoreEntry`) now carry `@StringRes` ids. `app/lint.xml`: `HardcodedText` flipped to **error**, `lintDebug` clean. **68 literals deliberately left** — the pure label/validation helpers whose exact output §3/§4 named unit tests assert verbatim (`goalHeadline`, `permissionLabel`, `flagExplanation`, `distanceLabel`, `validatePlannedSession`, `validateTemplate`, `basisNote`, `targetProgressRows`, `formatSleepDuration`) plus number-glued unit suffixes; listed in `docs/VERIFICATION.md` "Polish batch". `TodayScreen.kt`/`ActivityDetailScreen.kt` split (previews moved out) to stay inside R10. |
| **P8.2** Chart components (Compose Canvas) | opus | **yes** | 684 | 86.1 | No chart library (amendment A2). `ui/common/charts/ChartScale.kt`: pure Kotlin `niceTicks(min, max, maxTicks = 5)` picking the smallest `1/2/5 x 10^n` step that covers the data in at most `maxTicks` ticks (bounds always included; reversed, single-value and non-finite input handled), `NiceRange`, `fraction`/`toY`/`toX`, `decimalsFor`. `ChartModels.kt`: `ChartPoint(x, y?)` (a `null`/NaN `y` is a gap), `ChartSeries(name, points, dashed)`, `ChartBand(label, from, to, color)`, the M3-only palette (`chartSeriesColor` → primary/tertiary/secondary/error, grid = `outlineVariant`) and the legend chips. `ChartDraw.kt`: `layoutPlot` reserves a left gutter sized to the widest y label and a bottom strip for the x labels, `drawYAxis` (grid line + right-aligned label per tick via `rememberTextMeasurer`/`drawText`), `drawXAxis` (first/middle/last, anchored left/centre/right so nothing clips). `LineChartCard(title, series, xLabels, yFormatter, goalLine, bands, …)`: shaded y-bands clipped to the plot, dashed goal line, one `Path` per series with round cap/join that breaks on gaps, dots while a series has < 40 points, legend from 2 series (or `alwaysShowLegend`). `BarChartCard(title, values, xLabels, yFormatter, highlightIndex)`: zero-based axis, highlight in `tertiary`, value labels on ≤ 14 bars. Both are `SectionCard`s, default 180 dp tall, and show an empty-state line instead of an empty canvas. `ChartMath.kt`: `movingAverage`, `dailySeries`, `dropGaps`, `invertY`, `downsample`. 8 `@Preview`s. Tests: `ChartScaleTest` (11) + `MovingAverageTest` (8). |
| **P8.3** Charts on Body, Load and Activity screens | sonnet | **yes** | 684 | 86.1 | **Body**: `BodyRange` 30/90/365 d segmented selector; `BodyViewModel` gained `healthRepo` and now emits the windowed measurements, the window's daily summaries and the last 14 nights. `BodyCharts.kt` (pure): `weightPoints`/`bodyFatPoints` (one point per day, several readings averaged), `movingAveragePoints` (the 7-day average is computed on the **daily** grid, then both lines `dropGaps()` so an every-third-day weigh-in reads as one trend), `restingHrPoints`, `sleepHoursBars`, `dayAxisLabels`/`nightAxisLabels`. Four cards: weight + dashed 7-day average + goal-weight line, body fat, resting HR, sleep bars (14 nights, last night highlighted). **Load**: `LoadCharts.kt` maps `daily_load` to `atlPoints`/`ctlPoints`/`acwrPoints`/`recoveryPoints`/`trimpBars` over `[first, last]` cached day (missing days are gaps, but a day without training is a real TRIMP zero); the screen now has an ATL/CTL line chart, an **ACWR** chart with the §3.2.3 zones shaded (0.8–1.3 primary tint, 1.3–1.5 amber, > 1.5 error), the daily-TRIMP bars (replacing the old progress-bar list) and a recovery-score line. `AcwrZone.color()` now uses the theme tokens instead of hard-coded hex. **Activity detail**: `ActivityCharts.kt` (pure) — `hrPoints`, `paceSeriesFromStream(offsetsSec, distanceMeters)` differentiating over ≥ 10 s and turning a standstill into a gap, `paceSeriesFromSpeed` (HC sessions carry `speedMps` but no cumulative distance), `speedPoints`, `altitudePoints`, `minuteAxisLabels`, `formatPaceAxis`; `ActivityChartCards.kt` renders HR over elapsed minutes, pace for runs on an **inverted** axis (`invertY()` + a negating formatter, so faster is higher) or speed in km/h otherwise, and altitude when the stream has it. The existing header/laps/HR-zone tables are untouched. **Running PRs**: `RunningPrCharts.prProgressionSeries` groups every kept effort by canonical distance, keeps the distances with ≥ 2 efforts and plots finishing time against date (`RunningPrsViewModel` combines the eight `observeByDistance` flows); the legend is forced on so a single-distance chart still names it. Tests: `PaceSeriesTest` (5), `BodyChartsTest` (7), `LoadChartsTest` (6), `RunningPrChartsTest` (4). |
| **P8.4** JSON backup export and import | opus | **yes** | 643 | 86.1 | `domain/repository/BackupRepository.kt` (`BackupMode`, `BackupSummary`, `BackupContentSource` — Android-free). `data/backup/{BackupModel,BackupSerializer,BackupService,BackupMerge,AndroidBackupContentSource}.kt`: `@Serializable BackupFile(schemaVersion, exportedAtMillis, appVersion, …)` with one list per table over the Room entities themselves (all 26 entities gained `@Serializable`; `ingredient_fts` is excluded — it is an index). `kotlinx.serialization` with `ignoreUnknownKeys`/`encodeDefaults`, streaming both ways; a backup whose `schemaVersion` exceeds the app's is refused. New `data/db/dao/BackupDao.kt` (all/insert/delete per table). REPLACE wipes child-first and restores the original ids; MERGE inserts only rows whose **natural key** is missing — `(source, externalId)` for source records, `night` for sleep, `barcode` or `name+brand` for ingredients, `day`/`key` for the caches — remapping every foreign key through a per-table id map, so re-importing the same file is a no-op. The whole import runs in one injected transaction (`db.withTransaction` in the app, a plain lambda in tests). Settings/DataStore and credentials are never exported. `ui/settings/{BackupScreen,BackupViewModel}.kt` replaces the placeholder `BackupRoute`: `CreateDocument("application/json")` export, `OpenDocument` import with a Merge/Replace chip, per-table counts. Tests: `BackupSerializerTest` (round-trip equality, future schema rejected, unknown keys/tables ignored, non-JSON → `AppError.Parse`) and `BackupServiceTest` over `FakeBackupDao` (export counts, REPLACE verbatim restore, MERGE remaps FKs, double-merge writes 0 rows, MERGE never overwrites). |
| **P8.5** Ingredient full-text search | sonnet | **yes** | 643 | 86.1 | `data/db/entity/IngredientFtsEntity.kt`: `@Fts4(contentEntity = IngredientEntity::class)` over `name`, `brand` (external content — no second copy of the rows). DB `version = 2` with `Migrations.MIGRATION_1_2`: Room's own `CREATE VIRTUAL TABLE` + the four `room_fts_content_sync_*` triggers, copied verbatim from `2.json`, then `INSERT INTO ingredient_fts(ingredient_fts) VALUES('rebuild')` so rows that already existed are indexed. `IngredientDao.searchFts(match, limit)` joins `ingredient_fts.rowid = ingredient.id`; `RoomIngredientRepository.search` routes to it from 3 characters up (`ftsMatchQuery` turns the query into one `term*` per token and strips every FTS4 operator), `LIKE` below that or when nothing tokenises. `app/schemas/…/2.json` committed next to `1.json`; PLAN §6.4 ledger row updated; `MyHealthDatabaseTest` gained `migration_1_to_2_builds_the_ingredient_fts_index_over_existing_rows` using `MigrationTestHelper`. Verified in place on the emulator: `user_version` 1 → 2 with all 42 activities intact, FTS tables + 4 triggers present, `ingredient_fts MATCH 'hafe*'` finds the existing row. |
| **P8.6a** Green theme (owner request) | sonnet | **yes** | 554 | 83.3 | `ui/theme/{Color,Theme}.kt`: full M3 light/dark schemes seeded from `#2E7D32`/`#81C784`, teal-green secondary, lime/olive tertiary, green-tinted neutrals + `surfaceTint = primary`, red error; the semantic amber/green/red the ACWR and confidence UI uses are named in `Color.kt` and unchanged. `AppSettings.useDynamicColor` (default **false**) added to `SettingsKeys`, `DataStoreSettingsRepository`, `SettingsRepository.setUseDynamicColor` and a "Use wallpaper colours" switch in Settings; `MyHealthTheme(darkTheme, dynamicColor)` now defaults `dynamicColor = false` and `MainActivity` feeds both from `graph.settings.settings` via `collectAsStateWithLifecycle` (`themeMode` is finally applied too, through `isDarkTheme`). |
| **P8.6** Visual polish | opus | **yes** | 691 | 87.2 | New `ui/common/Dimens.kt` (`SCREEN_PADDING` 16 dp, `CARD_CORNER_RADIUS` 12 dp) applied to every screen root and card surface, and `ui/common/LoadingBox.kt` for async first loads. `PullToRefreshBox` (M3 1.4) on **Today** and **Activities**, both running `syncNow()` with the spinner bound to `SyncScheduler.observeState()`; `ActivitiesViewModel` gained `isSyncing`/`syncError` and the screen a retryable `ErrorBanner`. Body & Health's history list gained the standard icon + title + message + action `EmptyState`, and its "Last N days" header now follows the 30/90/365 selector instead of a hard-coded 90. Adaptive launcher icon redrawn: diagonal green gradient background, white heart + pulse trace + leaf foreground, and a dedicated `ic_launcher_monochrome.xml` (one `evenOdd` path) for themed icons — all vector, no external assets. |
| **P8.7** Release build configuration | opus | **yes** | 691 | 87.2 | `release { isMinifyEnabled = true; isShrinkResources = true; proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"); signingConfig = signingConfigs.getByName("debug") }` — debug signing documented in the build file as personal-use sideload only (it also keeps `adb install -r` working over the debug install without clearing data). `app/proguard-rules.pro` rewritten with keeps for kotlinx-serialization (`@Serializable` classes, `$$serializer`, `Companion.serializer()`), Room (`*_Impl`, entities, converters, DAOs), the type-safe nav routes, DataStore's protobuf, `ListenableWorker` subclasses + `com.myhealth.sync.**`, Health Connect, ML Kit / Play Services, CameraX, `com.garmin.fit.**` and OkHttp. `tools/verify.sh` now runs `:app:assembleRelease` after the debug steps and prints both sizes. **Release APK 13.9 MB vs debug 87.2 MB (6.3×)**; installed on `emulator-5554` over the existing data and driven through Today / Calendar / Nutrition / Training / Activities + detail / Ingredients / Import / Backup / Integrations + "Sync now" with **zero** `FATAL`/`ClassNotFoundException`/serialization errors in logcat (`docs/VERIFICATION.md` "Polish batch"). Debug APK reinstalled afterwards. |
| P8.8 Home-screen widget (optional) | sonnet | **skipped (optional)** | — | — | PLAN §5 marks P8.8 as the one droppable task and §6 risk R21 keeps it that way. Glance (`androidx.glance:glance-appwidget`) is not in `gradle/libs.versions.toml`, and adding it would be a new third-party dependency outside a task that lists it (R4/R5). Skipped deliberately, not blocked. |

## P9 — Optional direct Garmin client (LAST)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P9.1 Isolation boundary and feature flag | opus | skipped (optional, see VERIFICATION.md) | — | — | |
| P9.2 Garmin SSO client (OAuth1 → OAuth2) | opus | skipped (optional, see VERIFICATION.md) | — | — | |
| P9.3 MFA flow and encrypted credential storage | opus | skipped (optional, see VERIFICATION.md) | — | — | |
| P9.4 Wellness endpoints and merge into daily summaries | opus | skipped (optional, see VERIFICATION.md) | — | — | |
| P9.5 Garmin direct screen | sonnet | skipped (optional, see VERIFICATION.md) | — | — | |
| P9.6 Recovery engine uses Body Battery / HRV when available | sonnet | skipped (optional, see VERIFICATION.md) | — | — | |

## P10 — Instrumented UI tests

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| P10.2 Fix the instrumented (`connectedDebugAndroidTest`) UI test suite | sonnet | **yes** | 10/10 (691 unit) | 13.9 | All 4 failing tests were test-code bugs, not product bugs; no production code changed except reading two pre-existing `Modifier.testTag`s (`settings_dynamic_color_switch`) that were already in place. `IngredientTest`/`MealTemplateEditScreen`'s "Save template": the energy/macro fields and the Save button sit in `item {}`s below the fold of a `LazyColumn`, so they are never composed at all until the list itself is told to scroll (`onNode(hasScrollAction()).performScrollToNode(hasText(...))`) — a plain `performScrollTo()` on the target only works once a node already exists in the tree. `OnboardingFlowTest`: `onNode(hasSetTextAction())` matched both the read-only birth-date `OutlinedTextField` and the `DatePickerDialog`'s real text-input field; scoped with `hasAnyAncestor(isDialog())`. `SettingsPersistenceTest`: same below-the-fold issue for the "Use wallpaper colours" switch (`ProfileSection`, item 1, is by itself taller than the screen); a `printToLog` dump also showed that re-tapping the bottom-nav "More" tab from Settings (reached via a plain `navigate()` push, not the bottom bar's own save/restore path) is a dead click after `recreate()` restores the back stack to Settings — the fixed test scrolls the already-showing screen instead of re-navigating into it. `MealTemplateAndDiaryTest`: the new template has no default slot, so "Log now" logs it under the fallback slot (Lunch); the diary's `LazyColumn` shows "Breakfast" first, so the item row needed the same `performScrollToNode` fix (the header's kcal total was correct all along). Two full `connectedDebugAndroidTest` runs in a row were 10/10 green — no flakiness, no `@Ignore` needed. |

## P11 — Menstrual cycle tracker + cycle-aware training

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P11.1** Cycle model, engine, storage | opus | **yes** | 718 | 86.2 | `domain/model/Cycle.kt` (`CycleEntry`, `CyclePhase`, `CycleConfidence`, `CycleStatus`, `PredictedCycle`, `CycleForecast`, `CycleDefaults`) and the pure `domain/engine/cycle/CycleEngine.kt`: cycle length = mean of the last ≤ 6 intervals between logged period starts, period length = mean of the last ≤ 6 finished `end − start + 1`, `statusFor` rolling forward past the last logged start, `forecast(entries, today, cycles = 6)`, confidence HIGH (≥ 3 intervals, population SD ≤ 3) / MEDIUM (≥ 1) / LOW (defaults). Storage: `CycleEntryEntity` (`cycle_entry`, unique index `uq_cycle_entry_start` on `periodStartDay`), `CycleDao`, DB **version 3** + `Migrations.MIGRATION_2_3` (SQL copied verbatim from the exported `3.json`; `1.json`/`2.json` untouched), `CycleMappers`, `domain/repository/CycleRepository.kt` + `data/repository/RoomCycleRepository.kt` (`observeAll`/`observeStatus`/`observeForecast`/`statusFor`/`statusesFor`/`upsert`/`delete`, `isTrackingEnabled = settings.cycleTrackingEnabled \|\| profile.sex == FEMALE`), `AppSettings.cycleTrackingEnabled` + `SettingsKeys.CYCLE_TRACKING_ENABLED` + `SettingsRepository.setCycleTrackingEnabled` (Onboarding sets it when `sex == FEMALE` is saved; Settings sets it when the profile *changes* to FEMALE, and never clears a user's own "off"), `cycle_entry` added to the JSON backup (REPLACE verbatim, MERGE keyed on `periodStartDay`, `BackupFile.CURRENT_SCHEMA_VERSION = 3`), wired into `AppGraph.cycleRepo`. Tests: `CycleEngineTest` `cyc01`…`cyc11` (11) + `RoomCycleRepositoryTest` (5, over `FakeCycleDao` which enforces the unique index) + a `cycleEntry_roundTrips` mapper case; `MyHealthDatabaseTest` gained `migration_2_to_3_adds_cycle_entry_with_a_unique_period_start` (compiles; needs a device to run). **Two spec ambiguities**, both documented in `CycleEngine`'s KDoc and pinned by tests: (1) P11.1 defines ovulation twice — absolute `nextPeriodStart − 14` (`cyc07`) and day-of-cycle index `cycleLength − 14` that the 3-day OVULATION window is centred on (`cyc05`: days 13–15 of a 28-day cycle); they differ by one day because a cycle day is 1-based, so both are implemented as written and the absolute date always lands inside the window; (2) "clamped 21–45" is applied per interval *and* to the mean, so one missed log cannot drag the forecast (identical to the mean-only reading for in-range data). |
| **P11.2** Cycle-aware suggestions and nutrition note | opus | **yes** | 718 | 86.2 | `SuggestionInput.cycleStatusByDay: Map<Long, CycleStatus> = emptyMap()` (also hashed by `SuggestionInputsHash`, so a newly logged period regenerates the batch). New `domain/engine/suggest/CycleRules.kt` holds the four rules; they reach the engine through the existing seams: `ConstraintId.CYCLE_MENSTRUAL` (days 1–2 cap at `MODERATE`), `CYCLE_OVULATION` (`MAX` → `HIGH` in the window — the catalog itself tops out at `HIGH`, so the cap is asserted directly on `CycleRules`), `CYCLE_LATE_LUTEAL` (≤ 1 hard session across the window, fixed calendar sessions counting toward it like C5); `Scorer` gained an unweighted `cycleBonus` term (`+0.10` for hard runs and `STRENGTH_*` in the follicular phase, for `RECOVERY`/`LOW`/`MOBILITY` in the late luteal) with the total capped at 1.0, and `recoveryFit × 0.85` for hard work on menstrual days 3…period end; `Periodization.compute` multiplies the weekly target by `CycleRules.weeklyTargetFactor` **after** the §3.5.2 formula, leaving that formula and its tests untouched. Rationale ids `CYCLE_MENSTRUAL_EARLY` / `CYCLE_FOLLICULAR` / `CYCLE_OVULATION` (asks for a thorough warm-up, ligament laxity) / `CYCLE_LATE_LUTEAL` (recovery, sleep, hydration); a `LOW`-confidence forecast keeps the rule and appends "Based on a default 28-day cycle — log your period to improve this". `RoomSuggestionRepository` fills the map for `[today, today + horizon)` only while `cycleRepo.isTrackingEnabled()`. Nutrition: `NutritionTargetInput.cycleStatus`, one extra explanation line in the luteal phase ("Luteal phase: appetite and core temperature are typically higher; the target is unchanged, listen to hunger") and `cyclePhase` added to `TargetInputsHash` so the stored snapshot follows it; **no** kcal or macro changes. Tests: `sug21`…`sug25` in the new `SuggestionEngineCycleTest` (split out of `SuggestionEngineTest` to stay inside R10; `sug01`…`sug20` pass unchanged), `CycleRulesTest` (4) and `nut21_luteal_note_added_without_changing_target`. **Ambiguity**: "weekly target ×0.90 for the days in the window" scales a weekly number by a per-day property — implemented pro-rata (`1 − 0.10 × lateLutealDays / horizonDays`), so a horizon entirely inside the window is exactly ×0.90 (`sug24` uses a 5-day horizon to assert that exact equality). |
| P11.3 Screens | sonnet | **yes** | 734 | 88.6 (release 14.0) | `ui/cycle/{CycleUiState,CycleViewModel,CycleScreen}.kt` + `CycleRoute` in `Routes.kt`/`MyHealthNavHost.kt`; `MoreScreen.kt` gains a `MoreViewModel` so "Cycle" only appears while `cycleRepo.isTrackingEnabled()`. Status card (phase badge + "late luteal" chip, "Day N of ~C", next-period countdown with date, ovulation date, fertile window, confidence line), "Log period start" (date picker, defaults today; a start day already on file is refused client-side as `cycle_error_duplicate_start` rather than silently folded into an edit — `RoomCycleRepository.upsert` itself still would fold it, so the check lives in `CycleViewModel`), "Period ended" (sets `periodEndDay` on the latest entry), history list (delete with confirmation) and a 6-cycle forecast list; empty state when nothing is logged. Pure helpers in `CycleUiState.kt` (`dayOfCycleLabel`, `countdownLabel`, `ovulationLabel`, `fertileWindowLabel`, `phaseLabelRes`, `confidenceLine`, `historyRows`) — hardcoded English strings, matching the existing `lastSyncedLabel`/`weightChipLabel` convention, since only `phaseLabelRes` needs a `@StringRes` id (used directly in a composable). **Today**: a `TodayCycleCard` (phase, day of cycle, countdown; tap → `CycleRoute`) shown when tracking is enabled, with an empty-state hint when nothing is logged yet; `TodayViewModel` gained a `cycleRepo` dependency and a 5th flow into the `coreAndSync` combine. **Calendar**: new pure `ui/calendar/CycleMarkers.kt` (`CycleDayMarker{LOGGED_PERIOD,PREDICTED_PERIOD,OVULATION,FERTILE}`, `cycleDayMarker(status)`) computed straight from `CycleEngine.statusesFor` over the visible window (only while tracking is enabled) in `CalendarViewModel`; `DayCell`/`WeekStrip` render it as an extra dot (`CYCLE_MARKER_TEST_TAG` for the instrumented test) — solid for a logged period day, translucent for a predicted one, a ring for ovulation, a translucent dot for the fertile window (a marker-row treatment rather than literal cell-background tinting, matching the app's existing `DayMarker` dot language). `DayDetailViewModel`/`DayDetailUiState` gained a per-day `CycleStatus` (via `CycleEngine.statusFor`, reactive to the shown day); `DayDetailSections.kt` adds a "Cycle" line ("Menstrual · Day 1 of ~28 (predicted)"). **Settings**: a "Track menstrual cycle" switch in `AppPreferencesSection` (`settings_cycle_tracking_switch` test tag). **Onboarding**: `OnboardingDraft.cycleTrackingEnabled` (default `true`), the same switch shown in step 3 only for `Sex.FEMALE`; `OnboardingViewModel.save()` now honours that draft value instead of always forcing `true`. Rationale ids (`CYCLE_MENSTRUAL_EARLY`/`CYCLE_FOLLICULAR`/`CYCLE_OVULATION`/`CYCLE_LATE_LUTEAL`, PLAN item 5) were already readable plain-English sentences from P11.2 — no change needed. Tests: `CycleUiStateTest` (10 cases: label formatting, phase mapping, history ordering), `CycleMarkersTest` (6 cases) — both new, +16 unit tests (718 → 734); instrumented `app/src/androidTest/java/com/myhealth/ui/CycleScreenTest.kt` (seeds a FEMALE profile via `TestSupport.seedProfileBlocking(sex = Sex.FEMALE)`, More → Cycle → Log period start → asserts "Menstrual" + "Day 1 of ~28" → Calendar asserts a `CYCLE_MARKER_TEST_TAG` node exists); compiles (`:app:compileDebugAndroidTestKotlin`), not run (no emulator). `bash tools/verify.sh` → `VERIFY OK` (734 tests, 0 failures; lint 0 errors; debug APK 88.6 MB, +2.4 MB over the P11.2 baseline from the new screens/strings, within the 5 MB no-explanation-needed band; release APK 14.0 MB). |

## P12 — Cycling workouts (release 1.1.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P12.1** Data + sources | opus | **yes** | 775 | 88.0 (release 14.0) | **Enums** (all appended, ordinals preserved): `SessionType += ENDURANCE_RIDE, BIKE_INTERVALS, TRAINER_SESSION, RECOVERY_SPIN`; `GoalType += BIKE_FTP, BIKE_VOLUME, BIKE_EVENT`; `LoadMethod += POWER_TSS`; new `RideBestKind` (`POWER_5/20/60MIN` watts, PR = `MAX`; `TIME_10/20/40/100K` seconds, PR = `MIN`) with `isPower`/`windowSec`/`distanceMeters` helpers. Exhaustive-`when` sites filled minimally: `Rationale.sessionPurpose` (4 new lines), `Scorer.goalSportGroup` (`BIKE_* → CYCLE`), `GoalProgress.compute` + `GoalDraft`/`GoalEditScreen` (P12.2/P12.4 placeholders — the bike goals report as *manual* and use the generic value field), `ActivityDetailScreen` (`activity_detail_load_method_power_tss` = "from power (TSS)"). `sug01…sug26`, `goal01…goal12`, `pr01…pr12` unchanged. **Models**: `ActivitySession`/`ActivitySummary` += `avgPowerW`/`maxPowerW`/`normalizedPowerW`, `ActivityStreams.powerW: IntArray?` (LVCF, hand-written `equals`/`hashCode` extended), `Profile` += `ftpWattsManual`/`indoorTrainerAvailable`, new `RideBest`; `cadenceSpm`/`avgCadenceSpm` documented as rpm for CYCLE rides everywhere they appear. **Room v5** (`MIGRATION_4_5`, SQL copied from the exported `5.json`; `1–4.json` untouched): 3 session columns, `activity_stream.powerWJson`, 2 profile columns, `ride_best` (+ `idx_ride_best_kind_value`, unique `uq_ride_best_activity_kind`, FK `SET_NULL`). `indoorTrainerAvailable` carries `@ColumnInfo(defaultValue = "0")` so the exported `createSql` matches the `ALTER TABLE … NOT NULL DEFAULT 0` the migration must write. `RideBestDao` derives the PR per kind in one grouped query (`MAX(CASE WHEN kind LIKE 'POWER_%' THEN value ELSE -value END)`) projected into `RideBestRow`; `Converters` decode an unknown `RideBestKind` to `POWER_5MIN` (a stray power row is one extra line on the Bike screen, whereas decoding watts as a `TIME_*` kind would win a `MIN` PR outright). `RideBestRepository`/`RoomRideBestRepository` mirror the running-best pair, wired as `AppGraph.rideBestRepo`. **Merge**: `ActivityFields += AVG_POWER_W/MAX_POWER_W/NORMALIZED_POWER_W` in `MergeFieldGroup.MOTION` (not `STREAMS`, which has no `CSV_IMPORT` rank); each field is picked independently so a CSV-only NP survives an HC avg/max. **Backup**: `CURRENT_SCHEMA_VERSION` 3 → **5** (fixes the pre-existing drift), `rideBest` list + `rowsPerTable`, `BackupDao` all/insert/delete, REPLACE wipes it before `activity_session`, MERGE keys on `"$kind\|$value\|$day"` with `activityId` remapped; an older backup without the new fields still decodes to the entity defaults. **Sources** — CSV: `Columns += AVG_POWER/MAX_POWER/NORMALIZED_POWER/AVG_CADENCE` with the EN and DE aliases; `normalizeHeader` already resolves `Ø Leistung`, `Max. Leistung`, `Normalized Power® (NP®)` and `Ø Trittfrequenz`; `0` and `--` both decode to `null`. FIT: `FitRecord.powerW`, `FitSession.avg/max/normalizedPowerW` from `RecordMesg.power` / `SessionMesg.avg-max-normalizedPower`, mapped to the session fields and a LVCF `powerW` stream. Health Connect: `HcExercise += powerSamples/pedalCadenceSamples` read per session window, axis fallback `hr ?: speed ?: power`, `derive()` computing avg/max/NP, pedal cadence used for CYCLE. `domain/engine/bike/PowerMath.kt` (pure): time-weighted 30-s trailing NP with a 60-s gap cap, `averagePower`, `null` under 30 s. **Tests**: `bike01`…`bike08`, `np01`…`np03` plus a sparse-axis NP case, a new binary `ride_power.fit` fixture (60 min, 220 W with a 20-min 300 W block, 90 rpm, no HR) written by `FitEncoderFixtureTest`, two new CSV fixtures copied verbatim out of the owner's export, and androidTest `migration_4_to_5_adds_power_columns_and_ride_best` (compiles; needs a device). `bash tools/verify.sh` → `VERIFY OK` (775 tests, 0 failures, lint 0 errors). **Two plan deviations, both forced by the SDK** (see the report): connect-client 1.1.0 maps `CyclingPedalingCadenceRecord` to `android.permission.health.READ_EXERCISE`, not to a `READ_CYCLING_PEDALING_CADENCE` that does not exist — so `OPTIONAL_DETAIL` adds only `android.permission.health.READ_POWER` to the manifest and to `ALL`, and the Integrations row for `READ_EXERCISE` is relabelled "Workouts and cycling cadence" instead of getting a second row. The German export spells the running and the cycling cadence column identically, so the parser now keeps **all** indices per canonical column name and takes the first non-empty cell. R10 splits: `HcMapper.kt`'s derivation half moved to `HcStreams.kt` (291 + 152 lines) and the two HC power cases to `HcMapperPowerTest.kt`. |
| **P12.2** Engines | opus | **yes** | 807 | 90.2 (release 14.1) | `domain/engine/common/SplitFinder.kt` holds the two-pointer split scan, moved **verbatim** out of `RunningBestEngine.bestSplitSec` (which now delegates; `pr01…pr12` unchanged). **`FtpEstimator`** → `FtpEstimate(watts, source ∈ {MANUAL, STREAM_20MIN, SESSION_NP}, basisActivityId?, basisDay?)`: manual ?: `0.95 × max POWER_20MIN` (≤ 90 d) ?: `0.95 × max(normalizedPowerW ?: avgPowerW)` over eligible rides ≥ 40 min (≤ 90 d); half-up watts, sanity 50–600 W → **null** rather than a clamp, never cached. `RidePower` is the tiny input type, with `estimateFromSessions`/`estimateFromSummaries` adapters so the service and the ViewModels can both drive it. **`BikeBestEngine`**: `POWER_5/20/60MIN` = max time-weighted mean over exactly W seconds off a step function whose gaps are capped at 60 s (both window edges enumerated, since the window integral is piecewise linear), coverage ≥ W, `isEstimated` above a 10 s median interval, sanity 30–1500 W; `TIME_10/20/40/100K` via `SplitFinder` plus the full-ride method (`max(1 %, 50 m)` tolerance, scaled, estimated beyond 5 m), sanity 8–70 km/h, non-estimated wins then faster. HC rides carry no cumulative distance stream, so their `TIME_*` bests come from the full-ride method only (documented in §3.8.3). **`TrimpCalculator`** gains the `POWER_TSS` rung between `HR_AVERAGE` and `RPE_ESTIMATE` (`TrimpDefaults.TSS_TO_TRIMP = 1.5`, `TrimpInput += avgPowerW/normalizedPowerW/ftp`); `ESTIMATED_LOAD` only for a `SESSION_NP` FTP, and — decided here — **no** `MISSING_HR` on the power rung, which is a measurement rather than a stand-in for one. **`LoadRecomputeService`** takes `rideBestRepo` and runs `ride_best` refresh for every CYCLE session → `getSince(today − 90)` + rides → FTP resolved once → TRIMP. **`GoalProgress`** gains `rideBests`/`ftp` parameters: `BIKE_FTP` = FTP ÷ target, on track from 95 %; `BIKE_VOLUME` = ride hours over 4 weeks ÷ 4; `BIKE_EVENT` with a time = best `TIME_*` of that distance (no Riegel equivalent for rides, so "on track" means ridden), date-only = manual. `GoalsViewModel` now also reads `rideBestRepo` (PR table + the top 50 `POWER_20MIN` rows) and `profileRepo`, and widens its activity window to 90 days for the FTP rung. Nutrition `completedKcal` ladder: recorded kcal > `avgPowerW × durationSec / 1000` > MET. Docs: PLAN §3.2.2/§3.2.4 (power rung + `pw01…pw06`) and a new §3.8. Tests: `pw01…pw06`, `ftp01…ftp05`, `rb01…rb07`, `goal09…goal12`, `nut22`, plus `ride_bests_are_refreshed_and_the_ftp_resolved_before_trimp_for_a_power_only_ride` (the ride scores 120.08 AU off FTP 285 W, unreachable unless the 300 W `POWER_20MIN` row was written first) and gap-cap / sanity-gate cases. `sug01…sug26`, `pr01…pr12`, `load01…load18`, `goal01…goal08` untouched. `bash tools/verify.sh` → `VERIFY OK` (807 tests, 0 failures, lint 0 errors) and `:app:compileDebugAndroidTestKotlin` green. |
| **P12.3** Suggestions | opus | **yes** | 824 | 90.2 (release 14.1) | **Catalog** `SessionCatalog.ALL += ENDURANCE_RIDE (CYCLING, LOW, 90, RPE 4 → 108 AU), BIKE_INTERVALS (CYCLING, HIGH, 60, 8.0 → 144), TRAINER_SESSION (CYCLING_INDOOR, MODERATE, 45, 6.0 → 81), RECOVERY_SPIN (CYCLING_INDOOR, RECOVERY, 30, 2.0 → 18)`, appended and **gated** by `suggestableFor(bikeEnabled)`. **New `suggest/BikeRules.kt`** (+ `BikeContext`): the gate (`capsOf[CYCLE] > 0` or an ACTIVE `BIKE_*` goal), `isIndoorSeason(day)` = month ∈ Nov…Mar evaluated per candidate day — with `profile.indoorTrainerAvailable` every `CYCLING` candidate (rides **and** `CROSS_TRAINING`) becomes `CYCLING_INDOOR` and carries `BIKE_INDOOR_SEASON`, while `TRAINER_SESSION` is offered year-round but only with the flag — and `C14`, the bike spacing. **Periodization**: `primaryRaceGoal` accepts `RACE_TIME` **and** `BIKE_EVENT`; new `bikePreferredTypes(phase)` second table, selected by `Scorer.preferredTypesFor(phase, primaryGoalGroup)` when the primary goal's group is `CYCLE`; the taper shortens `BIKE_INTERVALS` ×0.6 like `INTERVAL_RUN`. **Constraints**: one new id `C14` (≥ 3 d between two `BIKE_INTERVALS`, ≥ 2 d from any other hard item, symmetric, in `BikeRules.violatesSpacing`); `RECOVERY_ONLY_TYPES += RECOVERY_SPIN` (C4/C8); the `CYCLE` cap rides on the existing `C10`. `BIKE_INTERVALS` is `HIGH`, so C1/C5/C9 needed no change. **Rationale** ids `BIKE_FTP_GOAL`/`BIKE_VOLUME_GOAL`/`BIKE_EVENT_PREP` (one per `CYCLE` session, naming the top-priority active bike goal) and `BIKE_INDOOR_SEASON`; rationale text is built in `domain/` and rendered verbatim by `RationaleList`, so there is no `strings.xml` mapping to extend. **`SuggestionInputsHash`** adds `ftpWattsManual` + `indoorTrainerAvailable` as a `bike=` line that is **omitted when neither is set**, so existing profiles keep their digest (no forced regeneration on upgrade) — this is what lets `sug28` assert the hash, not just the sessions. **UI**: `ONBOARDING_SPORT_GROUPS += CYCLE`, `DEFAULT_SESSIONS_PER_WEEK += CYCLE to 0` (an explicit 0 next to non-zero sports is a real cap, not POLISH-11's all-zero case), `PLANNABLE_SPORT_TYPES += CYCLING_INDOOR` so `sessionTypesFor(CYCLING/CYCLING_INDOOR)` offers the four new types. **Tests**: `sug27`, `sug29`…`sug33` in the new `SuggestionEngineBikeTest` (R10 split, like `SuggestionEngineCycleTest`), `sug28` in `SuggestionEngineTest` — it diffs the full rendered output *and* `inputsHash` of nineteen bike-free fixtures against `app/src/test/resources/fixtures/suggest/sug28_baseline.txt`, generated from the pre-change engine (7095a77) by `SuggestionSnapshot.renderAll` before any source was touched; `c14`/`c15`/`c16` in the new `ConstraintsBikeTest` (the c-numbers follow the task, the ids do not: only `C14` is new, `c14_cycle_cap_respected` pins `C10` and `c16_…` pins `C4`); `BikeRulesTest`; `cat01` extended to sixteen rows; `ScorerTest` gains the bike goal groups and the second phase table. `sug01`…`sug26`, `pr01`…`pr12`, `goal01`…`goal12` unchanged. `bash tools/verify.sh` → `VERIFY OK` (824 tests, 0 failures, lint 0 errors) and `:app:compileDebugAndroidTestKotlin` green. **Open point for P12.4**: with the new onboarding default the `CYCLE` cap is written as 0, and `C10` then blocks every ride even when a `BIKE_*` goal exists — Settings must expose "Rides / week" (it currently hardcodes RUN/STRENGTH/SOCCER) or a rider's goal alone produces no rides. |
| **P12.4** UI | sonnet | **yes** | 828 | 88.9 (release 14.1) | **Settings**: `PreferredSportsFields` iterates `ONBOARDING_SPORT_GROUPS` (was hardcoded RUN/STRENGTH/SOCCER) so the `CYCLE` cap is reachable, labelled "Rides / week" (onboarding step 3 gets the same label for its already-present `CYCLE` field); a new "Cycling" section: FTP override (nullable watts; hint resolves the estimate with `manualWatts = null` so it always shows the underlying estimate, e.g. "Estimated 247 W from a ride on 2 Sep") and "Indoor trainer available". `SettingsViewModel` gained `rideBestRepo`/`activityRepo` to resolve that hint's `FtpEstimate`. **Activity detail**: a power card (avg/NP/max; IF = `NP/ftp` and TSS = `TrimpCalculator.powerTss`'s formula duplicated locally, shown once an FTP resolves) for any ride with a power field, a power-over-time chart (`ActivityCharts.powerPoints`/`ActivityChartCards.PowerChartCard`) when `streams.powerW` exists, cadence relabelled rpm for `SportGroup.CYCLE` (the stored number is unchanged, P12.1 already documents it as rpm); `ActivityDetailViewModel` resolves its own `FtpEstimate` the same way `GoalsViewModel` does (`rideBestRepo` + a 90-day `activityRepo.observeRange`); the `POWER_TSS` load-method label was already wired in P12.1. **New `ui/bike/{BikeUiState,BikeViewModel,BikeScreen}.kt`** + `BikeRoute`, More entry "Bike & power": FTP card (value, source in plain words, basis date linking to the basis ride, indoor-trainer status line) and two PR tables (power bests 5/20/60 min max-wins, time bests 10/20/40/100 km min-wins) each row linking to its ride and marked `(est.)` when estimated; `List<RideBest>.bestPerKind/powerBests/timeBests` are pure, unit-tested reductions (defensive re-aggregation matching `RideBestDao.observeBestPerKind`'s SQL). **Goals**: `GoalDraft` gets a `BIKE_EVENT` distance set (`BIKE_EVENT_DISTANCES` = 10/20/40/100 km, default 40 km on a mismatched switch) with its own validation arm (distance required, time optional per `GoalProgress.bikeEvent`) and `toGoal`/`goalHeadline` now carry `targetDistanceMeters`/`targetTimeSec` for `BIKE_EVENT` instead of the P12.1 generic `targetValue` placeholder (`BIKE_FTP`/`BIKE_VOLUME` keep the generic watts/hours field, already correctly labelled since P12.1); `GoalEditScreen`'s `BIKE_EVENT` arm is a distance dropdown + optional min/sec fields, mirroring `RACE_TIME`'s; `GoalsUiState.showCycleCapHint` (an active `BIKE_*` goal with the `CYCLE` cap at 0) renders a hint line above the list ("Set Rides / week in Settings to get ride suggestions"). **Session labels**: verified already correct from P12.3 — `SessionType.label()`/`displayName()` are generic name-splitters so the four new types need no mapping, and `sessionTypesFor(CYCLING/CYCLING_INDOOR)` already lists them (`PLANNABLE_SPORT_TYPES` already includes both sports) — no code change needed. Tests: `BikeUiStateTest.bikeui01_ftp_card_shows_source_and_basis`/`bikeui02_bests_table_max_for_power_min_for_time`, `GoalDraftTest.goaldraft_bike_event_defaults_40km`/`goaldraft_bike_ftp_requires_watts`; `sug01…sug26`, `pr01…pr12`, `goal01…goal12` unchanged. `bash tools/verify.sh` → `VERIFY OK` (828 tests, 0 failures, lint 0 errors — `HardcodedText` stays an error; debug APK 88.9 MB, +0.9 MB over P12.3's 88.0 MB for the new Bike screen/strings; release 14.1 MB) and `:app:compileDebugAndroidTestKotlin` green. **Ambiguity resolved**: the plan's Bike-screen "hint to set the override when no estimate exists" and the Settings FTP hint are two different things (one says "no estimate at all", the other always shows the non-manual estimate even when an override is set) — implemented as written, both independently correct. |
| **P12.5** Instrumented + emulator | sonnet + lead | **yes** | 830 | 88.9 (release 14.1) | **Lead half (emulator, VERIFICATION.md session 10)**: seeder extended with a Tuesday trainer ride (`PowerRecord` + pedal cadence, no HR) and a Saturday 40.2 km ride; upgrade path 1.0.3 → 1.1.0 on populated data without re-granting (sync fine, no power), then `READ_POWER` granted from Integrations → found **BUG-13** (neither the changes token nor a backfill re-reads already-synced sessions) → fixed with `SyncScheduler.rereadExerciseDetail(90)` from `IntegrationsViewModel.onPermissionsResult`; afterwards FTP 285 W (20-min best), trainer ride "121.0 (from power (TSS))" with power card/chart, Bike & power bests (40 km 1:14:38 est.), Settings trainer + ride cap, Bike FTP goal "285 W … on track", generated week with two trainer sessions. POLISH-15 (cap label, whole watts). Instrumented half (sonnet): new `BikeScreenTest.kt` (`bikeScreen_showsFtpFromManualOverride_andRideBests`, `settings_persistsIndoorTrainerAndRideCap`, `goalEditor_createsBikeFtpGoal_showingProgressLine`) plus two Settings testTag hooks (`settings_indoor_trainer_switch`, `settings_ride_sessions_field`); `:app:connectedDebugAndroidTest` on `emulator-5554` (scoped via `ANDROID_SERIAL`, since a physical Pixel 7a was also attached) → 17/17 passed, and `bash tools/verify.sh` → `VERIFY OK` (830 unit tests, 0 failures, lint 0 errors; debug 88.9 MB / release 14.1 MB, unchanged). |
| **P12.6** Release 0.2.0 (was 1.1.0) | lead | **yes** | 830 | 88.9 (release 14.1) | Tagged and published 2026-09-13 (renumbered from v1.1.0 the same day, with v0.2.1 = BUG-14 hotfix). |

## P13 — Active recovery on rest days (release 0.3.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P13.1** `ActiveRecovery` post-pass 7c | lead | **yes** | 838 | — | `GridItem.isActiveRecovery` / `isRestDayFiller`, `DayPlan.isTrueRestDay`; `Rationale.forActiveRecovery` (`ACTIVE_RECOVERY`); rules: one true rest day (farthest after the last hard day), run when the run cap allows, spin when the cycle cap > 0 or a trainer exists, alternation, no same type on adjacent days, spin on early-menstrual/late-luteal days, eve of a key event and a `STRAINED` today excluded. Tests `ar01…ar08`; `sug03/13/16/27/29` adjusted to ignore fillers; `sug28` baseline regenerated. |

## P14 — Heart-rate zones, paces, intervals and strength (release 0.4.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P14.1** Zone model, schema v6, strength storage | opus | **yes** | 860 | 87.3 (release 14.2) | **Enums** (all new classes, nothing appended to an existing one, so every ordinal and every fixture is untouched): `HrZoneScheme`, `BodySide`, `MuscleGroup` (16, each carrying `side` + `isLowerBody`), `Equipment`, `MovementPattern`, `StrengthWorkoutKind`, `MuscleLoadBand`, `WorkoutStepKind`, `WorkoutTargetKind`. **Zones (§3.9)**: `HrZoneModel` (`HrZone`, `resolve(profile, bounds)`, `zoneOf`/`rangeOf`/`minutesPerZone`) with the three schemes resolved MANUAL → LTHR_FRIEL → HRR_KARVONEN; an implausible manual blob is *ignored* with `IMPLAUSIBLE_VALUE`, never rejected. `timeInZones` is kept **verbatim** and joined by a model-driven overload `timeInZones(offsets, hr, model)` with the same 60 s gap cap and null-sample rule, so the default Karvonen model reproduces the legacy band list exactly (`hz06`); `PolarisationSplit.of` is the 28-day easy/hard split, and `SessionZoneTargets.targetFor` the whole per-session-type table (null for `STRENGTH_*`, `SOCCER_*`, `REST`). **`domain/model/Workout.kt`**: `WorkoutStructure`/`WorkoutStep` + `WorkoutStructureCodec` (kotlinx.serialization; decode returns `null` for an unknown `version`, for malformed JSON and for nesting deeper than one `REPEAT` level — no builder yet, that is P14.3). **Room v6** (`MIGRATION_5_6`, every statement copied verbatim from the exported `6.json`; `1–5.json` untouched): the three tables first (`strength_workout` + unique `uq_strength_workout_template`, `strength_workout_exercise` + `idx_swe_workout`/`uq_swe_order` CASCADE, `strength_set_log` + `idx_ssl_day`/`idx_ssl_planned`/`idx_ssl_activity`, both links `SET_NULL`), then `ALTER TABLE` for `profile` (`hrZoneBoundsJson`, `lactateThresholdHrManual`), `suggested_session` (`targetPaceSecPerKm`, `structureJson`, `workoutTemplateId`) and `planned_session` (`structureJson`, then `workoutId INTEGER REFERENCES strength_workout(id) ON DELETE SET NULL` + `CREATE INDEX idx_planned_workout`). **The FK was added by `ALTER TABLE`, not by recreation** — SQLite accepts a `REFERENCES` clause on an added column whose default is NULL, so `planned_session` (the most-referenced table in the app) is never rebuilt; §6.4 row 6 stands as written. **Storage**: 3 entities + `StrengthWorkoutWithExercises` + `StrengthDao` (every read `@Transaction`) + `StrengthMappers` (sorts children by `orderIndex`) + `domain/model/Strength.kt` (`StrengthWorkout`, `StrengthWorkoutExercise`, `StrengthSetLog`) + `StrengthRepository`/`RoomStrengthRepository` (whole-workout upsert that rewrites the rows and renumbers `orderIndex` from the list position, so a reorder cannot leave a hole) wired as `AppGraph.strengthRepo`. `Converters` decode an unknown `StrengthWorkoutKind` to `CUSTOM`. **Backup**: `CURRENT_SCHEMA_VERSION` 5 → **6**, three new lists + `rowsPerTable`, `BackupDao` all/insert/delete, REPLACE deletes set logs and exercise rows first and `strength_workout` right after `planned_session` (inserting in the mirror order, workouts before planned sessions), MERGE keys a workout on `templateId ?: "custom\|name"`, a set log on `completedAtMillis\|exerciseId\|setIndex` (both links remapped, including the new planned-session remap) and carries the exercise rows as owned children. **Models**: `Profile`, `PlannedSession`, `SuggestedSession` gained their new fields **with defaults, appended last**, so every existing call site compiles unchanged and `SuggestionInputsHash` — which lists the profile fields it hashes explicitly — produces a byte-identical digest (`sug28`'s baseline file untouched). **Tests** (+22, 838 → 860): `HrZoneModelTest` `hz01`…`hz10`, `WorkoutStructureTest` `iv08`/`iv09` + a nesting case, `StrengthMappersTest` `sw06`/`sw07` + an empty-workout case, `MapperRoundTripTest` gained `suggestedSession_roundTrips` (and the new columns in the profile/planned cases), `BackupServiceTest.strength_workout_and_set_log_merge_by_natural_key`, and the new `MigrationSqlTest` — a device-free guard that parses `6.json` and compares its `createSql`/index SQL character for character against the (concatenation-flattened) source of `Migrations.kt`, checks every `ALTER TABLE`, and asserts `planned_session` is not recreated. androidTest `migration_5_to_6_adds_strength_tables_and_session_columns` populates a v5 database (profile, plan, planned session, batch, suggested session), validates the upgrade and then exercises the FK (`SET_NULL` on workout delete) and the CASCADE — compiles, needs a device. R10 split: the five migration replays moved out of `MyHealthDatabaseTest` (which was past 400 lines) into the new `MyHealthMigrationTest`. `bash tools/verify.sh` → `VERIFY OK` (860 tests, 0 failures, lint 0 errors; debug 87.3 MB, release 14.2 MB) and `:app:compileDebugAndroidTestKotlin` green. **Ambiguity resolved**: P14.1 needs domain models for its mappers while P14.4 lists `domain/model/Strength.kt` as a *Create* — the file is created here with the three **stored** models only; `Exercise` and the computed `estimatedMinutes` still belong to P14.4. Second: the model-driven zone minutes use `zoneOf` (so the bpm ranges a screen prints are the ones the minutes were counted with) rather than delegating to the raw HRR bands; the two agree wherever `reserve × 0.6/0.7/0.8/0.9` is integral (the `hz06` fixture and the owner's 190/50 bounds), and the one-bpm difference otherwise is documented in `HrZoneModel`'s KDoc. |
| **P14.2** Zone ↔ pace engine + Daniels paces | opus | **yes** | 884 | 88.2 (release 14.2) | **`DanielsPaces`** (§3.10.1): `VdotCalculator.velocityFor(vdot, pct)` inverts the §3.4 oxygen-cost polynomial (its three coefficients are now named constants, `vo2` unchanged), and `DanielsPaces.secPerKmExact/secPerKm/table` turn it into the E/M/T/I/R table — at VDOT 50: 334 / 268 / 255 / 234 / **219** s/km. **`PaceZoneEngine`** (§3.10.2): `compute(PaceZoneInput)` → five `PaceZoneBand(zone, centre/low/high SecPerKm, confidence, inZoneSec, activities)` with all seven sample rules (10 min warm-up skip, 20 s HR-lag pairing with a ±5 s tolerance, 1.50–7.00 m/s speed gate, interval weights capped at 60 s, grade ignored, treadmill behind `includeTreadmillInPrs`, one summary point per stream-less run capping its zone at `MEDIUM`), the weighted median/IQR, the HIGH/MEDIUM/LOW/MODELLED/NONE ladder and the 1.00/0.75/0.50 blend against the Z1 E×1.08 ·5 % · Z2 E ·5 % · Z3 M ·4 % · Z4 T ·3 % · Z5 I ·3 % anchors; `recommendedPaceFor(sessionType, bands)` takes the first zone of `SessionZoneTargets.targetFor` except `TEMPO_RUN` → Z4 and `INTERVAL_RUN` → Z5. **`PaceZoneInput`/`PaceRun`** keep the engine pure: the treadmill setting and the VDOT are passed in, never looked up, and `PaceRun.of(session)` / `of(summary, streams)` are the only adapters. **Speed is the axis** — `distanceMeters` is only a fallback when a FIT stream has distance but no speed, so a Health-Connect run works (pinned by `pz01`, whose fixture has speed only). **Tests** (+24, 860 → 884): `DanielsPacesTest` `vd01`…`vd06` + 3, `PaceZoneEngineTest` `pz01`…`pz12` + 3 (recommendation table, 90-day/sport window, determinism); `pr01`…`pr12` untouched; `:app:testDebugUnitTest --tests "*PaceZone*"` run twice with `--rerun-tasks`, results identical. `bash tools/verify.sh` → `VERIFY OK` (884 tests, 0 failures, lint 0 errors; debug 88.2 MB, release 14.2 MB), `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved** (all noted in PLAN §3.10): `vd05`'s exact value is 219.47 s/km, not 220 — inside the plan's ±1; `pz01` cannot assert the measured centres 333/250 *and* `MODELLED` empty zones in one call, because one activity caps confidence at `LOW` and `LOW` blends half with the anchor, so the test asserts the centres with no VDOT and the `MODELLED` zones with VDOT 50 (which gives 334/253); a zone below the 120 s `LOW` floor is treated as unmeasured rather than as a sixth rung; and a modelled/blended band is spread from the **rounded** centre, which is what produces `pz10`'s 247…263. |
| **P14.3** Structured workouts + interval catalog + suggester wiring | opus | **yes** | 898 | 88.2 (release 14.2) | **`IntervalCatalog`** (§3.11): the eleven templates as data (`IntervalTemplate`/`IntervalStepSpec` — min/max reps, a work step, a recovery step, the Daniels pace or the FTP share, `innerReps` for the 30/30 and the 15/10 min run · 10/5 min ride framing). **`IntervalBuilder.buildFor(candidate, ctx)`** implements the seven selection rules: the four structured session types, the phase table (`PEAK` → 400s for a ≤ 5 k goal else 1000s, `BUILD` → 1000s / `BIKE_4X8_FTP`, `BASE` → hills / cruise, `IN_SEASON` → 4×4 / 30-30, `TAPER`/`RACE_WEEK` → the peak choice shortened, `RECOVERY_WEEK`/`OFF_SEASON` → none), the ≥ 21.1 km / ≤ 5 k goal overrides, `reps = clamp(floor(workBudget / repCycle), minReps, maxReps)`, the `acwr > 1.30` / `MODERATE`-`FATIGUED` / starter-week minimum, the zone-only fallback without a VDOT or FTP, and the 6 km work-distance cap; pace targets come from the measured `PaceZoneBand` when it measures that zone's own anchor pace and from `DanielsPaces` otherwise, power from the FTP, and `summary(structure)` renders "5 × 1000 m @ 3:54" / "4 × 8:00 @ 271–299 W" / "2 × (10 × 0:30) @ 371 W" for the cards. R10 split: the value types (`IntervalContext`, `IntervalPlan`) and the step assembly live in `IntervalPlan.kt` (`IntervalStructures`). **Engine**: `SuggestionInput` gained `vdot` / `paceBands` / `ftpWatts` (all defaulted), `SuggestionEngine.resultOf` attaches `structureJson` + `targetPaceSecPerKm` when it builds each `SuggestedSession` — **candidate generation, scoring and constraints are untouched** — `Rationale.intervalEntries` adds `INTERVAL_STRUCTURE` / `INTERVAL_SHORTENED_TAPER` / `PACE_TARGET`, and `SuggestionInputsHash` gained a `paces=` line that is **omitted** when all three inputs are empty. **Repository**: the new `SuggestionPaceResolver` fills the three inputs the way the rest of the app already resolves them (Riegel source + `VdotCalculator` off `running_best`; `PaceZoneEngine` over 90 days of runs against `HrZoneModel.resolve`; `FtpEstimator` over the manual override, `ride_best` and the rides), wired into `RoomSuggestionRepository` through three **optional** repositories (`runningBestRepo`, `rideBestRepo`, `healthRepo` — `null` in the P6.5 tests, so nothing is read there), and `accept` now carries `structureJson` + `targetPaceSecPerKm` into `planned_session` (`workoutTemplateId` has no counterpart until P14.5 materialises it as `workoutId`). `PlanMappers` needed no change — P14.1 already mapped all three columns. **Tests** (+14, 884 → 898): `IntervalBuilderTest` `iv01`…`iv07` + `iv10`…`iv13` (11), `SuggestionEngineIntervalTest` `sug34`/`sug36`, and `sug35_structure_survives_accept` in `RoomSuggestionRepositoryTest` (the repository level, where the P6.5 fakes already round-trip a suggestion into a planned session). `sug01`…`sug33` and `ar01`…`ar08` pass unchanged and `sug28_baseline.txt` was **not** touched — the new structures are invisible to it because the digest line is omitted, `structureJson` is not part of the snapshot rendering, and the three rationale lines are only emitted once a pace or power exists. `bash tools/verify.sh` → `VERIFY OK` (898 tests, 0 failures, lint 0 errors; debug 88.2 MB, release 14.2 MB), `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved** (all recorded as PLAN §3.11 notes): `iv03`'s "5 → 3 reps" contradicts rule 2's own `max(minReps, …)` floor, which gives **4** for `RUN_1000_I`; `iv07`'s `2 × (10 × 30/30)` cannot nest a `REPEAT` in a `REPEAT` under the P14.1 codec, so the inner ten sit as `repeat = 10` on the outer repeat's children; `TAPER`/`RACE_WEEK` reuse the `PEAK` template choice; a distance template with no pace to convert it falls back to the time-based `RUN_4X4`; the 6 km cap applies to distance-based work only; and `targetPaceSecPerKm` is filled for every running session type with a zone target (§3.10's rule), not only for the four that carry a structure. |
| **P14.4** Exercise catalog, muscle groups, workout templates + repository | opus | **yes** | 913 | 88.2 (release 14.2) | **`ExerciseCatalog`** (§3.12.1) — a Kotlin object, not a JSON asset: **54 entries** across `ExerciseCatalogUpper.kt` (22), `ExerciseCatalogLower.kt` (22) and `ExerciseCatalogCore.kt` (10), joined by `ExerciseCatalog.ALL` (R10; each file ≲ 220 lines). Every entry is an `Exercise(id, name, primary, secondary, equipment, pattern, unilateral, isTimed, cue)`; **ids are permanent** because `strength_workout_exercise.exerciseId` stores them, and a duplicate id is a `check` at class init. Coverage: all **16** muscle groups are the primary of at least one entry (`ex04`), **20** entries are `BODYWEIGHT`-only (`ex10` needs ≥ 12), primary ∩ secondary = ∅ everywhere (`ex03`), all 8 `Equipment` and all 11 `MovementPattern` values are used. API: `byId` (null, never throwing — a saved row may name a dropped id), `search(query)` over the name **and** the muscle-group names with `_` → space so "lower back" works (name matches first, then muscle-only matches, catalog order inside each half → deterministic), `filter(equipment?, pattern?, muscle?)` AND-ed with `null` = any and `muscle` matching primary **or** secondary, and `byPrimary(group)` for the body figure's tap target. **`domain/model/Strength.kt`** gained `Exercise` (+ `allMuscles`, `isBodyweightOnly`) and the computed `StrengthWorkout.estimatedMinutes` = `ceil((Σ sets × (workSec + restSec) + 480) / 60)` with `workSec = seconds ?: reps × 3` and `DEFAULT_REST_SEC = 90` — computed, never stored, so a template corrected in code cannot leave a stale number in a row. **`StrengthTemplates`**: `UPPER_A`/`UPPER_B`/`LOWER_A`/`LOWER_B`/`FULL_A`/`CORE_A`, 6–7 exercises each, built through a small private `TemplateBuilder` whose `reps()` rejects a timed exercise and whose `hold()` rejects a counted one — so `ex08` (plank/side plank/hollow hold carry `seconds`, never `reps`) cannot be broken by a later edit, and every id is checked against the catalog at class-initialisation time. `UPPER_A` is exactly 6 × 3 sets × 10 reps × 90 s rest → **44 min** (`sw04`); the upper days contain no lower-body primary and the lower days no upper-body primary (`sw02`/`sw03`). **`StrengthWorkoutSeeder`** (`data/repository/`): idempotent on `templateId` — it skips a built-in whose row exists, so seeding twice leaves **6** rows and a user's edit to a seeded copy survives (`sw05`); `workoutFor(templateId)` seeds-then-reads for P14.5's `accept`; wired as `AppGraph.strengthWorkoutSeeder` with the graph's `Clock`. **`RoomStrengthRepository`** gained the validation §2.2.7 cannot express in SQLite: a blank name, a row with `sets < 1`, and a row that sets **both or neither** of `reps`/`seconds` return `AppError.Validation` (not an exception) before anything is written; `orderIndex` compaction on delete/reorder was already inherent (rows are renumbered from their list position on every whole-workout upsert) and is now documented as such. **Tests** (+15, 898 → 913): `ExerciseCatalogTest` `ex01`…`ex10`, `StrengthTemplatesTest` `sw01`…`sw05` (the seeder case runs against a private in-memory `StrengthRepository` fake, since the seeder talks to the interface, not the DAO). `bash tools/verify.sh` → `VERIFY OK` (913 tests, 0 failures, lint 0 errors; debug 88.2 MB, release 14.2 MB), `:app:compileDebugAndroidTestKotlin` green, `ArchitectureTest` green (the catalog imports nothing Android). **Ambiguities resolved**: the plan's "≈ 52" is realised as **54** (≥ 40 required) — the named list of 49 exercises plus five that close coverage gaps (inverted row, band pull-apart, dead hang, Pallof press, hip abduction as its own entry beside hip adduction); "hip abduction/adduction" is **two** entries because they load different groups (`GLUTES` vs `ADDUCTORS`, the only other `ADDUCTORS` primary being the Copenhagen plank); `MuscleGroup` has no medial-delt member, so the lateral raise is `SHOULDERS_FRONT` primary with `TRAPS` secondary; `Exercise` was placed in `domain/model/Strength.kt` (as PLAN P14.4 lists) rather than in the catalog file, so the catalog stays pure data; and holds/isolations in the templates prescribe a 60 s rest rather than the 90 s default — only `UPPER_A`'s estimate is pinned, and `CORE_A` comes out at 43 min. |
| **P14.5** Muscle-load engine + strength suggestion rules (C15) | opus | **yes** | 935 | 88.2 (release 14.2) | **`MuscleDistribution`** (§3.12.4): the five endurance share tables (`RUN`/`SOCCER`/`CYCLE`/`WALK`/`SWIM`, `OTHER` deliberately empty) and the three generic strength tables (`STRENGTH_UPPER`, `STRENGTH_LOWER`, `STRENGTH_FULL` = the renormalised mean of the two), every row `check`ed to sum to 1.00 at class-initialisation time. **`MuscleLoadEngine`** (+`MuscleSession`, `MuscleLoadInput`, `MuscleLoadState`): `deposit → decay → sum → band` — shares from the table for an endurance session, from the exercises themselves for a strength session with a workout (`w(g) = Σ sets × (1.0 primary \| 0.5 secondary)`, normalised) and from the generic table for one without; `0.5^(age/2)` over a 14-day window; `ref = max(0.35 × ctl, 12.0)`; `FRESH < 0.75·ref ≤ LOADED ≤ 1.50·ref < FATIGUED`. `MuscleLoadState` carries **all 16** groups (zeros included, for P14.7's body figure), the per-group bands, `ref`, the lower/upper band (max of the five lower groups / of the other eleven) plus `loadOf`/`bandOf`/`lowerBodyLoad`/`upperBodyLoad`/`intensityOf` for the UI. No cache table (§2.2.7) — it is a few dozen multiplications. **`StrengthRules` + `MuscleContext`** (§3.12.5, the `BikeRules`/`BikeContext` seam): `C15` (appended to `ConstraintId`) discards `STRENGTH_LOWER`/`STRENGTH_FULL` when (a) the day's *projected* lower band is `FATIGUED`, (b) a ≥ 150 AU `RUN`/`SOCCER`/`CYCLE` activity or a `HIGH`/`MAX` grid item of those groups sits within 36 h before, or (c) a `TEMPO_RUN`/`INTERVAL_RUN`/`LONG_RUN`, match or race sits within 48 h after (C2 widened); `Scorer.muscleBonus` adds `+0.10` unweighted (total still capped at 1.0) for an upper day on loaded legs with a fresh trunk and for a leg day on fresh legs with nothing hard ahead; `templateIdFor` alternates the built-ins per kind. **Engine**: `SuggestionInput` gained `muscleLoad` + `lastAcceptedTemplateByKind` (both defaulted), `SuggestionEngine` proposes `workoutTemplateId` on placed `STRENGTH_*` sessions and appends the `STRENGTH_WORKOUT` line, `Rationale` gained `MUSCLE_LOWER_LOADED` / `MUSCLE_LEGS_FRESH` / `C15_RESPECTED` / `STRENGTH_WORKOUT` (the three placement lines live in `StrengthRules.rationaleEntries` so `Rationale.kt` stays under R10's 400 lines), and `SuggestionInputsHash` gained a `muscle=` line **omitted when `muscleLoad == null`**. **Repository**: the new `SuggestionMuscleResolver` (the P14.3 `SuggestionPaceResolver` pattern) assembles `MuscleLoadInput` from 14 days of activities joined to the planned sessions that claimed them (`linkedActivityId` → `sessionType`, `workoutId` → the exercise list) with the latest `daily_load.ctl`, and reads the alternation off the planned sessions carrying a built-in; `RoomSuggestionRepository` takes `strengthRepo` + `strengthSeeder` (both `null` in the P6.5 fakes, so nothing is read there) and `accept` materialises `workoutTemplateId` into `planned_session.workoutId` through `StrengthWorkoutSeeder.workoutFor`. **Tests** (+22, 913 → 935): `MuscleLoadEngineTest` `ml01`…`ml12`, `ConstraintsStrengthTest` `c17`/`c18`/`c19` + an inertness case, `SuggestionEngineStrengthTest` `sug37`…`sug41`, and `accepting_a_strength_suggestion_materialises_its_workout` in `RoomSuggestionRepositoryTest`. `sug39` diffs the **whole** rendered output of all nineteen `SuggestionSnapshot` fixtures against `sug28_baseline.txt` (untouched) and `sug40` pins the digest for `null`; the three new test classes were re-run with `--rerun-tasks`, results identical (V8). `bash tools/verify.sh` → `VERIFY OK` (935 tests, 0 failures, lint 0 errors; debug 88.2 MB, release 14.2 MB) and `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved** (all recorded as PLAN §3.12 notes): `ml03`'s quoted 20.25/10.125 assume a two-secondary barbell row — the catalog's has four, so Σw = 15 and the values are **16.2 / 8.1**; "projected" band = today's load decayed forward with the same half-life; "36 h before" = the candidate day and the day before, "48 h after" = `Constraints.HARD_WINDOW_DAYS`; the template alternation is extended inside a batch by the same-kind occurrence index; and `sug37`'s "day +1" needs the long run on day −1 (a 220 AU run *today* makes day 0/+1 recovery-only under C4) in an `IN_SEASON` week, which is where §3.5.5 prefers `STRENGTH_UPPER`. |
| **P14.6** Zones & paces screen; zone/pace across the plan UI + Settings | sonnet | **yes** | 945 | 89.3 (release 14.3) | **Create** `ui/zones/{ZonesUiState,ZonesViewModel,ZonesScreen,ZoneTable,PaceBandRow}.kt`: `ZonesUiState` holds the resolved `HrZoneModel`, the five `PaceZoneBand`s, VDOT and the `PolarisationSplit`, plus the pure display logic (`zoneRowLabel` — "Z4 · Threshold · 162–175 bpm", Z5 open-ended "176+ bpm"; `paceBandLabel`; `confidenceMessageKind` — HIGH→measured, MEDIUM/LOW→partly modelled, MODELLED→modelled from VDOT, NONE→not enough data; `easyPercent`/`moderatePercent`/`hardPercent`/`showsPolarisationHint` at the < 70 % threshold; `targetZoneIndexFor`/`zoneChipLabel`/`sessionZoneRows` — the whole `SessionType` table, `null`/`null` for `STRENGTH_*`/`SOCCER_*`/`REST`; `lightweightHrZoneModel(profile, today)` — a profile-only resolution the four extended plan-UI rows share, since only a bpm range is needed there, not a measured band). `ZonesViewModel` resolves VDOT (Riegel source + `VdotCalculator`, mirroring `RunningPrsViewModel`), the zone model (`HrBounds.compute` fed 7-day resting HR + the 90-day window's observed max) and the pace bands (`PaceZoneEngine.compute`) directly against `ProfileRepository`/`ActivityRepository`/`RunningBestRepository`/`HealthRepository`/`SettingsRepository` — the same numbers `SuggestionPaceResolver` produces for the suggester, but re-derived here since `ui/` may not import `data/repository/SuggestionPaceInputs.kt` (`ArchitectureTest`); the 28-day polarisation sums `HrZoneModel.minutesPerZone` over every activity with a stream in that window. `ZoneTable` shows the scheme in plain words + the five zone rows; `PaceBandRow` shows one zone's band and confidence sentence; `ZonesScreen` adds the Daniels-paces card, the polarisation bar (three `Box`es, no new chart infra) and the session-type table, with an `EmptyState` when there is neither a VDOT nor any recorded zone time. **Modify**: `ui/nav/{Routes,MyHealthNavHost}.kt` (+`ZonesRoute`), `ui/more/MoreScreen.kt` (+entry). `ui/training/PlannedSessionCard.kt` gained the shared `TargetZoneChip` and `WorkoutStructureSection` composables (decodes `structureJson`, shows `IntervalStructures.summary` collapsed and `structureStepLines` — one line per warm-up/`N ×` work+recovery/cool-down step, each with its own target — expanded) plus `hrZoneModel: HrZoneModel? = null` on `PlannedSessionCard` itself (so the Training week board's existing call site needs no change); `SuggestionReviewScreen`/`ViewModel`/`UiState` gained a profile-resolved `hrZoneModel` and the card now shows the chip, the target pace and the structure section; `PlannedSessionEditViewModel`/`Screen` show the chip and a **read-only** structure display (kept off the editable `PlannedSessionDraft` on purpose — the editor does not offer to change a structure, so `save` cannot lose it either); `ui/today/{TodayViewModel,TodayUiState,TodayScreen,TodayPlanCard}.kt` thread the same lightweight zone model into both the planned and the suggested row. `ui/activities/{ActivityDetailUiState,ActivityDetailViewModel,ActivityDetailScreen}.kt`: the HR-zone table is now `HrZoneModel`-driven (named Z1–Z5, bpm ranges, the athlete's own scheme) rather than the fixed-band table, and the row matching a linked planned session's target zone (`CalendarDay.planned`, matched on `linkedActivityId`) is marked — `HR_ZONE_LABELS` is retired, `timeInZones`'s original fixed-band overload is untouched (nothing else calls it here now, but it is still what P2.9's own value came from). `ui/settings/{SettingsSections,SettingsScreen}.kt`: a **Heart-rate zones** section — the resolved scheme, an LTHR field, the four zone-boundary fields (`parseHrZoneBoundsSlots`/`encodeHrZoneBoundsSlots` tolerate a blank slot per box, unlike `HrZoneModel`'s own all-or-nothing manual-bounds parser, so a partial edit redisplays instead of collapsing) and a live `ZoneTable` preview; `hrZoneBoundsStatus` is `EMPTY`/`VALID`/`INVALID` (partial or non-ascending), and only `EMPTY`/`VALID` persist a clean `hrZoneBoundsJson` — an `INVALID` string is still written (so the domain's own `parseBounds` sees exactly what is in the boxes and falls back to derived zones per `hz08`) but shows the ascending-error message. `res/values/strings.xml` +45. **Tests** (+10, 935 → 945): `ZonesUiStateTest` `zui01`…`zui08`, `SettingsZoneValidationTest` `zui09`/`zui10`. `bash tools/verify.sh` → `VERIFY OK` (945 tests, 0 failures, lint 0 errors; debug 89.3 MB, release 14.3 MB), `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved**: the plan's four extended rows (Activity detail, Settings, Today, suggestion review/planned session) need a zone model but only Activity detail and Settings already had a profile observer cheap enough to reuse as-is — Today, the suggestion review screen and the planned-session editor each gained a `profileRepo` (Today already had one) and use the same `lightweightHrZoneModel` rather than the full 90-day `PaceZoneEngine` resolution, which stays exclusive to the Zones screen since only it needs a measured band, not just a bpm range; the Training week board's `PlannedSessionCard` was extended with an optional, defaulted `hrZoneModel` parameter rather than threading a new dependency through `TrainingViewModel`/`TrainingScreen`/`WeekBoard.kt` (none of which the task's file list named), so its cards gain the structure section for free (any planned session that already carries one) while the chip stays off there until a future task wires it through; and the confidence sentence collapses `MEDIUM`/`LOW` into one "partly modelled" wording (`§3.10.2` names four blend rungs, not four sentences). |
| **P14.7** Body figure, exercises, workouts, set logging | sonnet | **yes** | 956 | 89.3 (release 14.4) | **Create** `ui/common/body/{MusclePaths,BodyFigure,BodyFigureLegend}.kt`: `MusclePaths` is Compose-free (a plain `BodyPoint`, not `Offset`) so it is a plain JVM test — `FRONT`/`BACK` maps of `MuscleGroup -> polygons` (~22 shapes a side incl. the outline pieces) in a normalised 100 × 220 box, `groupAt` (bounding-box hit test) and `pathsFor`; `highlightFor(exercise)`/`highlightFor(workout)` (primary 1.0, secondary 0.35, workout = union/max over its rows resolved off `ExerciseCatalog`, unknown ids contribute nothing) live here too since they need no Compose type. `BodyFigure` draws both silhouettes with plain `Canvas`, colouring each group `lerp(surfaceVariant, primary, intensity.coerceIn(0f,1f))` — continuous, so the same composable is P14.8's heat map — and takes optional `onFrontTap`/`onBackTap` for the Exercises screen's muscle filter. `BodyFigureLegend` renders primary/secondary or fresh/loaded/fatigued swatches off the same two colours. **Create** `ui/strength/{ExercisesUiState,ExercisesViewModel,ExercisesScreen,ExerciseDetailScreen,WorkoutsUiState,WorkoutsViewModel,WorkoutsScreen,WorkoutEditUiState,WorkoutEditViewModel,WorkoutEditScreen,ExercisePickerSheet,SetLogSheet}.kt`. **Exercises**: search + equipment chips + a tappable figure filter, all `AND`-ed over the constant `ExerciseCatalog` (no repository, no loading state); detail screen with a large figure (`highlightFor(exercise)`), equipment/pattern/unilateral/timed flags, cue, and "Add to workout…" (`AlertDialog` list over the live workout list; picking one appends a default-prescribed row — 3 sets, 10 reps or a 30 s hold for a timed exercise, bodyweight off the catalog — and re-saves the whole workout). There is deliberately no `ExerciseDetailViewModel`: both screens share `ExercisesViewModel`, which also seeds the six built-ins on open. **Workouts**: list with kind chip, exercise count + estimated minutes, a `BodyFigure` thumbnail (`highlightFor(workout)`); new / duplicate (`"name (copy)"`, incrementing on a clash) / edit / delete (`AlertDialog` confirm) / "Plan for a day" (a `DatePickerDialog`, then a `STRENGTH_*` `PlannedSession` with `workoutId` set and `targetDurationMin = estimatedMinutes`, through the same `PlanRepository.upsertSession` manual sessions already use — `sessionTypeFor(kind)` maps `CORE`/`CUSTOM` to `STRENGTH_FULL`, there being no dedicated session type for either). **Workout edit**: name, kind dropdown, ordered exercise rows — sets, then *one* numeric field (reps or seconds, fixed by the exercise's own `isTimed`, not a manual toggle that could disagree with the catalog), load/bodyweight switch, rest, note — with move up/down/remove, `ExercisePickerSheet` (a `ModalBottomSheet` over `ExerciseCatalog.search`, local query state, no ViewModel — the `LinkActivitySheet` pattern), a live figure + estimated minutes (`WorkoutEditDraft.estimatedMinutes`/`.highlight()`, computed straight from the draft so every keystroke stays in step), and per-row/name validation. Both `ExercisesViewModel` and `WorkoutsViewModel` seed the built-ins through a plain `suspend () -> Int` (`AppGraph.strengthWorkoutSeeder::seed` passed in as a function value) rather than the concrete `data/repository/StrengthWorkoutSeeder` type, which `ui/` cannot reference (`ArchitectureTest`). **Modify**: `ui/nav/{Routes,MyHealthNavHost}.kt` (+`WorkoutsRoute`/`WorkoutEditRoute`/`ExercisesRoute`/`ExerciseDetailRoute`), `ui/more/MoreScreen.kt` (+"Strength workouts"/"Exercises"). **Planned session integration**: `PlannedSessionDraft` gained `workoutId` (round-tripped in `toPlannedSession`/`plannedSessionDraftOf`) and `SessionType.isStrength()`; `PlannedSessionEditViewModel` gained `strengthRepo` and a `workouts` list, `PlannedSessionEditScreen` offers a workout dropdown ("None" + every workout by name) only when the session type is `STRENGTH_*`, and switching away from strength clears `workoutId`. `PlannedSessionCard` gained an optional `workoutName: String?` line; `TrainingViewModel` now also observes `strengthRepo.observeAll()` (`workoutsById` in `TrainingUiState`) purely to resolve that name and to know whether "Mark done" should open a set-log sheet, threaded through `WeekDayRow`'s new `workoutNames` map. `TrainingScreen`'s "Mark done" now checks the tapped session: with a *loaded* workout it opens `SetLogSheet` (one row per set, pre-filled from the prescription — exercise, set index, reps/seconds, load — each skippable); "Save" writes the checked-in rows as `StrengthSetLog` via `TrainingViewModel.completeStrengthSession` (`strengthRepo.insertSetLogs` then the existing `setSessionStatus(COMPLETED)`) and "Skip logging" completes the session with no log at all — a session with no workout (or one not yet loaded) behaves exactly as before. `res/values/strings.xml` +70. **Tests** (+11, 945 → 956): `MusclePathsTest` `bf01`…`bf05` (every group has ≥ 1 path; every point inside the box; a group's `FRONT`/`BACK` map membership matches its `side`; `highlightFor` clamps to 0..1 and maps primary 1.0/secondary 0.35; a workout's highlight is the max across its exercises and ignores an unknown id), `WorkoutEditUiStateTest` `swui01`…`swui06` (move/add-with-catalog-defaults/remove, `UPPER_A` → 44 min, reps-xor-seconds, blank name) — both pure, no Robolectric. `bash tools/verify.sh` → `VERIFY OK` (956 tests, 0 failures, lint 0 errors; debug 89.3 MB, release 14.4 MB), `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved**: no `ExerciseDetailViewModel` is in P14.7's file list, so the detail screen reuses `ExercisesViewModel` rather than adding an unlisted file; the polygon count landed at ~22 a side against the plan's "roughly 24/20" — `bf01`/`bf02` pin coverage and box bounds, not the count (risk K4, "correctness of the highlight map is what the tests pin, not the aesthetics"); and showing a planned session's workout name and gating "Mark done" on it required extending `TrainingViewModel`/`TrainingScreen`/`WeekBoard.kt` beyond P14.7's literal "Modify" list (only the three `training/{PlannedSessionCard,PlannedSessionEditScreen,PlannedSessionDraft}.kt` files are named there) — unavoidable since that is where the mark-done action and the week board's session data already live, and the change is additive (new optional parameters/fields, existing call sites unaffected). |
| **P14.8** Muscle-load card on Load & Recovery + Today hint | sonnet | **yes** | 960 | 89.8 (release 14.4) | **Create** `ui/load/MuscleLoadUiState.kt`: `topMuscleLoadRows` (the three most-loaded groups, AU rounded to whole numbers, ties broken by `MuscleGroup`'s own ordinal), `muscleLoadBandLabelRes` (reuses P14.7's `body_figure_legend_fresh/loaded/fatigued` — the row list can never disagree with its own legend), `muscleLoadHeatMap` (`BodyFigure`'s `highlight` map — `MuscleLoadState.intensityOf` per group, already `load/ref` clamped `0..1`), `hasMuscleLoadActivity` (every group at `0.0` AU — no session in the 14-day window — is the card's empty state) and `muscleLoadHint`/`todayMuscleLoadHint` (a `MuscleLoadHint` enum, `UPPER_DAY_FITS` when `lowerBody != FRESH` and `upperBody == FRESH`, `LOWER_DAY_FITS` when `lowerBody == FRESH`, `null` otherwise) + `labelRes()`. **Modify** `ui/load/{LoadUiState,LoadViewModel,LoadScreen}.kt`: `LoadUiState` gained `muscleLoad: MuscleLoadState?`; `LoadViewModel` gained `activityRepo`/`planRepo`/`strengthRepo` and a `resolveMuscleLoadState` (internal, module-visible) suspend function that re-derives `SuggestionMuscleResolver`'s activity→planned-session→workout join (P14.5) directly against the domain repositories — `ui/` may not import `data/repository` (`ArchitectureTest`), the same seam `ZonesViewModel` used for pace bands in P14.6 — minus the template-alternation half, which only the suggester needs; `LoadScreen` gained a "Muscle load" `SectionCard`: `BodyFigure(highlight = muscleLoadHeatMap(...))`, `BodyFigureLegend(LOAD_BAND)`, the top-three rows ("Quads — 42 AU · Loaded") and the hint line, or the empty-state message. **Modify** `ui/today/{TodayUiState,TodayViewModel,TodayScreen,TodayLoadCards}.kt`: `TodayUiState` gained `muscleLoad`; `TodayViewModel` gained `strengthRepo` and calls the same `resolveMuscleLoadState` (shared rather than duplicated, since both screens must agree); `LoadCard` (`TodayLoadCards.kt`) gained a `muscleLoad` parameter and shows the hint as a line **only** when `todayMuscleLoadHint` fires — stricter than the Load screen's own condition (see ambiguities). `res/values/strings.xml` +5 (`load_muscle_title`/`_empty`/`_row`/`_hint_upper`/`_hint_lower`). Previews: `LoadScreen`'s existing `LoadContentPreview` and `TodayScreenPreviews`' "Populated" preview now carry a sample `MuscleLoadState` (a long run yesterday, `CTL 45`) so both show the populated card/chip. **Tests** (+4, 956 → 960): `MuscleLoadUiStateTest` `mlui01`…`mlui04` (top-three ordering + ordinal tie-break; band label for all three bands; heat intensity clamped, e.g. `40/20 → 1.0`; hint selection for all four lower/upper band combinations plus the empty-state check). `bash tools/verify.sh` → `VERIFY OK` (960 tests, 0 failures, lint 0 errors; debug 89.8 MB, release 14.4 MB), `:app:compileDebugAndroidTestKotlin` green. **Ambiguities resolved**: the plan's own text ("Today: the same hint … when any lower-body group is FATIGUED") is stricter than the Load screen's hint condition (`LOADED` *or* `FATIGUED`) — since a group's band is a monotonic function of its load, "any lower-body group is `FATIGUED`" is exactly `state.lowerBody == FATIGUED` (`lowerBody` already tracks the most-loaded of the five), so `todayMuscleLoadHint` gates the same `muscleLoadHint` on that stronger condition and, because `LOWER_DAY_FITS` can never coexist with a fatigued lower body, only ever yields `UPPER_DAY_FITS` or `null` — Today therefore reuses the Load screen's own `load_muscle_hint_upper` string rather than adding a second copy of the same sentence. Second: P14.8's plan-file bullet named only `ui/today/TodayLoadCards.kt`, but the chip needs the muscle-load state itself, so `TodayUiState`/`TodayViewModel`/`TodayScreen` also gained the (additive, default-`null`) plumbing — the same kind of unavoidable extension P14.7's note already flagged for `TrainingViewModel`. |
| **P14.9** Instrumented + emulator pass | sonnet | **yes** | 960 | 91.0 (release 14.4) | **Create** `app/src/androidTest/java/com/myhealth/ui/{ZonesScreenTest,WorkoutScreenTest,MuscleLoadCardTest}.kt`. `ZonesScreenTest` seeds a profile and one RUN activity (3 days ago, 40 min of 1 Hz HR≈140/speed 3.0 m/s streams past the 10-min warm-up skip) via `activityRepo.upsert`, then asserts More → "Zones & paces" shows all five `"Z$n · <name>"` rows and at least one pace band containing "/km" (LOW confidence is enough — the engine only needs `inZoneSec ≥ LOW_MIN_SEC`, not a second activity, to produce a band from pure measurement). `WorkoutScreenTest` seeds the six built-in templates (opening Workouts triggers the seeder), opens "Upper A", clicks its first exercise's "Move down", renames it and saves in place (a built-in becomes an editable copy on save, per `WorkoutEditViewModel`'s own doc), asserts the renamed row shows in the list, creates a `STRENGTH_UPPER` `PlannedSession` for today directly through `graph.planRepo` (workoutId left `null`), then opens it from the Training board's overflow menu, attaches the renamed workout through `PlannedSessionEditScreen`'s `WorkoutPickerField` (clicking the "Workout" label, not the current value, to open the dropdown — the pattern already used for goal-type dropdowns) and confirms the card's "Workout: …" line. `MuscleLoadCardTest` seeds a RUN activity yesterday with `trimp = 220.0` set directly on the `ActivitySession` (no `daily_load` row needed — `ctl` defaults to `0.0`, giving `ref = 12.0` AU off the floor, which `MuscleDistribution.RUN`'s 25% CALVES share and a 1-day, 0.71× decay clears easily) and asserts Load & Recovery's "Muscle load" card shows its title and the `load_muscle_hint_upper` string. All three needed the LazyColumn-virtualization idiom already established in `SettingsPersistenceTest`/`BikeScreenTest` (`onNode(hasScrollAction()).performScrollToNode(...)` before touching a node that may not be composed yet, never a bare `performScrollTo()`/`waitUntilTextExists()` on one). **Fix** (pre-existing test broken by a P14 layout change): `SettingsPersistenceTest` — P14.6's new "Heart-rate zones" `item{}` sits between `ProfileSection` and `AppPreferencesSection`, so after scrolling down to `AppPreferencesSection`'s `settings_dynamic_color_switch` (both before setting the sleep target and again after `recreate()`), `ProfileSection` (and its "Sleep target" field) is now far enough above the viewport that the LazyColumn had already discarded it — both of the test's assumptions that it was still composed (`performScrollTo()` on the label's own node, then a bare `onAllNodesWithText("9.5")`) now `performScrollToNode` back to the label explicitly first. **Fix** (lint): `zones_polarisation_hint`'s `"Less than 70 % easy …"` tripped `StringFormatInvalid` (lint reads the substring `"% e"` as a space-flagged, conversion-less `%e`), escaped to `70%%` (a bare, non-consumed literal — the string is never run through `String.format`/`stringResource(id, args)`, so this is the same escape `zones_polarisation_summary_format` already uses, just resolved by AAPT rather than at format time). **Tests**: unit count unchanged (960 — this task adds only `androidTest` sources, which `tools/verify.sh` does not run); `bash tools/connected.sh emulator-5554` → **21/21 green** (18 pre-existing + 3 new), including `MyHealthMigrationTest.migration_5_to_6_adds_strength_tables_and_session_columns` (Room's runtime schema validation accepts the `ALTER TABLE`-added `workoutId` FK on `planned_session` — no fallback recreation needed). `bash tools/verify.sh` → `VERIFY OK` (960 tests, 0 failures, lint 0 errors; debug 91.0 MB, release 14.4 MB). **Ambiguities resolved**: the plan says "duplicate or open Upper A" — opening it directly and saving in place is simpler and is exactly what `WorkoutEditViewModel`'s own doc comment says a built-in does ("becomes an editable copy on save"), so no separate duplicate step was needed; and "create a planned session … through the graph (or the Training UI)" was read as *graph for creation, UI for the workout attachment* — the point of this test is exercising the P14.6/P14.7 `WorkoutPickerField` UI path, not the "Plan for a day" quick action `WorkoutsScreen` already offers (which would have attached the workout without ever exercising the editor's picker). |


## P15 — Body figure v2 and exercise animations

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P15.1** Jointed body model + rounded figure | opus | **yes** | 965 | 88.4 (release 14.4) | POLISH-18 closed. **Create** `ui/common/body/{BodyModel,BodyShapes,BodySegments,BodyMuscles}.kt` + `app/src/test/java/com/myhealth/ui/common/body/BodyModelTest.kt`. `BodyModel` is the jointed model: `BodySegmentId` (16 parts), `BodyFace` (FRONT/BACK — a view of the skeleton, deliberately *not* domain's `BodySide`, whose `BOTH` has no meaning for a silhouette), `BodySegment(id, parent, pivot, outline, muscles)` with pivot in the parent's local frame (in the box itself for the root `TORSO`) and outline/muscles in the segment's own, `BodyPose(angles)` in degrees about each pivot (`STANDING` = all zero), plus `BodyFrame` (cos/sin/tx/ty — no Compose, no `android.graphics`), `frames()` (memoised parent-chain walk) and ray-casting `pointInPolygon`. `BodySkeleton.FRONT`/`.BACK` share one set of bones: `BodyGeometry.segments(muscles)` builds the subject's left side and mirrors it onto the right, and `BodyGeometry.mirrored()` flips the whole skeleton for the back view (root about the box centreline, children about their parent's origin; `mirrorX` reverses the point order as well as negating x, so every subpath keeps the same winding and the union in `BodyFigure` cannot punch holes). `BodyShapes` turns hand-placed control points into smooth closed polygons — `smoothClosed` (closed Catmull-Rom, 5 samples per span, so ≥ 12 points per curve), `ellipse` (head, hands), `roundedCapsule`/`roundedBlock` (the rectus' six blocks), `mirrorX`. `BodySegments` holds the joints and the silhouette (rounded head at y ≈ 3.5…33, neck, shoulders sloping into a tapered torso, arms drifting outward from the shoulder so the hands hang clear of the hips, pelvis, thighs, calves, feet at y ≈ 216); `BodyMuscles` the regions — front: two pectorals, front deltoids, biceps, forearms, a six-block rectus, obliques, two quadriceps heads, adductors, tibialis; back: trapezius diamond, rear deltoids, triceps, forearms, two lats (V), lower back, two glutes, two hamstring heads, two gastrocnemius heads. **Modify** `ui/common/body/MusclePaths.kt` — same public API (`WIDTH`/`HEIGHT`/`Polygon`/`FRONT_OUTLINE`/`BACK_OUTLINE`/`FRONT`/`BACK`/`pathsFor`/`groupAt`/`highlightFor`), but the maps are now `BodySkeleton.worldPolygons/outlinePolygons` at `BodyPose.STANDING` and `groupAt` ray-casts instead of comparing bounding boxes (P14.7's boxes claimed the empty air beside a limb; the rounded figure would have made that much worse). `BodyFigure` gained `pose: BodyPose = BodyPose.STANDING` (recomputing the world polygons only when a caller passes a non-standing pose), unions the 16 body parts into one `Path` (`PathOperation.Union`, remembered per face/pose and scaled per draw through a `Matrix`) so the joints do not show as seams, strokes muscle regions with a lighter 0.6 dp hairline, and gives each silhouette **half** the row width (a weighted `Box`) — without that a caller constraining the width but not the height (the Load screen's heat map, `fillMaxWidth()`) let the front figure take the whole row and pushed the back one off the edge. Compose's canvas paints are anti-aliased by default, so no extra work was needed for smooth fills. **Tests** (+5, 960 → 965): `BodyModelTest` `bm01` (every standing polygon inside the 100 × 220 box, head above y = 10, soles below y = 205, both faces), `bm02` (a −90° `FOREARM_L` rotation swings the hand > 20 units across the body and up to the elbow's height, while `TORSO`/`HEAD`/`UPPER_ARM_L`/`THIGH_R` do not move), `bm03` (point-in-polygon for a square, a concave L's notch, the real chest region, and two points the old bounding-box test would have mis-claimed), `bm04` (every muscle point inside its segment's outline bounds, ±0.5), `bm05` (same segment ids and parents front and back, pivots x-mirrored — the root about `WIDTH`, children about 0 — and both silhouettes occupying the same place in the box). `bf01`…`bf05` unchanged and green. `bash tools/verify.sh` → `VERIFY OK` (965 tests, 0 failures, lint 0 errors; debug 88.4 MB, release 14.4 MB) and `:app:compileDebugAndroidTestKotlin` green. Emulator (VERIFICATION.md session 14): `emu_p15_01_figure_bench.png`, `emu_p15_02_figure_squat.png`, `emu_p15_03_heatmap.png`; the muscle-filter tap was re-checked by hand (tapping a pectoral toggles the CHEST filter on and off). **Ambiguities resolved**: the spec's `worldPolygons(side, …)` takes a new `BodyFace`, not domain `BodySide`, because a skeleton is drawn from exactly one of two viewpoints and `BOTH` would be meaningless; the model's type vocabulary lives in `BodyModel.kt` while the ~390 lines of shape data live in `BodySegments.kt`/`BodyMuscles.kt` (R10's 400-line ceiling beats the spec's single-file wording); `roundedCapsule` ended up carrying the abs blocks and `ellipse` the head and hands, with every other part a `smoothClosed` spline, since splines read as muscle and capsules read as the rectus' blocks. |
| **P15.2** Exercise animations | — | no | — | — | Planning note only (PLAN §P15.2): keyframe `BodyPose`s per `MovementPattern`, an optional `Exercise.animation` override, a Compose `rememberInfiniteTransition` driving `lerp(BodyPose, BodyPose, t)` into the `pose` parameter P15.1 added. Open question first: a 2-D frontal figure may not be able to show a hinge or a squat at all — a side-view `BodyFace` may be needed before any pose data is written. |

## P16 — My equipment and load progression (release 0.5.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P16.1** Data + engines | opus | **yes** | 983 | 88.9 (release 14.4) | **Domain**: `Feedback { TOO_EASY, EASY, HARD, TOO_HARD }`; `Profile.availableEquipmentJson` (`null` = everything); `ExerciseProgress` (per-exercise state) and `ExercisePrescription` (what to do today, never stored); `StrengthSetLog.feedback`. **`ProgressionDefaults`**: the 22 body-weight ratios (default 0.15), the per-hand set (incline DB press, DB row, both curls, farmer's carry), the increments (barbell/machine/cable 2.5 kg, dumbbell/kettlebell/band/medicine ball 1 kg), the rep ranges (barbell compound 5–8, other compound 8–12, isolation 10–15, counted core 10–20, plyometric 5–8, every hold 30–60 s) and `floorToIncrement`/`roundToIncrement`. **`ProgressionEngine`** (pure): `initial` floors `ratio × body weight` to the increment (`pg01` squat 80 kg → 40 kg × 5; `pg02` bench 78 kg → 30 kg — and 80 kg → 30 kg too, so P16.3's "32.5?" note is wrong by the plan's own rule), `next` is the double progression (`pg03`…`pg07`: +2 reps → at the top +5 % ≥ one increment and reps back to the bottom, 40 → **42.5**; `EASY` +1 rep / +2.5 %, 100 → 102.5; `HARD` nothing; `TOO_HARD` −5 % ≥ one increment, never below one, 40 → 37.5), bodyweight moves reps only (`pg08`) and a hold moves seconds (`pg09`, ±10/±5 s clamped to 30–60); the first feedback clears `isEstimated` (`pg10`); `prescription(exercise, state, bodyWeightKg)` is what the UI calls. **`ExerciseSubstitution`** + `EquipmentSetCodec`: same pattern + same primaries → shared primary → dropped (`eq01`…`eq03`), `null` equipment is byte-identical (`eq04`), and `UPPER_A` against {bodyweight, dumbbell} becomes push-up / DB row / pull-up / curl with the OHP and the pushdown dropped (`eq05`). **Room v7** (`MIGRATION_6_7`, three additive statements, `7.json` exported, `1–6.json` untouched): `profile.availableEquipmentJson TEXT`, `strength_set_log.feedback TEXT`, table `exercise_progress` (`exerciseId` TEXT PK); `BackupFile.CURRENT_SCHEMA_VERSION` 6 → **7** and the table is exported / replaced / merged (keyed on `exerciseId`, the device's own state wins). **Repository**: `observeProgress` / `getProgress` / `getAllProgress` / `upsertProgress` / `prescriptionFor`, and `saveSetLogs`, which inserts the rows **and** applies one `next` per exercise, returning the new state per exercise id for the P16.2 snackbar (`insertSetLogs` stays the plain insert; `TrainingViewModel` still calls it — P16.2 switches it over). `StrengthWorkoutSeeder` materialises every template through the substitution against `profile.availableEquipmentJson` (so `RoomSuggestionRepository.accept` does too, through `workoutFor`). Body weight for the estimate = latest measurement within 365 days, fallback 75 kg. §6.4 row 7 + a "P16.1 as built" note in PLAN §P16. androidTest `migration_6_to_7_adds_progress_table_and_columns` compiles (`:app:compileDebugAndroidTestKotlin` green); it needs an emulator to run (P16.3). **+18 unit tests** (965 → 983), lint clean |
| **P16.2** UI | — | no | — | — | Next: equipment chips in Settings, the "Only my equipment" switch, prescriptions on the workout rows and the planned-session card, feedback buttons in the set-log sheet, the Progression card |
| **P16.3** Emulator + release 0.5.0 | — | no | — | — | Expect the bench at **~ 3 × 5 @ 30 kg** for an 80 kg profile (rounded down, `pg02`), not 32.5 kg |

---

## P17 — Mobility exercises and mobility routines (release 0.6.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P17.1** Catalog, templates, suggester | opus | **yes** | 1000 | 88.4 (release 14.5) | **Enums (append-only, so every stored ordinal is untouched)**: `MovementPattern.MOBILITY`, `Equipment.FOAM_ROLLER`, `StrengthWorkoutKind.{MOBILITY_LOWER, MOBILITY_UPPER, MOBILITY_FULL}` (+ `StrengthWorkoutKind.isMobility`), `Exercise.isMobility`. **`ExerciseCatalogMobility.kt`**: **33** timed drills, ids prefixed `MOB_`, equipment bodyweight / band / foam roller, one cue each, `primary` = the group being mobilised; all 16 `MuscleGroup`s are a mobility primary (`mob02`). `ExerciseCatalog` gains `strength` / `mobility` and an `ExerciseKind?` argument on `search` / `filter` (`null` = the "All" chip, so `filter()` and `search("")` are still `ALL` and `ex09`/`ex10` are untouched). **Templates**: `MOBILITY_LOWER_A` / `MOBILITY_UPPER_A` / `MOBILITY_FULL_A`, 7–8 holds of 30–60 s with **15 s** rest (a `flow()` builder that refuses a non-mobility id and anything outside 30–60 s), each **21 min** — `StrengthWorkout.DEFAULT_REST_SEC` and the estimate formula are untouched, the short rest is per row. Seeder and `sw01`/`sw05` are now **nine** built-ins, still idempotent (`mob04`), and `accept` → `workoutFor` materialises a routine exactly like a lifting template. **Progression**: `ProgressionDefaults.carriesLoad` (bodyweight **or** mobility → no load, so the band pull-apart and the roller never get kilos), `FOAM_ROLLER` increment 1.0, holds stay 30–60 s. **Substitution** never crosses strength ↔ mobility (`mob09`). **Muscle load**: `MuscleDistribution.isMobilityOnly` (a `MOBILITY_*` kind, or every row a mobility drill) → `MuscleLoadEngine.sharesOf` deposits **nothing** (`mob08`) — mobility is recovery. **Suggester**: `StrengthRules.mobilityTemplateFor(state)` (lower ≥ LOADED + upper FRESH → lower routine, upper ≥ LOADED + lower FRESH → upper routine, else full), wired into `templateIdFor` for `SessionType.MOBILITY` so both the rest-day pass 7d and any placed `MOBILITY` session carry it, plus the `MOBILITY_FOCUS` rationale line; `workoutEntry` skips a mobility routine (it is announced by its own line). **Gating**: `SuggestionSnapshot.render` does not print `workoutTemplateId`, and the whole mobility layer follows the §3.12.5 gate (`ctx.enabled`), so `fixtures/suggest/sug28_baseline.txt` is byte-identical and was **not** regenerated; `sug39`/`sug43` pin it. `ui/strength/WorkoutsViewModel.sessionTypeFor` maps the three new kinds to `SessionType.MOBILITY` (needed to compile; the rest of the UI is P17.2). **+11 unit tests** (989 → **1000**): `mob01`…`mob09`, `sug42`, `sug43`; `sw01` renamed `sw01_nine_builtin_templates`, `sw05` and `RoomSuggestionRepositoryTest.accepting_a_strength_suggestion_materialises_its_workout` updated 6 → 9 rows. Lint clean |
| **P17.2** UI | — | no | — | — | Next: kind chips on Exercises, the three kinds in the workouts list/editor, "Routine: Mobility lower A" on a planned `MOBILITY` card, the foam-roller chip in Settings |
| **P17.3** Emulator + release 0.6.0 | — | no | — | — | versionCode 150 / 0.6.0 |

---

## P18 — Exercise animations (release 0.7.0)

| Task | Model | Done | Tests | APK MB | Notes |
|---|---|---|---|---|---|
| **P18.1** Pose model, side view, clips for all 87 | opus | **yes** | 1011 | 88.6 (release 14.5) | **Pose types to the domain**: `domain/model/Body.kt` (which already held `BodyMeasurement`) gains `BodySegmentId`, `BodyFace {FRONT, BACK, SIDE}` and `BodyPose(angles, rootOffsetX, rootOffsetY, rootAngle, **rootScale**)` + `STANDING` / `lerp` / `mirrored`; the geometry stays in `ui/common/body` and imports them. `rootScale` is the one field beyond the plan and is not optional — a 213-unit figure cannot lie horizontally inside a 100-wide box, and `an03` pins every keyframe inside it; it is constant per clip (`an02`) so the figure never pulses. `worldPolygons`/`outlinePolygons` apply scale → rotate → translate about the standing hip joint (50, 110) **after** the joint chain (`rootFrame`). **Side view**: `BodySkeleton.SIDE` + `MusclePaths.SIDE` (`BodyProfile.kt` / `BodyProfileMuscles.kt`, own files for R10) — a profile facing +x with the same sixteen segment ids and the same joint heights as the front view, its own head/torso/foot shapes, the far limbs drawn 1.6 behind, and **15 of 16** muscle groups (only `ADDUCTORS` is invisible edge-on). `MusclePaths.SIDE` is kept out of `pathsFor`, so `bf01`…`bf05` are untouched — `bf03` needed no change because it only speaks about the FRONT/BACK maps; `bm04` was widened to check the profile too, and `BodyFigure` now draws an explicit FRONT + BACK list instead of `BodyFace.entries`. **Clips**: **44** (`AnimationClips.kt` 33 + `AnimationClipsMobility.kt` 11, re-exported into one namespace; the named-joint DSL and the `Keyframe`/`AnimationClip` types in `AnimationPoses.kt`), 2–4 keyframes each, five timing presets, 15 flagged `mirror`. **`ExerciseAnimations`**: an explicit table for all **87** ids with an `init` check against `ExerciseCatalog` (a new catalog entry fails the build), `clipFor(id): AnimationClip?`, `clipFor(exercise)`, `defaultFor(pattern)` and `clipOrStanding(id)` — the last is the one that lands on the static `STANDING` clip for an id nothing knows. **+9 unit tests** (1002 → **1011**): `an01`…`an09`. Conventions, the clip list and the known rough edges are written up under `### P18` in PLAN.md |
| **P18.2** Rendering + screens | — | no | — | — | Next: `AnimatedBodyFigure.kt`, exercise detail / picker / editor / set-log; `anui01`…`anui03` |
| **P18.3** Emulator + release 0.7.0 | — | no | — | — | versionCode 160 / 0.7.0; squat / push-up / pigeon screenshots at two keyframes |

---

## Roll-up

| Phase | Tasks | Done |
|---|---|---|
| P0 | 5 | 5 |
| P1 | 11 | 11 |
| P2 | 10 | 10 |
| P3 | 7 | 7 |
| P4 | 13 | **13** |
| P5 | 9 | **9** |
| P6 | 8 | **8** |
| P7 | 6 | **6** |
| P8 | 8 | **4** |
| P9 | 6 | 0 |
| P11 | 3 | **3** |
| P12 | 6 | **6** |
| P13 | 1 | **1** |
| P14 | 10 | **9** |
| P15 | 2 | **1** |
| P16 | 3 | **1** |
| P17 | 3 | **1** |
| P18 | 3 | **1** |
| **Total** | **114** | **95** |


package com.myhealth.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 defaults — no custom design system and no custom fonts (§1.7).
 */
val MyHealthTypography = Typography()

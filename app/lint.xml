<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Versions are pinned deliberately (docs/TOOLCHAIN.md); upgrade advice is informational. -->
    <issue id="GradleDependency" severity="warning" />
    <issue id="NewerVersionAvailable" severity="warning" />
    <issue id="AndroidGradlePluginVersion" severity="warning" />

    <!-- English-only app. P8.1 moved the user-facing strings in ui/** into res/values/strings.xml;
         HardcodedText is an error from here on so no new literal creeps back into a layout. -->
    <issue id="HardcodedText" severity="error" />
    <issue id="MissingTranslation" severity="warning" />

    <!-- API-level misuse must never ship. -->
    <issue id="NewApi" severity="error" />
    <issue id="InlinedApi" severity="error" />
</lint>
